# Log de defectos introducidos

Una fila por defecto que **yo introduje** (no por bug heredado ni por hallazgo de auditoría
sobre código viejo). Existe para que "¿estamos mejorando?" deje de ser una sensación.

Nació el 2026-08-04, cuando Favio observó que seguían apareciendo errores pese al cambio de
modelo, y quedó claro que el problema no era el modelo: había **siete memorias** de feedback
diciendo variantes de "verificar mejor" y una de ellas se violó al día siguiente de escribirla.
Una memoria no es un control. Un control es algo que corre y falla.

## Cómo se usa

- **Una fila por defecto**, el día que se descubre. Incluye los que se encuentran y arreglan en
  la misma sesión: esos son justamente los que enseñan barato.
- **La columna que importa es "clase"**, no la severidad. Si una clase se repite, falta un
  control mecánico, no disciplina.
- **"Lo encontró"** distingue lo que se atrapó solo de lo que se atrapó de casualidad. Si la
  mayoría dice "Favio" o "revisión posterior", la verificación previa no está funcionando.
- El retro semanal (`/retro`) lo lee. Sin esto, cada sesión empieza creyendo que la anterior
  fue limpia.

## Clases vistas hasta ahora

| Clase | Qué la produce |
|---|---|
| `camino-feliz-unico` | verifiqué lo que construí, no la rama de error / el usuario del muro / el kill duro |
| `efecto-lateral-al-deduplicar` | unifiqué dos caminos que *decían* lo mismo sin mirar qué *hacían* además |
| `consumidor-no-actualizado` | agregué estado nuevo y no actualicé a todos los que lo leen o lo copian |
| `error-no-leido` | supabase-js no lanza; sin leer `{error}` un fallo se lee como "no había nada" |
| `barrido-de-un-solo-arbol` | escribí el guard que cierra un hallazgo mirando solo el repo donde lo encontré, cuando el mismo hallazgo vive en los dos canales |
| `antivacuidad-anclada-al-defecto` | el control anti-vacuidad se apoya en que el problema siga existiendo, así que arreglarlo rompe el build |
| `cobertura-alrededor-del-cambio` | los tests que escribí junto al fix cubren todo menos el cambio de comportamiento en sí; revertirlo los deja verdes |
| `barrido-ciego-por-preprocesado` | el paso que limpia el fuente antes de barrerlo borra justo el texto que había que mirar |
| `asercion-vacua-por-implicacion` | la aserción tiene forma `if (P) expect(Q)`, así que si P deja de cumplirse el test pasa sin comprobar nada |
| `causa-inventada-junto-al-numero` | el número es real y verificable, la explicación que va al lado no se midió; corregir el número deja la causa falsa intacta y se propaga en copias |
| `guard-limitado-al-corpus-existente` | el guard compara dos implementaciones sobre lo que HAY en el árbol, así que una divergencia sobre una ruta que todavía no tiene archivos es invisible |
| `correccion-que-abre-la-direccion-opuesta` | al arreglar un falso negativo ensanché la condición y abrí un falso positivo; el caso original queda cubierto y la prueba por mutación solo mira ese lado |
| `paridad-entre-proyecciones` | el guard compara dos copias por una PROYECCIÓN (un conjunto, un veredicto sobre lo que existe) en vez de por lo que son; por cada proyección hay mutaciones que no la cruzan, así que taparle una deja las otras |
| `evidencia-que-no-discrimina` | la observación es real y se cita como prueba, pero es igual de compatible con la hipótesis contraria: no separa las dos, solo no las contradice |
| `cobertura-que-cuenta-lo-que-no-verifica` | la métrica de cobertura cuenta apariciones en vez de casos DECISIVOS, así que informa un número alto justo donde no se probó nada |
| `justificacion-que-no-se-midio` | el comportamiento es una decisión declarada en un comentario y hasta fijada en un test, pero el ARGUMENTO que la sostiene nunca se ejercitó y es falso; el test documenta la decisión sin validar su premisa |
| `guard-que-bendice-lo-que-vino-a-vigilar` | el guard acepta como prueba de validación algo que el código puede fabricar solo (una palabra, un nombre local), así que el patrón que venía a prohibir pasa en verde — y a veces ya estaba pasando |
| `orden-de-operaciones-que-invalida-el-contrato` | los pasos son todos correctos y están todos, pero en un orden que hace que la función devuelva justo lo que su contrato promete rechazar; el test del valor frontera pasa y el que redondea hasta él no existe |
| `guard-que-afirma-sobre-el-intermedio` | el test compara contra el valor que produce MI función (el patrón escapado, el payload armado) en vez de contra lo que el sistema de destino hace con él; si el destino lo reinterpreta —PostgREST traduciendo `*` a `%`— el guard confirma el paso intermedio y no ve el resultado |
| `guard-partido-por-una-extraccion` | partí una función en dos y el guard que la miraba quedó cubriendo una mitad; la misma mutación que ya se había verificado pasa de nuevo, un nivel más abajo |
| `equivalencia-verde-por-casualidad` | el test declara que dos implementaciones son equivalentes y no lo son; queda verde porque ningún caso de prueba toca la diferencia, y todo lo que se apoye en esa equivalencia hereda el hueco |
| `mis-guards-tambien-fallan` | el guard que escribí para cerrar un hallazgo tiene su propio hueco, y casi siempre es el mismo: mide TEXTO (una cadena, un nombre, un estilo) en vez de comportamiento, así que la próxima persona lo evade sin querer |
| `conclusion-mas-ancha-que-la-medicion` | medí UNA parte y escribí la conclusión sobre EL TODO ("el modelo es correcto" tras verificar una de sus reglas). El dato es real, el alcance no; y suele convivir con un instrumento que en la misma corrida está avisando qué quedó sin ejercitar |
| `invariante-con-una-puerta-sin-barrer` | establecí una regla sobre una columna en el camino que estaba mirando, y no fui a buscar los OTROS que escriben la misma columna. El invariante existe, está documentado, tiene tests — y una puerta que nadie nombró lo pisa |
| `corrida-truncada-que-sale-verde` | canalicé la salida de una herramienta de verificación por algo que la corta (`head`, `grep -m`), el proceso murió de SIGPIPE a mitad, y el reporte parcial salió con **exit 0** y con el mismo aspecto que uno completo |
| `binding-que-choca-en-el-scope` | reutilicé un nombre (`error`) que ya estaba declarado más abajo en la misma función; no es un bug de lógica, es un fallo de carga que sólo aparece si algo compila el archivo antes de que corran los tests |
| `asercion-atada-al-calendario` | la aserción compara contra `hoy` mientras el fixture lleva un valor FIJO, así que sólo puede pasar el día en que se escribió. Si además el harness no está en el canary, se pone roja sin que nadie se entere |
| `el-destino-reescribio-el-valor` | escribo un valor y después comparo contra el literal que escribí, pero la capa de destino (un trigger, un normalizador, un serializador) lo transformó en el camino. La comparación no lanza ni falla ruidosamente: **nunca acierta**, así que el valor especial se comporta como si no existiera y se pinta como si fuera un dato normal. Prima hermana de `guard-que-afirma-sobre-el-intermedio`, pero en código de producción y no en un guard |
| `movido-a-donde-no-sobrevive` | bajé algo compartido al árbol que lo USA, mirando quién lo consume y no cuánto tiene que durar. El consumidor está bien mientras esté montado; lo que se pierde es el efecto que se dispara **justo antes de desmontarlo** — típicamente un aviso seguido de una navegación. No lanza, no se loguea: el aviso simplemente no aparece en ninguna pantalla |
| `gate-de-la-query-equivocada` | tapé el bloque que falla con la señal de error de OTRA query. Mientras las dos caen juntas parece correcto; el día que cae sólo una, el bloque se pinta entero con los ceros de la que no responde y el estado de error queda escondido en una pestaña que quizá nadie tiene abierta |
| `ampliar-el-alcance-sin-reapretar-el-predicado` | saqué un predicado del contexto acotado donde vivía (una ventana de 48h, una rama) para que decida en TODOS los casos, sin volver a mirarlo con esa lupa. La laxitud que era tolerable cuando corría poco —un regex sin borde de palabra, una tolerancia ancha— pasa a estar permanentemente alcanzable, y el falso positivo deja de ser raro |
| `guard-que-afirma-mas-de-lo-que-ejercita` | el guard se ejecuta con un fixture que corta antes de llegar a lo que dice cubrir (una lista vacía, un gate horario que no pasa), así que su conteo describe una muestra recortada. La mutación obvia lo mata igual, y eso alcanza para creerle. Prima de `mis-guards-tambien-fallan`: allá el problema es medir texto, acá es no llegar |
| `predicado-copiado-sin-mirar-la-nulabilidad` | copie un predicado que funciona (`IS DISTINCT FROM`) a otra tabla sin verificar que la columna tenga la misma nulabilidad. Con NULL el operador da TRUE y la condicion se invierte en silencio: no lanza, no loguea, simplemente decide al reves. Y el fixture del harness suele traer la columna llena, porque uno la llena al sembrar aunque produccion la deje vacia |
| `limpieza-que-corta-al-primer-fallo` | el cleanup de un harness es un try/catch ÚNICO, así que un statement roto aborta el resto. Lo que queda huérfano no es lo primero sino lo ÚLTIMO — y ahí suele estar justo lo que ningún cascade alcanza (Storage, `auth.users`), o sea lo que sobrevive a cualquier limpieza posterior |
| `estimador-que-cambia-un-error-por-otro` | la primera versión de una medición falla hacia el lado seguro, la "corrección" la manda al peligroso, y ninguna de las dos admite que la muestra no alcanza para la pregunta que se le hace |
| `la-unidad-del-barrido-no-es-la-linea` | el guard barre línea por línea algo que se escribe en varias (un template literal, una concatenación, una variable armada antes), así que parece más ancho y es ciego |
| `el-mock-no-implementa-lo-que-el-bug-usa` | el doble de la dependencia deja como no-op justo la operación de la que depende el bug (los filtros de una query, el orden de un sort), así que el guard corre, pasa y **no puede distinguir el código arreglado del roto**. Se detecta con una sola prueba: reintroducir el bug y ver si el test sigue verde |
| `mutacion-que-no-se-aplico` | corrí la prueba por mutación con un patrón que no matcheó el archivo (CRLF, indentación), así que el guard nunca vio el bug reintroducido. La salida es **idéntica** a la de una mutación que sobrevive: tests en verde. Se cierra de una sola forma — verificar que el archivo CAMBIÓ antes de creerle al resultado |
| `corte-por-un-separador-que-vive-dentro-del-dato` | partí un texto por un delimitador sin comprobar que ese carácter no formara parte del VALOR. La coma que separa dos frases es la misma que separa los decimales, así que el corte se comió parte del monto y lo dejó plausible: no lanza, no queda vacío, entra un número más chico con una confirmación arriba |
| `arreglo-que-desalinea-al-vecino` | corregí una comparación para un tipo de dato y desalineé al OTRO que se compara contra lo mismo. Un timestamptz y una DATE viven en líneas de tiempo distintas: mover el borde para que uno caiga bien tira al otro un mes atrás. El comentario suele SABER que hay dos tipos y sacar la conclusión para uno solo |
| `dos-relojes-para-la-misma-pregunta` | dos funciones deciden sobre la misma fila preguntando por instantes distintos (una "alguna vez", otra "hasta ahora"). Mientras los datos son sanos coinciden; con una fecha del futuro la misma persona cae de los dos lados de un ratio. El comentario al lado suele afirmar que ya es el mismo instante |
| `dos-ramas-de-la-misma-fila-decididas-por-separado` | dos columnas de la misma fila de un reporte se resolvieron en momentos distintos con criterios opuestos (una suma el evento, la otra lo suprime "para no contradecir"). Cada una es defendible sola; juntas, la fila deja de reconciliar justo en el caso que motivó el cambio |
| `inventario-incompleto-de-lo-que-decide` | el guard vigila ALGUNAS de las columnas de las que depende la decisión. Las vigiladas están cubiertas y la que falta hace más daño que todas: nada falla, el número sale distinto, y la suite entera queda verde |
| `negativo-que-rechaza-por-otra-condicion` | el control negativo existe, pasa, y el motivo escrito al lado es falso: la función lo rechaza por una condición distinta de la que el test dice cuidar. Verde permanente sobre un caso descubierto. Se detecta descomponiendo la regla y exigiendo que el negativo caiga por UNA sola condición |
| `guarda-del-lado-equivocado-del-corte` | apliqué la guarda a una de las dos mitades DESPUÉS de partir, cuando lo que la guarda vigila puede haberse mudado a la otra mitad con el corte. Contar sobre el fragmento en vez de sobre el original la vuelve ciega por construcción, no por un caso olvidado |
| `asercion-que-repite-la-formula` | la aserción del test re-deriva lo que la implementación ya garantiza por construcción (`x % n < n`), así que es cierta pase lo que pase. No hay mutación del código de producción que la pueda matar: el test se lee como cobertura y no puede fallar nunca |
| `nombre-que-no-resuelve` | el comentario remite a otro símbolo (una función, un archivo, un test) por un nombre que no existe. No rompe nada al ejecutar, y rompe justo lo único que el comentario hacía: mandar a alguien a mirar el otro sitio. Se cierra con un `grep` del nombre, igual que un import |
| `perimetro-de-un-salto` | el guard deriva su alcance siguiendo UN nivel de dependencia, así que mover una query un archivo más adentro la saca de jurisdicción — y ese refactor es justamente el que produce la clase que el guard vigila. El síntoma es cruel: el hermano de lo que acabás de arreglar, en el mismo archivo, delegando en otro |
| `regla-aplicada-sin-verificar-su-premisa` | apliqué una rama de una taxonomía por su FORMA ("tira, que el catch de abajo ya loguea") sin ir a mirar si la premisa se cumple en ESTE call-site. El arreglo queda escrito, un test lo fija, y no compra nada: el catch de abajo era silencioso |
| `negativo-producido-por-el-instrumento` | el probe reporta que el sujeto NO hace algo, y el que no lo hace es el probe. Tres mecanismos distintos con el mismo síntoma: el sujeto lo detecta y se niega a actuar; la propia intervención del probe rompe lo que iba a medir; o el dato viaja y el probe no sabe decodificarlo. Un `NO_REPORTA` sólo vale si antes hay un CONTROL de que el instrumento ve algo |
| `gotcha-documentado-al-lado-y-reintroducido` | escribí un archivo tomando de modelo a su hermano, y reintroduje el bug que el hermano documenta en un comentario de seis líneas. Leerlo no alcanzó: el camino que lo dispara no es el feliz, así que sólo aparece probando el fallo a propósito |
| `repeticion-que-no-es-muestra` | corrí N veces y promedié, pero las N no eran observaciones independientes (un cache del lado del servicio devolvió la misma). La mediana sale convincente, el rango sale sospechosamente angosto, y no hay nada en la salida que lo delate salvo el TIEMPO de cada corrida |
| `verde-mas-ancho-que-lo-que-protege` | el guard acepta con una condicion mas laxa que la que su `on_fail` promete vigilar ("al menos UNA de cuatro metricas" cuando el canary protege tres umbrales concretos). No es que no vea: es que su verde y su promesa hablan de cosas distintas, y el que lee el verde cree la promesa |
| `configuracion-fijada-a-medias` | fijé en el código la propiedad que decide el ON/OFF y dejé cayendo al control remoto la propiedad HERMANA que decide el alcance. El agujero que el cambio dice cerrar sigue abierto un nivel más abajo, y encima el diff se lee como si estuviera cerrado |
| `enumeracion-que-no-cubre-el-conjunto` | escribí una lista explícita de casos (rutas, endpoints, sufijos) donde lo correcto era una regla sobre el conjunto. Funciona para todo lo que enumeré y falla en silencio para el que no conocía, y la afirmación escrita al lado —"cubre todos"— queda falsa sin que nada la contradiga |

## Registro

| Fecha | Defecto | Clase | Lo encontró | ¿Había un control que debió atraparlo? | Control que quedó |
|---|---|---|---|---|---|
| 2026-08-03 | `try/catch` tragándose errores de PostgREST en el barrido de la webapp | `error-no-leido` | barrido posterior de Favio | No | `auth-callsites.test.ts` ya existía para el mapeo; el patrón quedó en la memoria de verificación |
| 2026-08-03 | Fetch extra para 42 de 48 usuarios (el del muro recibe 402 y el fallback era el camino normal) | `camino-feliz-unico` | barrido posterior de Favio | No | doble gate `useBootstrapGate` + `enabled: proPagado` |
| 2026-08-03 | Copy que prometía un botón que no renderiza en 1 de 4 estados | `camino-feliz-unico` | barrido posterior de Favio | No | — |
| 2026-08-03 | Harness que dejaba filas en prod si el proceso muere sin `finally` | `camino-feliz-unico` | barrido posterior de Favio | No | preclean por marcador al inicio |
| 2026-08-04 | `/premium` delegado al intent arrastró `solicitarComprobante`: 48h en que la foto de un gasto se rechaza sin registrarlo, justo al usuario del muro | `efecto-lateral-al-deduplicar` | **pregunta de Favio**, no mi verificación (685 tests + E2E del muro 35/35 + 3 curls estaban verdes) | No | `tests/handlers/premium-comando-sin-comprobante.test.js` (10 casos, guard probado fallando) |
| 2026-08-04 | Los tres fixes de la Ola 1 declarados "listos" con la rama de error / el camino real sin ejercitar (B1, B5, M8) | `camino-feliz-unico` | **pregunta de Favio** | No | `gmail-guardar-tokens-errores.test.js`, `qa-referido-premio-trial.mjs` (al canary), `plan-limites-paridad.test.js` |
| 2026-08-05 | Declaré S4 cerrado con el backend todavía emitiendo los MISMOS dos códigos de invitación desde `Math.random()`. El guard que escribí para cerrarlo barre `webapp/src`, así que no podía verlos, y el ledger decía "las 4 pasan por lib/codigos-seguros" cuando eran las 4 **de la webapp** | `barrido-de-un-solo-arbol` | re-verificación del ledger contra el código a pedido de Favio; ninguna suite lo agarró (818+159 verdes) | No | `tests/codigos-seguros.test.js` (barrido del runtime backend) + `tests/services/spaces-invite-code.test.js` (que el helper se ESCRIBA), los dos probados por mutación |
| 2026-08-05 | Cambié el alfabeto de los códigos de espacio a mayúsculas y NO moví `unirseEspacio`, que busca exacto. Un código dictado por teléfono y tipeado en minúsculas devolvía "no encontré ese espacio". Antes del cambio la case-sensitivity protegía algo (había códigos con `a` y con `A`); después ya no, y solo perdía joins | `consumidor-no-actualizado` | **revisor adversarial** (yo lo había marcado como "lo dejo a propósito") | No | exacto primero + reintento en mayúsculas, que conserva el código legacy; `maybeSingle` para no colapsar "no existe" con "falló la lectura" |
| 2026-08-05 | El guard nuevo de S4 barría 6 subdirectorios y dejaba **10 archivos fuera, incluido `gmail.js`** — el más denso en credenciales del backend, que vive en la raíz. Y su regex de palabras era solo en inglés: un `generarClaveDeAcceso()` con la variable `clave` pasaba verde, en un repo que nombra en español | `barrido-de-un-solo-arbol` | **revisor adversarial**, con una sonda que replicó el barrido y le pasó 6 generadores; 6 de 7 pasaron | No | raíz + `scripts/` al barrido; palabras en español al regex; `Math['random']()` y el alias al detector; 5 contrapruebas nuevas con las formas que se colaban |
| 2026-08-05 | La antivacuidad de ese guard era `conMathRandom > 0`, y ese contador valía **1**: el único archivo con `Math.random` era `lib/formatters.js`, que está EXENTO. Migrar `generarRefCode` a `crypto` —la mejora que el propio guard recomienda— lo tiraba a 0 y rompía el build | `antivacuidad-anclada-al-defecto` | **revisor adversarial** (lo midió, no lo dedujo) | No | contar bytes leídos, que mide si el barrido mira algo y no se puede romper arreglando código |
| 2026-08-05 | Escribí el test de wiring ("que el helper de verdad se ESCRIBA") solo para `crearEspacio`. `compartir_meta` emite la misma clase de credencial y quedó sin equivalente: la mutación "llama al helper y guarda 'AAAAAAAA'" dejaba las 15 aserciones verdes | `camino-feliz-unico` | **revisor adversarial** | No | `tests/handlers/metas-invite-code.test.js`, probado con esa mutación exacta |
| 2026-08-05 | El único cambio de PRODUCCIÓN de mi fix (el reintento en mayúsculas de `unirseEspacio` y el `throw` sobre lectura fallida) viajó **sin un solo test**. Los tres tests que agregué cubrían el guard estático y el wiring de metas; los que ya tocaban esa función pasan `'CODE1234'`, ya en mayúsculas, así que el reintento nunca corría. Revertir el cambio entero los dejaba a todos verdes | `cobertura-alrededor-del-cambio` | **segunda revisión adversarial**, midiéndolo por construcción | No | `tests/services/spaces-join-normaliza.test.js` (9 casos), visto fallar contra las 4 mutaciones: sin reintento, sin trim, `throw`→`return null`, y upcasear la entrada (que rompe el código legacy) |
| 2026-08-05 | `soloCodigo()` del guard cortaba la línea en la primera `//`, y **toda URL literal lleva `//`**: `const link = 'https://app.neto.pe/join/meta/' + inviteCode` se barría como `const link = 'https:`. Cualquier `Math.random()` a la derecha de una URL era invisible, en los dos archivos que el guard existe para proteger. Faltaban además `password`, `contraseña` y el destructuring `const { random } = Math`; y `pin` sin boundary inicial matcheaba dentro de `spin` | `barrido-ciego-por-preprocesado` | **segunda revisión adversarial**, con una sonda que midió 6 de 7 generadores pasando verde | No | el `//` exige inicio de línea o espacio delante; palabras y formas nuevas al detector; 9 contrapruebas más; `key` salió porque la exención es por ARCHIVO y un falso positivo cuesta la cobertura entera del archivo |
| 2026-08-05 | `estaEnMuro` de la webapp —del que cuelga el paywall entero del dashboard (`dashboard-shell.tsx:141`)— no tenía **ningún** test directo. La única aserción que lo mencionaba era `if (estaEnMuro(u.plan)) expect(...)`: al romperlo para que devolviera `false` siempre, la implicación se cumplía por vacuidad y los 159 tests + `tsc` seguían verdes. Un bug que abre el dashboard completo al que no paga llegaba a producción sin que nada lo frenara | `asercion-vacua-por-implicacion` | salió de romperlo a propósito para probar el gate de deploy, no de leer el test | No | 5 casos directos sobre `estaEnMuro` (incluido fail-closed con `undefined`) + antivacuidad sobre el filtro de la implicación vieja. La misma mutación mata 4 tests; antes mataba cero |
| 2026-08-07 | "Prod se quedó 14h en el commit sin gatear **porque los tres commits siguientes fueron de docs**". Las 14h eran reales —ya corregidas desde un "30h" que sí estaba 2x mal—, pero la causa era falsa entera: fueron **cuatro** commits, **uno solo** de docs, dos tocaron `tests/` (ruta observada) y a uno lo frenó **el gate funcionando**, que es lo contrario del punto. Y 13h15m de las 14h no hubo un solo push, o sea que la métrica medía que Favio dormía. Estaba en **cuatro** copias: CLAUDE.md, la memoria, la fila Q9 y el comentario del hook | `causa-inventada-junto-al-numero` | **pregunta de Favio** ("buscá números sin respaldo"); la corrección del 30→14 del día anterior había dejado la causa intacta | No | la línea de tiempo real con `git log --format=%cd` en las cuatro copias, y la memoria `feedback_numeros_sin_medicion`. Salieron en el mismo barrido `~850 pruebas`, `hasta 14h` del canary (la cota es ~24h) y `18 crons` en dos bullets (son 24 `setInterval`, 16 en el tick de 15min) |
| 2026-08-07 | El test de paridad de `watchPatterns` no veía una divergencia sobre una ruta sin archivos versionados: agregar `'infra/'` al predicado y no a `railway.json` (o al revés) pasaba las **cuatro** pruebas en verde. La dirección peligrosa es que el harness sub-reporte, porque entonces `backend-deploy-fresh` da PASS sobre un backend genuinamente stale | `guard-limitado-al-corpus-existente` | **pregunta de Favio** ("¿por dónde barre y no solo qué busca?"), medida con dos mutaciones | El guard era el control, y el control tenía el hueco | exclusiones sacadas a datos (`EXCLUSIONES`) y comparadas como **conjuntos**, además del corpus. Las 5 mutaciones fallan; antes 2 pasaban |
| 2026-08-07 | La clasificación de errores de `gh` en `backend-deploy-tested` miraba `stderr + message`, y `message` es `"Command failed: gh api .../runs?head_sha=<40 hex>"`. Un sha que contenga `404` (≈1% de los sha) convertía cualquier caída de red o de auth en un falso `GUARD CIEGO: ¿se renombró ci.yml?`, mandando a arreglar algo que no pasó. El bug original era el opuesto (miraba solo `message`, donde `gh` no escribe nada, y la rama no se alcanzaba nunca) | `correccion-que-abre-la-direccion-opuesta` | **revisión adversarial del propio cierre**, a pedido de Favio | La prueba por mutación que encontró el bug original solo ejercitaba el 404 real, no el sha contaminado | clasificar solo por `stderr` + `tests/backend-deploy-tested-errores.test.js` con el fixture malicioso (red caída + sha con "404"), visto fallar al revertir |
| 2026-08-07 | `backend-deploy-tested` tomaba la conclusion del RUN de `ci.yml` como respuesta a "¿el backend pasó los tests?", pero ese run también corre `webapp`, `deploy-webapp` (un `vercel deploy`) y `railway-gate`. Un token de Railway vencido o un Vercel caído lo ponían rojo y el harness decía "api.neto.pe corre código cuya suite no salió verde", mandando a arreglar una suite verde. Es el mismo argumento que eligió `ci.yml` sobre los check suites, sin aplicar un nivel más adentro | `camino-feliz-unico` | **revisión adversarial del propio cierre**; lo había reportado y decidido NO arreglar por "solo alcanzable en la ventana del fail-open", hasta que Favio pidió cerrar todo | No | lee el job `test` y emite un quinto veredicto; `clasificarRojo()` puro, probado contra listas de jobs **reales** (runs 31041521401 y 31045807530). Lista ilegible o job renombrado **no** se asumen sanos |
| 2026-08-07 | En el detector nuevo del fail-open, el control de legibilidad era `Number.isFinite(new Date(v).getTime())`, y `new Date(null)` es **epoch 0**, no `NaN`. Un `deployTerminadoAt` ausente pasaba el control como una fecha de 1970 y salía clasificado `FAIL_OPEN`: alarma máxima por un dato que faltaba, sobre un guard cuyo valor entero es no gritar en falso | `camino-feliz-unico` | **mi propio test**, antes del commit. Es la única fila del log encontrada así, y no fue suerte: el corazón se extrajo como función pura para poder probarlo contra los timestamps del incidente real, y el caso "datos ilegibles" vino con esa estructura | Sí, y funcionó | helper `instante()` que exige string no vacío; el test cubre `null` y `'no-es-una-fecha'`, y ninguno puede devolver `ok: true` |
| 2026-08-07 | El único test flaky de la suite (1 de cada 10 corridas): `require('../../cron/checks')` **dentro del `it`**, donde cargar medio backend se cobra contra el `testTimeout` de 10s. Con la máquina cargada tardó 36.533ms y el test murió por timeout. Los otros dos archivos que usan `cron/checks` lo requieren a nivel de módulo y nunca flakearon | `camino-feliz-unico` | **insistencia de Favio** en cerrar el cabo suelto; yo lo había reportado como "no reproducible" tras 10 corridas sin capturar el nombre | No: `npm test` es verde 9 de cada 10 veces, que es peor que rojo siempre | `require` a nivel de módulo, como los otros dos archivos. El test pasó de 36.533ms a 1-5ms; 5 corridas completas limpias (873 tests) |
| 2026-08-07 | El cierre del hueco anterior: dije que sacar las exclusiones a un objeto y compararlas **como conjuntos** cerraba el agujero, y "las 5 mutaciones fallan". Falso. Tres mutaciones distintas pasan 5/5 en verde: (a) la exclusión escrita en el CUERPO del predicado —la forma que el código tenía ANTES del refactor—, (b) una tercera clave en el objeto, porque el test construía el esperado con dos claves fijas y descartaba el resto, y (c) `[..., '!infra/**', 'infra/**']`, porque comparar el conjunto de negados tira el orden. En las tres el harness sub-reporta y `backend-deploy-fresh` da PASS sobre un backend stale | `paridad-entre-proyecciones` | **tres revisores adversariales**, sobre código YA DESPLEGADO (la revisión fue después del push, no antes) | El guard era el control, y el cierre del control tenía tres huecos más | el predicado se **deriva** de `railway.json`: no hay dos listas que puedan divergir. (b) y (c) dejan de ser escribibles —no hay objeto, y una lista que se re-incluye no compila porque la precedencia no está medida—; (a) la mata un test sobre el fuente de lo que se exporta (ni una comilla ni una barra), visto fallar con la mutación original y con la variante que envuelve el export desde afuera |
| 2026-08-07 | La tabla de supuestos de CLAUDE.md daba por **medido** que `!/*.md` ancla a la raíz, citando `aaed32e` y `cf6029b` ("tocaron solo `CLAUDE.md` de raíz → `No changes to watched files`"). Las dos observaciones son reales y son igual de compatibles con que el patrón sea **recursivo**: para separarlas hace falta un commit cuyo veredicto dependa de un `.md` ANIDADO, y en 100 deployments no hubo ninguno. El supuesto sigue sin medir | `evidencia-que-no-discrimina` | medición propia contra la API de Railway (el harness nuevo, buscando qué distinciones ejercita la historia) | No: la fila decía "que lo prueba" y nadie preguntó qué separaba de qué | la fila de CLAUDE.md dice ahora que no está medido y de qué lado cae el error (falsa alarma de STALE, nunca falso PASS); `backend-watchpatterns-real` reporta `anclaDeRaizEjercitada` y avisa cuando es 0 |
| 2026-08-07 | En ese mismo harness, la métrica de cobertura del ancla contaba las filas que **tenían** un `.md` anidado en el diff y reportaba `18` sobre 48. En las 18 había además archivos de backend, así que el veredicto no dependía del ancla en ninguna. El número decía "bien cubierto" exactamente sobre el único supuesto sin medir | `cobertura-que-cuenta-lo-que-no-verifica` | **mi propia lectura del output**, antes del commit: el 18 no cuadraba con la medición previa que decía 0 | Sí, en el sentido de que la medición anterior era el control, y contradecirla fue la señal | la métrica compara el veredicto contra el de una variante con el ancla quitada: decisiva, no presente. Ahora reporta 0, que es la verdad |
| 2026-08-07 | **Escribí en CLAUDE.md y acá que "Railway desplegó `8e338ff` ignorando `watchPatterns`, porque no resolvió `railway.json`".** Falso, y con la forma exacta de la clase que estaba documentando en la fila de arriba: el `meta` de ese deployment viene sin `configFile` y con `watchPatterns: []`, y armé una explicación entera sobre un campo **que falta porque el deployment nunca llegó a la etapa que lo escribe**. Railway lo dice sin ambigüedad al pedirle los logs: *"Deployment does not have an associated build"*. Nunca construyó — se quedó en `WAITING` durante el outage de Actions (el gate haciendo su trabajo) y quedó `REMOVED` cuando `096593a` lo reemplazó. Lo que lo hizo pasar por "construyó" fue mi propio conjunto `CONSTRUYO`, que tomaba `REMOVED` por build hecho. El "control exacto" que cité (`cde2525`, mismos dos archivos, resultado opuesto) era real y no probaba nada: la variable que yo creía distinta no era la que cambiaba | `causa-inventada-junto-al-numero` | **revisión adversarial**, indirectamente: encontró que la máquina de estados avanzaba la base del diff sobre deployments que nunca construyeron, y verificar eso tiró abajo la conclusión | No. Duró una hora en los docs, escrita como medida | `imageDigest` (confirmado contra `buildLogs`) como única señal de que un deployment construyó, en el harness y en CLAUDE.md. Y la afirmación se retrae entera: **no hay ninguna evidencia de que Railway haya desplegado ignorando `watchPatterns`** — 86/86 juzgables coinciden con el modelo |
| 2026-08-07 | El scan de fuente que mata la mutación (a) hubo que ampliarlo **dos veces en la misma sesión, por la misma evasión**. Lo verifiqué contra el predicado, después extraje `evaluarReglas` para otra cosa y no lo volví a correr: la primera revisión metió la exclusión ahí, 10/10 verde. Amplié a las dos funciones; la segunda revisión la metió en el closure que devuelve `crearPredicado()` —el eslabón del medio, y el sitio más natural para escribirla— con **886/886 verde** | `guard-partido-por-una-extraccion` | **dos revisiones adversariales seguidas**, cada una un nivel más abajo | El guard era el control y cubría una fracción decreciente de la cadena a medida que yo la partía | los tres eslabones en un solo loop, con la regla escrita en el docstring: si agregás un eslabón, va en esta lista. Verificado inyectando la mutación en cada uno de los tres |
| 2026-08-07 | Escribí la derivación perezosa de `backend-deploy-fresh` **para** que un `railway.json` roto no saliera como exit 1 con el `on_fail` equivocado, y en el mismo cambio puse `leerPatrones()` a nivel de módulo en el harness nuevo de al lado. Ahí el efecto era peor: ese harness **no necesita** `railway.json` para responder su pregunta (juzga con los patrones que trae la API), y el campo que sí lo usa es cosmético. O sea que una config rota tumbaba un guard que no depende de ella, con "el modelo no coincide con Railway" de diagnóstico | `barrido-de-un-solo-arbol` | **segunda revisión adversarial** | No: el argumento estaba escrito en un comentario del archivo de al lado, en el mismo diff | `try/catch` a `null` + aviso en el output; con `railway.json` mutilado el harness sigue dando PASS y dice que le falta la cobertura por patrón |
| 2026-08-07 | La reimplementación del test se pasó a expresiones regulares para que fuera de verdad independiente, y las regex ingenuas (`^dir/.+$`, `^[^/]+\.ext$`) **no son equivalentes** a `startsWith`/`endsWith` en 6 entradas: el directorio pelado (`webapp/`), un archivo llamado exactamente `.md`, y cualquier nombre con `\n` (porque `$` matchea antes de un salto final). Las 6 estaban verdes porque ningún probe las tocaba. En `.md` —el único nombre que git puede producir— la divergencia caía del lado peligroso: el harness lo excluía | `equivalencia-verde-por-casualidad` | **segunda revisión adversarial**, comparando las dos implementaciones sobre entradas adversariales en vez de sobre las verosímiles | No | regex sin `.` ni `$` suelto y con clases que admiten el caso vacío, + un test de entradas adversariales (20 casos) que corre la comparación sobre ellas. Encontró de paso una divergencia más, el string vacío, que era del contrato del predicado y no de los globs. Probado por mutación: volver a `.+$` lo pone rojo |
| 2026-08-08 | `clasificarRojo()` buscaba el job de tests con `jobs.find(j => j.name === jobTests)`, o sea que asumía sin comprobarlo que ese nombre identifica **a lo sumo un** job. Con una matriz que produzca varias patas llamadas igual gana la primera: verde la primera y roja la segunda devolvía `culpaDeLosTests: false` **con `"test: failure"` listado en `jobsRojos` del mismo objeto**, o sea un veredicto que se contradice a sí mismo en la línea de al lado y manda a no tocar una suite roja. Aparte, la EXACTITUD del match no estaba fijada por nada: mutar `===` a `startsWith` dejaba los 10 tests del archivo en verde, y ahí `test-e2e` pasa por `test` | `camino-feliz-unico` + `cobertura-alrededor-del-cambio` | **revisores adversariales**, sobre código ya desplegado | No: los 10 tests que había cubrían la clasificación (de quién es la culpa) y ninguno la selección (cuál es el job) | `filter` en vez de `find`, y TODAS las patas tienen que estar verdes; 5 tests nuevos sobre matrices y nombres que empiezan igual, los tres vistos fallar con las mutaciones exactas (`filter`→`find`, `===`→`startsWith`) |
| 2026-08-08 | **La rama VERDE de `backend-deploy-tested` no miraba ni un job.** Con `conclusion: success` devolvía PASS sin llamar a la clasificación, y la conclusion de un run es un AGREGADO: un job `skipped` la deja verde. `nlp-agent` lo demuestra en cada run desde el 14-jul. O sea que un `if:` nuevo sobre el job `test` —un filtro por paths, un toggle de standby como el que ya tiene `nlp-agent`— dejaba pasar un deploy con la suite del backend **sin correr**, en el harness escrito para atrapar exactamente ese fail-open. El commit `b6e44e8` afirma que un job `test` renombrado nunca se da por sano; eso valía solo en la rama roja, que era la que yo había arreglado | `barrido-de-un-solo-arbol` | **revisores adversariales**, sobre código ya desplegado | No, y la afirmación del commit decía lo contrario | los jobs se consultan en las dos ramas. Reproducido y verificado con el mismo comando: `NETO_CI_JOB_TESTS=job-que-no-existe` daba PASS y ahora da `GUARD CIEGO` exit 1. `skipped` deja de contar como `success` para ESE job (sigue contando para `jobsRojos`, donde el skip es deliberado), con dos veredictos nuevos y el detalle de por qué el run puede estar verde igual |
| 2026-08-08 | **El arreglo del agujero de arriba no tenía un solo control automático.** El revisor revirtió `main()` a la versión anterior —el `return done(0,'PASS')` con el run verde, antes de leer los jobs— y los 17 tests del archivo y los **894 de la suite** siguieron en verde. Los 7 tests que escribí cubrían `clasificarJobs()`, la función pura, y la decisión vivía en una cascada de `if` sin cobertura. Y esa cascada terminaba en `return done(0,'PASS')` por **caída libre**: un `motivo` nuevo que alguien agregara después salía PASS, que es el fail-open que el archivo cita tres veces para no cometerlo | `cobertura-alrededor-del-cambio` | **revisión adversarial**, probándolo por mutación en vez de leyéndolo | No, y es la tercera vez esta semana que la cobertura rodea el cambio sin tocarlo | `decidir()` puro con las 10 combinaciones de (color × motivo) fijadas y default exit 2, **más** dos tests estructurales sobre el fuente de `main()`: que el único PASS aparezca DESPUÉS de consultar los jobs, y que ningún `done()` posterior elija su exit code a mano. Los dos vistos fallar con la mutación exacta del revisor |
| 2026-08-08 | `severidad()` —el triage que dice si el deploy sin gate tuvo consecuencia— define "último commit con CI verde" con `runs?status=success`, o sea **el mismo oráculo agregado que este diff acaba de declarar no confiable**. Si el job `test` se filtra por paths, los commits salen con run verde y la suite sin correr; el harness detecta bien "LA SUITE NO CORRIÓ" y a continuación imprime *"el runtime que corre es el mismo que sí se testeó"*, que es falso, y el `on_fail` del canary traduce eso a "alcanza con anotarlo". El detector y su triage no pueden usar oráculos distintos | `consumidor-no-actualizado` | **revisión adversarial** | No: cambié el oráculo del veredicto y dejé el del triage donde estaba, en la misma función que estaba editando | `severidad()` baja al job de cada candidato antes de aceptarlo como "verde". Verificado en vivo: con `NETO_CI_JOB_TESTS=nlp-agent` (skipped real) ya no nombra ningún ancestro y lo dice |
| 2026-08-08 | `jobsDelRun` pedía `per_page=50` sin paginar ni mirar `total_count`, **en el cambio cuya justificación entera es la matriz de jobs**. Con más de 50 patas trunca en silencio y falla en las dos direcciones: si la pata roja queda fuera de la página sale `ok` y **PASS con la suite roja**; si el job `test` entero queda fuera, GUARD CIEGO falso. Latente hoy (5 jobs), pero es exactamente el escenario que motivó pasar de `find` a `filter` | `camino-feliz-unico` | **revisión adversarial** | No | `per_page=100` y `total_count` contra lo que llegó; truncada se trata como ilegible, que es el lado seguro. El error real de `gh` se propaga: el exit 2 llegaba al canary sin una línea con la que diagnosticar |
| 2026-08-08 | El guard que escribí para cerrar el hueco de arriba **dependía del estilo de comillas del próximo autor**. Leía el fuente de `main()` buscando `'PASS'` antes de la consulta de jobs; el revisor lo evadió con `done(0, "PASS", …)` —comillas dobles— y reintrodujo el fail-open entero con 31/31 en verde. Con backticks igual, y con el código por una variable también. Y `main()` seguía cayendo libre a PASS con una `clase` no contemplada: el mismo defecto que yo acababa de cerrar en `decidir()`, movido un nivel más arriba | `mis-guards-tambien-fallan` | **segunda revisión adversarial**, probando tres evasiones distintas | El guard era el control, y era un control sobre texto | el ensamblado se extrajo a `veredicto()`, **puro**, con `switch` exhaustivo y default exit 2: los tests llaman la función y miran lo que devuelve, sin inspeccionar texto. Incluye un barrido de 54 combinaciones que exige que exit 0 salga de UNA sola. Lo único que queda estructural es un conteo fijado de `done()` en `main()`, que no depende de comillas; las tres evasiones lo rompen |
| 2026-08-08 | Tres de los seis arreglos de esta sesión **no tenían un solo control**, y lo midió el revisor por mutación: revertir el chequeo de jobs de `severidad()`, la guarda de truncado, y la etiqueta del job sin conclusion pasaban con 908/908 en verde. Las funciones no estaban exportadas, así que "no se podía testear" era la excusa disponible | `cobertura-alrededor-del-cambio` | **segunda revisión adversarial** | No | `interpretarRespuestaDeJobs()` extraída y pura; `severidad()` exportada con `ghFn`/`leerJobs` inyectables. 7 tests nuevos, los tres vistos fallar contra la mutación que los revierte |
| 2026-08-08 | Frase partida a la mitad en la sección del gate de CLAUDE.md: *"Compara el fin del deployment de Railway contra el run de ci.yml del mismo sha: con el gate / Compara el INICIO del build contra el fin del run"*. Quedó de la edición de ayer que corregía justamente ese error de concepto (`cde2525`), y dejaba la afirmación VIEJA —la que detectaba el 16% de los deploys sin gate— pegada arriba de la nueva | `consumidor-no-actualizado` | lectura al pasar, editando la sección de al lado | No: ninguna suite lee prosa | corregida. No hay control posible acá salvo leer el diff de los docs con el mismo cuidado que el del código |
| 2026-08-08 | El corpus del test de paridad salía de `git ls-files` a secas, que **entrecomilla las rutas no-ASCII con escapes octales**: `docs/año.md` llega como `"docs/a\303\261o.md"`, empezando con una comilla en vez de con `docs/`. Las dos implementaciones coinciden sobre esa cadena —ninguna la reconoce como el directorio excluido— así que el acuerdo es **vacuo** y esa ruta nunca ejercita la regla. Medido el 08-ago agregando el archivo de verdad: sin `-z` el predicado contesta que redespliega, con `-z` contesta que no, y la correcta es la segunda. Latente porque hoy el árbol no tiene ninguna | `guard-limitado-al-corpus-existente` | **revisores adversariales**, y verificado agregando la ruta al índice de git | No | `git ls-files -z` y split por NUL, más un tripwire que falla si alguna entrada del corpus empieza con comilla. Probado con el archivo real en el índice: sin `-z` caen 2 tests |
| 2026-08-08 | Le agregué a `backend-deploy-fresh` un campo `truncado: ((.files\|length) >= 300)` para avisar que la lista de la API de compare podía venir cortada, **y no escribí el lector**. `grep truncad` sobre el repo lo confirma: cero consumidores. O sea que el harness calculaba la evidencia de que su propia entrada podía estar incompleta y contestaba igual, lo cual es peor que no calcularla: deja el rastro de que el problema estaba visto. Medido hoy contra la API en vivo: sobre `HEAD~400...main` la API devuelve **300 de 667** archivos reales, o sea **193 de los 302** que Railway observa, con **109 observados invisibles** | `consumidor-no-actualizado` | Favio, apuntándolo como el ítem 1 de la cola | No, y no había nada que romper: el campo no lo leía nadie | `compararRango()` en `qa-e2e/lib/github-compare.mjs`: la lista completa sale del media type `application/vnd.github.diff`, que **no está topado** (medido: 667 y 744 bloques `diff --git` en dos rangos reales, con `solo-en-git` vacío. La lista de RUTAS es un superconjunto a propósito —668 y 749— porque un rename aporta sus dos puntas). Y el veredicto ramifica por `completa`: con la lista marcada incompleta y cero observados es **exit 2, nunca PASS** |
| 2026-08-08 | **Mi propio arreglo reintrodujo el fail-open que venía a cerrar, y la suite entera lo daba por bueno.** `compararRango` hacía `Number(cmp.nFiles)` y con el campo ausente eso es `NaN` — y las **dos** comparaciones numéricas eligen la rama insegura ante `NaN`: `NaN < 300` es false (baja al diff crudo como si estuviera topado) y después `0 < NaN` también es false (la guarda de coherencia no salta). Resultado: `{ files: [], completa: true }`, la lista **vacía declarada completa**. Reproducido con el doble que ya usaban los tests de `severidad`, que devuelve un compare sin `nFiles`: el `handlers/webhook.js` que el doble sí traía desaparecía, con la suite entera en verde | `mis-guards-tambien-fallan` | **segunda vuelta de revisión sobre mi propio arreglo**, corriendo el doble existente contra el módulo nuevo | No: mis 21 tests pasaban `nFiles` siempre, porque yo sabía que el `jq` lo produce | `Number.isFinite(n)` valida el conteo ANTES de compararlo, y un conteo no usable es `completa: false`. Más una guarda de `entradas === 0` (un diff crudo sin ni un `diff --git` no es un rango vacío: es una respuesta que no era un diff). Los dos vistos fallar por mutación |
| 2026-08-08 | El fixture del parser de diffs tenía el rename **con** líneas `--- a/` y `+++ b/`, así que `+++ b/` tapaba el agujero y `rename to` no estaba ejercitado por nada: quitarlo dejaba los 21 tests en verde. Y `rename to` es imprescindible, porque un **rename puro** (`similarity index 100%`) no trae `---`/`+++` —verificado con un repo de prueba— y su cabecera tiene rutas de largo distinto, así que el fallback por longitud tampoco la resuelve: el bloque desaparecía entero. Un rename puede mover un archivo de una ruta excluida a una observada, o sea justo el cambio que decide el veredicto | `guard-limitado-al-corpus-existente` | **prueba por mutación**, que fue la única de las 9 que sobrevivió | No | el bloque del rename puro agregado al fixture, y la mutación vuelta a correr: ahora muere. 9/9 |
| 2026-08-08 | Escribí que el PASS falso no era alcanzable porque los prefijos excluidos que ordenan temprano suman un techo de **137** archivos, contando `.claude/` (13) del lado **excluido**. `.claude/**` **no está en la lista de exclusiones de `railway.json`**: está OBSERVADO, y ordena PRIMERO en orden de bytes. `.claude/` es precisamente lo que empuja el primer observado al índice 0 en la mayoría de los rangos (102 de 120 medidos después). La conclusión aguantó —y de hecho es más fuerte— pero el número tenía una composición inventada al lado. Lo desmintió el propio harness, listando `.claude/commands/deploy.md` dentro de `pendingBackend` | `causa-inventada-junto-al-numero` | **el output del harness al correrlo de verdad**, contradiciendo el docstring que yo acababa de escribir dos archivos más arriba | No: ninguna suite lee prosa | corregido, y después corregido otra vez: el techo de 137 pasó a 124 y de ahí a una muestra de 40 bases enunciada como "0 siempre", que una segunda revisión midió falsa (16 de 120 bases consecutivas tienen el primer observado en la posición 2). Lo que quedó es la cota DEDUCTIVA que las tres versiones habían tirado: `webapp/**` (323 archivos) ordena ÚLTIMO y nada del árbol ordena después, así que los excluidos que pueden preceder a un observado son `docs/` + `qa-e2e/` + `*.md` de raíz, dos órdenes de magnitud por debajo de 300, para cualquier rango. **El conteo exacto no quedó escrito**: decía 125 cuando ya eran 126, o sea que nació vencido igual que los tres números anteriores — quedó la forma del argumento y el comando para recontarlo |
| 2026-08-08 | `es404` clasifica bien, y sobre esa clasificación yo escribí **un mensaje que solo vale en uno de cuatro casos**. Medido contra la API: workflow inexistente, repo inexistente, repo privado sin acceso y owner inexistente producen un stderr **byte-idéntico** (`gh: Not Found (HTTP 404)`). Con `NETO_REPO=github/github` el harness imprimía *"GUARD CIEGO: no existe el workflow ci.yml"* y mandaba a revisar `.github/workflows/ci.yml`, un archivo intacto | `evidencia-que-no-discrimina` | Favio, ítem 2 de la cola, con el comando exacto que lo muestra | No: el exit 1 era correcto en los cuatro, así que ningún test de exit code lo veía | `diagnosticar404()` desambigua con hasta dos sondeos (`repos/{repo}`, `users/{owner}`) y da tres clases. La cuarta **no se puede separar y se dice**: GitHub devuelve 404 tanto para "no existe" como para "privado sin acceso", a propósito, así que el mensaje nombra las dos en vez de elegir. Los cuatro verificados en vivo |
| 2026-08-08 | `FAIL_OPEN_SIN_RUN` afirmaba *"se desplegó y **NUNCA existió** un run de CI para ese commit"* — una afirmación sobre la historia sostenida por una sola observación del presente ("hoy no hay run"). Tres causas dejan el mismo rastro (cero runs, cero check suites) y solo una es un fail-open. Y encontré una que la cola no listaba, **medida y viva en este repo**: GitHub crea UN run por evento de push, con `head_sha` en la PUNTA, así que los commits intermedios de un push en lote no tienen run — `112734f` (mismo segundo que `89206ac`, que sí tiene el run) y `373f82b`. En cambio la causa "llegó por PR" que la cola sí listaba **no aplica**: `ci.yml` corre `on: push: branches:[main]`, así que toda punta de main tiene su run | `evidencia-que-no-discrimina` | Favio para la clase, y la medición para las causas concretas | No | **solo el texto**: reporta lo observado y nombra las tres causas sin afirmar ninguna. El veredicto sigue en exit 1. El intento de descartar la retención con un dato se revirtió — ver la fila de abajo |
| 2026-08-08 | **Y el arreglo de esa fila cometió, para arreglarla, exactamente la clase que venía a cerrar.** Agregué `horizonteRetencion()` para descartar la causa benigna "Actions borró el run": mide el run más viejo que todavía existe (`2026-03-21T23:03:16Z`, sobre 691 runs) y ablanda a exit 2 los builds anteriores. El número es real y la causa que le pegué al lado es inventada: el commit que **crea** `ci.yml` (`48155ca`) es de las `22:50:09Z` del **mismo día**, o sea 13 minutos antes. No expiró ni un run; ese "horizonte de retención" era la fecha de nacimiento del workflow. Y las dos causas piden lo contrario: un build anterior al primer run de CI es el fail-open más puro que hay, porque no había CI. El disparador es realista, no teórico — un workflow renombrado (lo que el hint del harness hermano te manda a hacer) devuelve un horizonte de hace días, y con eso el incidente del 06-ago salía **exit 2 afirmando una causa falsa**, el incidente que estos cuatro harness existen para atrapar | `causa-inventada-junto-al-numero` | **revisión adversarial**, cruzando el horizonte contra la fecha del commit que creó el workflow | No: mis 5 tests pasaban el horizonte como literal, así que ninguno ejercitaba el valor calculado ni podía notar qué significaba | **revertido entero.** La función no existe y el veredicto no se ablanda. Queda un test estructural (`NINGUNA entrada sin run puede producir ok null`) + otro que verifica que el módulo no exporte ningún horizonte, para que no se reconstruya por un parámetro nuevo. El porqué queda escrito donde vivía la función |
| 2026-08-08 | Cuatro huecos de cobertura que la revisión probó por mutación, los cuatro con la suite en verde: (1) `dispara = disparaBuildRailway` en `decidirFrescura` → mutado a `() => false` da PASS para cualquier backend stale; (2) `?? 10` de `IN_FLIGHT_MIN` → mutado a `?? 1000000` convierte todo STALE en "deploy en vuelo"; (3) la rama `behind`/`diverged` → mutada a `if (false && ...)` cae en `identical` y sale **PASS "ninguno (== HEAD)"** sobre un sha fuera de main; (4) `newest: cmp.newest` en la costura de `compararRango` → quitarlo mata la ventana de deploy en vuelo. Los cuatro son la misma raíz: mis 6 tests **inyectaban** `dispara` e `inFlightMin` para controlar el escenario, así que ninguno ejercitaba el cableado de producción, que es justo lo que el refactor de esta sesión movió a un default de parámetro | `cobertura-alrededor-del-cambio` | **revisión adversarial**, por mutación | No, y es la cuarta vez esta semana que la cobertura rodea el cambio sin tocarlo | un `describe` que llama `decidirFrescura` **sin inyectar nada** (predicado real de `railway.json` y ventana real de 10 min), un test de las dos ramas `behind`/`diverged`, y uno que afirma `status`/`ahead_by`/`newest` sobre la salida de `compararRango`. Las cuatro mutaciones vueltas a correr: mueren |
| 2026-08-08 | Dos formas de perder un archivo que el parser no manejaba, las dos por `core.quotePath`, y la segunda **viola la propiedad que el módulo afirma sostener**: (1) una modificación de ruta no-ASCII sale citada con octales (`--- "a/docs/gu\303\255a.md"`), no matchea ningún prefijo y cae en `sinParsear` — lado seguro, pero deja el harness en exit 2 permanente; (2) en un rename MIXTO ascii→no-ascii git cita **solo el lado que lo necesita**, así que `rename from plain.txt` matchea, `rutas` queda no-vacío, `sinParsear` sale 0, y **la lista se declara COMPLETA con la cadena cruda en lugar de la ruta**. Aparte, el camino JSON no cita, así que la misma ruta se escribía distinto según si el rango cruzaba los 300 | `guard-limitado-al-corpus-existente` | **revisión adversarial**, y verificado generando los diffs con git de verdad | No: el repo no tiene ninguna ruta no-ASCII versionada, así que el corpus no podía ejercitarlo | `desescaparRuta()` decodifica los octales como BYTES y después como UTF-8 (`fatal: true`), y devuelve `null` ante un escape desconocido en vez de adivinar. Una ruta ilegible invalida el bloque ENTERO, no solo esa ruta. 12 tests nuevos, incluido el rename mixto |
| 2026-08-08 | La guarda de coherencia del diff crudo (`archivos.length < n`) **solo cubre la franja 0-299**, porque en esa rama `n` vale siempre 300 (es el tope de la API). Y el modo de falla que tenía que atrapar existe: `gh api` entrega el cuerpo PARCIAL con **exit 0 y stderr vacío** cuando el diff pasa de ~110 MB, el corte cae a mitad de hunk, y el último bloque conserva sus `---`/`+++` y parsea limpio. Demostrado por la revisión componiendo `compararRango` + `decidirFrescura` reales sobre un corte medido: `completa: true`, `exit 0`, `PASS`, con `index.js`, `handlers/webhook.js`, `lib/trial.js` y 50 archivos de `services/` sin desplegar. Este repo está a ~18x de ese tamaño (la historia entera son 6,35 MB, o sea 1,25 órdenes), no a órdenes de magnitud como decía la primera versión de esta fila, pero la propiedad que el módulo afirma es incondicional | `camino-feliz-unico` | **revisión adversarial**, midiendo el corte real 5 veces contra la API en vivo | No | un diff completo termina en newline y uno cortado a mitad de hunk no: es la única señal del transporte y es barata. Si falta, `completa: false`. Más `typeof n === 'number'` en vez de `Number(n)`, porque `null`/`[]`/`''` coaccionan a **0** y `0 < 300` declaraba completa cualquier lista |
| 2026-08-08 | **El consumidor del arreglo entero no se actualizó.** Todo el trabajo del truncado existe para que una lista incompleta no produzca una afirmación de calma, y el `on_fail` de `webapp/.claude/deploy-config.json` —lo que el canary de las 10am lee de verdad— seguía diciendo *"si `archivosDeBackendSinTestear` viene vacío, lo que corre es el mismo runtime que sí se testeó y alcanza con anotarlo"*. Con la lista incompleta ese array **sigue viniendo vacío**: el cambio movió el aviso a `lectura`/`listaCompleta`/`avisoLista`, campos que el `on_fail` no menciona. O sea que el canary seguía diciendo "alcanza con anotarlo" exactamente en el caso que el arreglo vino a cubrir. Y el `on_fail` de `backend-deploy-fresh` enumera las causas de exit 2 sin la nueva, así que la clasificaba como infra | `consumidor-no-actualizado` | **revisión adversarial de segunda vuelta**, siguiendo qué archivo lee el canary en vez de qué imprime el harness | No, y el archivo no estaba ni en el diff | los dos `on_fail` reescritos: el de `tested` obliga a leer DOS campos (`archivosDeBackendSinTestear` vacío alcanza solo si `listaCompleta` es true), el de `fresh` nombra la causa nueva de exit 2. Más los dos textos que afirmaban "nunca hubo run" |
| 2026-08-08 | Un comentario que **afirmaba lo contrario de lo que hace git**, y sostenía el eslabón que evita perder un archivo: *"las rutas citadas nunca llevan un TAB al final"*. Git agrega el TAB cuando la ruta tiene un **ESPACIO, cite o no cite** — o sea toda ruta no-ASCII con espacio, el caso normal en un repo en español (`+++ "b/Alg\303\272n d\303\255a.md"<TAB>`, medido contra la API en vivo). El código funcionaba de casualidad por el orden de los `if`; la mutación de la revisión (leer la línea sin limpiar en las ramas citadas) dejaba 124 tests en verde mientras sobre un diff real devolvía la cadena cruda con comillas, octales y TAB **como si fuera una ruta**, con `completa: true` | `guard-limitado-al-corpus-existente` | **revisión adversarial de segunda vuelta**, contra un repo público con rutas en español | No: mis fixtures citados no tenían espacio, así que `limpiar` y las ramas citadas nunca se ejercitaban juntas | fixture con ruta citada CON espacio y TAB; el tripwire ("ninguna ruta devuelta tiene forma de cadena cruda") pasó de un solo `it` a barrer TODOS los fixtures citados. La mutación exacta de la revisión ahora mata 2 tests. La condición de `limpiar` se simplificó, y verifiqué que las dos versiones son EQUIVALENTES sobre las 9 formas que git emite: el defecto era el comentario, no el código |
| 2026-08-08 | **De las nueve escapes de `SIMPLES` solo tres estaban fijadas** (`t`, `\\`, `"`); las otras seis eran libres. Y un valor equivocado ahí no da `null`: da una ruta **silenciosamente mal decodificada** dentro de una lista `completa: true`, el peor modo de falla del módulo. Que git escapa `\n` en un nombre está medido, no es teórico | `cobertura-alrededor-del-cambio` | **revisión adversarial de segunda vuelta**, por mutación (borrar `n: 10` pasaba en verde) | No | las nueve, una por una, con su byte exacto. Las nueve mutaciones vistas morir |
| 2026-08-08 | La guarda del newline **tapaba** a la de `entradas === 0` justo en el escenario para el que se escribió: `gh api` **sin `--jq`** no termina en newline (medido), y la bajada al diff crudo es sin `--jq`, así que un media type ignorado —que devuelve JSON— se diagnosticaba como "cuerpo cortado a mitad de hunk" en vez de "no era un diff". Los dos dan `completa: false`, o sea que no había fail-open, pero el mensaje mandaba a mirar el transporte cuando el problema era la petición. Y el test de `entradas === 0` lo escondía agregando el `\n` a mano, o sea con un fixture que la producción no puede producir | `asercion-vacua-por-implicacion` | **revisión adversarial de segunda vuelta** | No: el fixture irreal era mío | los tres controles reordenados del diagnóstico más específico al más genérico, con el porqué escrito, y un test que fija el orden con un JSON sin newline |
| 2026-08-08 | Dos justificaciones inventadas al lado de decisiones correctas. (1) El comentario de `maxBuffer` decía que el default de 1 MB *"cortaría el diff y devolvería una lista corta SIN saberlo"*: no, desbordar `maxBuffer` **TIRA** `ENOBUFS` (`status: null`, `SIGTERM`), lo atrapa el `try/catch` y sale `completa: false` — falla ruidoso y del lado seguro. (2) La guarda del newline se justificaba con ese mismo mecanismo inexistente; el real es otro (`gh api` entregando cuerpo parcial con exit 0 sobre ~110 MB) y lo midió la revisión, no yo. Las dos decisiones se quedan; las dos razones estaban mal | `causa-inventada-junto-al-numero` | **revisión adversarial de segunda vuelta**, midiendo el desborde en vez de razonarlo | No: ninguna suite lee comentarios | separadas y atribuidas: el desborde tira (y por eso subir el buffer evita un exit 2 espurio, no una lista corta), y el corte con exit 0 queda anotado como medido por la revisión y no por mí |
| 2026-08-08 | **La misma corrección aplicada en dos archivos, con test en uno solo.** El texto "NUNCA existió un run de CI" se arregló en `evaluarGate` (`backend-deploy-gated`) con sus tests, y estaba repetido en `backend-deploy-tested` —el `runs.length === 0` de `main()`—, donde lo cambié sin un solo control: revertir el título dejaba los 128 tests en verde. Es la cuarta vez en la sesión que la cobertura rodea el cambio | `barrido-de-un-solo-arbol` + `cobertura-alrededor-del-cambio` | **prueba por mutación** propia, después de la segunda revisión | No | extraído a `veredictoSinRuns()`, puro y exportado, con 4 tests; el hint tiene que nombrar el MECANISMO del commit intermedio ("un run por push", "la PUNTA"), no solo la palabra. Las dos mutaciones vistas morir |
| 2026-08-08 | Para arreglar el "techo" mal compuesto lo reemplacé por *"medido sobre 40 bases: **0 en todas**, el primer observado cae en el índice 0 **siempre**"*. Falso: 16 de 120 bases consecutivas lo tienen en la posición 2. El muestreo era cada 20 commits, o sea rangos largos que **siempre** tocan `.claude/` — un barrido que **solo podía confirmar su hipótesis**. Y lo peor: cambié una DEMOSTRACIÓN por una muestra. La cota deductiva sí cierra el caso y la había tirado: `webapp/**` (323 archivos) ordena último y nada del árbol ordena después, así que los excluidos que pueden preceder a un observado son dos órdenes de magnitud menos que 300, para cualquier rango | `evidencia-que-no-discrimina` | **revisión adversarial de segunda vuelta**, rehaciendo el barrido con stride 1 | No: ninguna suite lee prosa, y el barrido era mío | la cota deductiva restaurada, la distribución real (102/1/16 sobre 120) al lado, y la nota de que hay que RE-DERIVARLA si cambia `railway.json` o aparece un directorio que ordene después de `webapp/`. Tres versiones de este párrafo estuvieron mal; la cuarta al menos es una demostración |
| 2026-08-08 | Tres afirmaciones más que no se sostenían, todas en docs. (1) Dos filas de este mismo ledger describían arreglos que ya no existían: una conservaba textual el "667/667, idéntico a `git`" que la primera revisión había desmentido, y otra describía una "composición explícita (1 + 35 + 88)" borrada en el mismo commit. (2) La cita del trigger de `ci.yml` omitía la mitad (`pull_request` existe y lleva 29 runs) y apoyaba la conclusión en "100 de los 100 runs recientes son push", que es la distribución de los RUNS cuando la afirmación es sobre las PUNTAS de main. (3) "4-5 órdenes de magnitud" eran 1,26 (110 MB / 6 MB = 18x) | `causa-inventada-junto-al-numero` + `consumidor-no-actualizado` | **revisión adversarial de segunda vuelta**, verificando cada número con un comando | No | las tres corregidas, con la magnitud real y el trigger completo. El ledger de defectos quedando con el número desautorizado es la lección: **corregir el código y no la fila que lo registra deja la afirmación falsa como la versión oficial** |
| 2026-08-08 | El script ad-hoc con el que verifiqué **qué archivos observa Railway antes de pushear** —la medición sobre la que se apoyaba el experimento entero— hacía `.trim()` sobre TODA la salida de `git status --porcelain` y después `slice(3)` por línea, así que a la primera le comía el punto inicial: evaluó el predicado sobre `claude/commands/deploy.md`, no `.claude/commands/deploy.md`. Las dos dan `true`, o sea que **el veredicto era correcto por casualidad**: esa salida no distinguía "el script parsea bien" de "el script está roto", que es exactamente el defecto que el experimento venía a arreglar en la doc, cometido sobre el experimento mismo | `evidencia-que-no-discrimina` | yo, en el acto, **porque el script imprimía la ruta parseada al lado del veredicto** | No: one-liner de sesión, ninguna suite lo mira | Ninguno mecánico, y no lo amerita. La práctica que lo atrapó es la fila: imprimir el INPUT parseado junto al veredicto, nunca el veredicto solo. Rehecho con `--porcelain -z` + `split('\0')`, y con un control negativo (`CLAUDE.md → false`) al lado |
| 2026-08-09 | Escribí **"El ancla existe. El modelo era correcto"** en `CLAUDE.md` habiendo medido UNA de las dos reglas del modelo. La otra (`dir/**` anclado a la raíz) seguía siendo mi lectura de la sintaxis, y el deploy de control del día siguiente (`6de1392`) la desmintió: Railway saltea `.claude/docs/…`, o sea que `!docs/**` matchea en el medio de la ruta. Lo peor no es haberme equivocado en la regla —para eso se mide— sino que **el harness me estaba diciendo en esa misma corrida que la otra distinción seguía sin ejercitarse**, y aun así generalicé de "esta regla es correcta" a "el modelo es correcto" | `conclusion-mas-ancha-que-la-medicion` | el deploy de control del día siguiente, a las ~24h | No: ninguna suite lee prosa | la frase acotada a lo medido, con la regla al lado: una medición sostiene exactamente lo que ejercita, y mientras el campo `ejercitado` liste algo pendiente, "el modelo es correcto" no es una conclusión disponible. El arreglo real es el test de veredictos nuevo, que sí falla si alguien revierte la regla |
| 2026-08-09 | **El consumidor otra vez, y el mismo archivo que el 08-ago.** Actualicé `CLAUDE.md`, los comentarios de los dos módulos y la memoria con el resultado del ancla, pero no `webapp/.claude/deploy-config.json` — lo que el canary de las 10am lee de verdad. Su `on_fail` siguió diciendo *"quitarle el ancla de raiz a `!/*.md` o cambiar startsWith por includes pasan en verde"* y *"anclaDeRaizEjercitada, que hoy vale 0"*: las dos ya falsas, y la segunda mandando a leer un 0 como si fuera el estado del conocimiento | `consumidor-no-actualizado` | yo, al empezar la tarea siguiente preguntándome qué había quedado pendiente. **No** la verificación del cambio, que dio verde entera | No, y el archivo no entra en el diff: vive bajo `webapp/`, que Railway excluye y yo no estaba tocando | `on_fail` reescrito con los dos experimentos, qué sigue sin ejercitar, y por qué `anclaDeRaizEjercitada` vuelve a 0 sola. **Sigue sin control mecánico**: nada liga ese texto con el estado real del harness, y es la segunda vez que este archivo se queda atrás. Si hay una tercera, el control es obligatorio |
| 2026-08-09 | **El harness que vigilaba la trampa cayó en la trampa.** El stub de `obtenerDeudasProximasVencer` en `qa-e2e/qa-cron-deudas.mjs` era una copia a mano del `select` real. La query real sumó `usuarios.plan` (con un comentario explicando que sin esa columna el cron no puede saltear a quien está en el muro) y la copia no se movió. Con la fila parcial, `estaEnMuro()` lee `plan === undefined` y devuelve `true`, así que el cron salteaba SIEMPRE y las 9 aserciones de TRIGGER fallaban. Es literalmente la regla *"una fila parcial no puede decidir"* del `CLAUDE.md`, cometida por el harness escrito para vigilarla. Y el costo real no fue la cobertura perdida: **su exit 1 es indistinguible de "el cron de recordatorios de deuda está roto en producción"** hasta que abrís las dos queries y las comparás | `consumidor-no-actualizado` | esta tarea, al correr por primera vez un harness que no estaba cableado a nada. Ninguna suite lo miraba | No, y no podía haberlo: el harness no lo corría nadie. Ese es el hallazgo | El stub ya no reescribe la query: llama a la real y **recorta** el resultado al throwaway, así que el `select` no se puede volver a desincronizar. Probado por mutación (sacarle `plan` a `services/debts.js` deja el harness en 7/16, exit 1). Y el control de fondo es que el harness pasó a `canary.harnesses`: lo que no corre, se pudre |
| 2026-08-09 | Mismo archivo, segunda deriva: la aserción de copy exacta tenía escrito `'⏰ QA, mañana vence…'` de cuando el usuario QA se llamaba "QA". Hoy se llama Camila Rojas, y el cron arma el saludo con `nombre.split(' ')[0]`, así que la aserción fallaba **sin que el copy hubiera cambiado**. Un valor de fixture congelado dentro de una aserción sobre una REGLA | `causa-inventada-junto-al-numero` | la misma corrida | No | el saludo se deriva del `nombre` real del usuario QA. Sigue siendo copy exacta —la coma, el emoji, el resto del texto— pero afirma la regla en vez del valor. Renombrar al usuario QA ya no rompe nada; cambiar la regla del saludo, sí |
| 2026-08-09 | La primera versión del tercer check del guard de inventario marcaba como "harness escondido" todo `.mjs` de `qa-e2e/lib` o `fixtures` cuyo nombre empezara con `qa-`/`probe-`/`backend-`/`diag-`. Se disparó sola en la primera corrida con **`lib/qa-guard.mjs`, que es la barrera misma**. Medí el NOMBRE en vez de lo que el archivo hace, y de paso quedaba evadible por quien eligiera otro prefijo | `mis-guards-tambien-fallan` | yo, en la primera corrida, **antes de commitear**: escribí el test y lo corrí en vez de darlo por bueno | No | conjunto FIJADO del contenido de `lib/` y `fixtures/` en vez de un patrón de nombre. No se evade eligiendo nombres, y agregar una librería legítima rompe el build una vez y obliga a justificarla. Mismo patrón que los conteos fijados de `gmail-oauth-gates` y `notificaciones-duales`. Probado por mutación |
| 2026-08-09 | **Repetición de la lección del 08-ago, un día después.** El `until`-loop con el que esperaba el deploy de Railway leía `.commit` del JSON de `/version`, y ese campo se llama **`sha`**. La condición nunca se cumplió: gastó los 10 minutos completos de timeout y la salida fue indistinguible de "Railway no desplegó". El backend estaba desplegado hacía rato. Un one-liner que imprime SOLO el veredicto no separa "todavía no pasó" de "estoy leyendo mal el campo" | `evidencia-que-no-discrimina` | yo, al consultar `/version` a mano tras el timeout | No: one-liner de sesión | Ninguno mecánico, y la práctica ya estaba escrita en la fila del 08-ago —**imprimir el INPUT parseado junto al veredicto**— así que el problema no es que falte la regla sino que no la apliqué a un comando de espera, que es donde el silencio se disfraza de paciencia. Si vuelve a pasar, el control es hacer que todo loop de espera imprima el valor leído en cada vuelta |
| 2026-08-09 | El `on_fail` que escribí para `cron-deudas` mandaba a triar por *"'CLEANUP WARN' en la salida = quedaron filas en PRODUCCIÓN"*. Esa señal **no existía para nadie**: el `catch` de la limpieza imprimía el warning y el proceso salía **0** igual, así que el canary daba PASS, no escribía reporte —la regla es "silencio = sano"— y nadie iba a leer una línea de stdout de una corrida que pasó. Escribí instrucciones de triage para un caso que el harness no podía denunciar | `asercion-vacua-por-implicacion` | esta tarea, al agregarle exit 2 y tener que enumerar todos los caminos de salida uno por uno | No: nada liga el texto de un `on_fail` con los exit codes que el harness produce de verdad | la limpieza fallida es **exit 1** con `'==> LIMPIEZA FALLIDA'`, y el `on_fail` lo dice. La lección general: un `on_fail` que nombra una señal tiene que nombrar CÓMO llega esa señal al canary, y el canary solo ve el exit code |
| 2026-08-09 | Los caminos de aborto nuevos (exit 2) usaban `process.exit(2)` y en Windows devolvían **127**: salir con sockets keep-alive de `fetch` abiertos rompe el código de salida. El exit 2 llegaba al canary como fallo desconocido, o sea que el arreglo que venía a distinguir infra de regresión no distinguía nada. El gotcha ya estaba documentado en `qa-score-parity.mjs` y `qa-gating-score.mjs`; no se notaba antes porque el único camino de salida corría después de completar todos los fetch | `consumidor-no-actualizado` | yo, en la primera mutación, **porque verifiqué el CÓDIGO DE SALIDA y no solo el mensaje impreso**. El texto '==> INCONCLUSO (exit 2)' salía perfecto mientras el proceso devolvía 127 | No | `process.exitCode` en los tres caminos de los dos harness. Y la lección que se repite: el mensaje que un harness IMPRIME y lo que un harness DEVUELVE son dos afirmaciones distintas; verificar la primera y dar por buena la segunda es el mismo error que dar por bueno un guard porque su salida se lee bien |
| 2026-08-09 | **Segunda vez el mismo día, y por eso importa más que las dos por separado.** El pre-vuelo de `qa-trial-gate.mjs` medía a cuánta gente real alcanzaba `checkTrialExpiry` —un cron BULK contra la Supabase de producción— y lo pasaba por `check(..., true, ...)`, o sea que **pasaba siempre**. La corrida seguía igual y el cron le pasaba por encima a quien estuviera en la ventana. El comentario al lado decía "el número tiene que quedar impreso en la corrida y no adivinarse después", que es honesto sobre lo que hacía y no sobre lo que hacía falta: lo que protegía a esa gente no era el número, eran el spy y la barrera, y de esos dos el segundo nunca se midió. Junto con la fila del `CLEANUP WARN` de hoy, son dos controles míos que MEDÍAN o REPORTABAN sin ACCIONAR | `asercion-vacua-por-implicacion` | esta tarea, al ir a convertirlo en gate | No | el pre-vuelo aborta con exit 2 y **no corre el cron** si hay ≥1 usuario real en ventana. Probado por mutación con un diseño que no podía dañar a nadie: se ensanchó la ventana que mide el GATE (a 15 días, 8 usuarios reales) sin tocar la del cron, así que un gate roto habría corrido el cron con su ventana normal, vacía ese día. Salió exit 2 sin ejecutar el cron. **La lección de la repetición**: cuando escribo "acá hay que medir X antes de hacer Y", el reflejo tiene que ser preguntar qué pasa si X sale mal, no imprimir X |
| 2026-08-09 | En el mismo README escribí que arreglar ese pre-vuelo era *"el cambio que lo habilita"* para el canary diario. Lo medí al hacerlo y es **falso**: con los trials activos de hoy la ventana estaría ocupada **14 de los próximos 30 días** (cota inferior, no cuenta los que empiecen mañana), así que gatearlo lo vuelve seguro pero lo deja saliendo exit 2 la mitad de las mañanas. Escribí la condición de habilitación sin proyectarla una sola vez contra los datos que ya tenía a mano | `causa-inventada-junto-al-numero` | esta tarea, midiendo antes de recomendar en vez de después | No | la sección dice qué se midió, deja el comando para recontarlo cuando cambie la base de usuarios, y nombra lo que SÍ lo habilitaría (que `checkTrialExpiry` acepte un scope de usuario, como ya lo permite `obtenerDeudasProximasVencer`) |
| 2026-08-09 | **Un fixture de QA que se auto-destruye, y nueve meses de harness que no podían avisarlo.** El usuario "QA Free" existe para afirmar que a un Free le CIERRAN las features Pro, pero el trial de 14 días arranca con el PRIMER GASTO: cualquier harness que le registre uno lo deja en `plan='premium'`, y entonces abrirle todo pasa a ser el comportamiento correcto. Al darle exit code a `qa-espacios-gating-verify` reportó **5 aserciones rojas** —"un Free puede crear un segundo espacio", "puede escribir reglas de split", "el espacio le dice isPro=true"— y **ninguna era real**: estaba en trial activo hasta el 18-ago. Lo peor de escribir un harness sin exit code es esto: nadie lo corrió nunca, así que nadie vio que su premisa se había podrido | `consumidor-no-actualizado` | esta tarea, al darles exit code por primera vez. Se descartó como regresión mirando `plan`/`trial_estado` en la DB, igual que ayer con `cron-deudas` | No, y no podía: el harness no lo corría nadie | los dos harness de Espacios comprueban el plan del QA Free contra la DB ANTES de afirmar, descartan las rojas de esa sección y salen 2 con el `UPDATE` exacto para restaurar. Verificado en las dos direcciones: con el fixture restaurado, **15/15 verdes** — o sea que el gating Free de producción está bien y siempre lo estuvo |
| 2026-08-09 | Dos de esos nueve podían salir **verdes sin poder fallar**. `qa-cat-dedup` afirma "el donut no parte una categoría por mayúsculas" y el usuario QA tenía **una sola categoría**: con una, no hay colisión posible y el verde es cierto por construcción. `qa-filter-effect` afirma "todas las filas coinciden con el filtro" sobre una tabla que puede venir vacía, donde eso es verdad por vacío. Iban a quedar como los únicos dos verdes estables del lote, que es justo la peor propiedad | `asercion-vacua-por-implicacion` | esta tarea, al mirar el DATO de la primera corrida en vez de solo el veredicto | No | umbral explícito (2+ categorías) y evaluación condicionada a que el filtro traiga filas; los dos salen 2 con el motivo. La antivacuidad general vive en `lib/veredicto.mjs`: cero afirmaciones evaluadas es exit 2, nunca 0 |
| 2026-08-09 | La primera versión del tercer check del guard de inventario (el de ayer) marcaba por prefijo de nombre; hoy volvió a morder en otra forma: al agregar `lib/veredicto.mjs` el guard se puso rojo, **que es exactamente lo que tiene que hacer**, y hubo que declararlo. Se anota no como defecto sino como la primera vez que ese guard cobró: un archivo nuevo en `qa-e2e/lib/` no entra sin que alguien diga qué es | — | el propio guard, en la primera corrida tras crear el archivo | Sí, y funcionó | — |
| 2026-08-09 | **Escribí el filtro para `free` y no corrí un solo sweep en `free` antes de pushear.** Los tres barridos toman el plan por argumento; verifiqué los seis bloques de veredicto corriendo SOLO `pro`. Al correr `free` después: `qa-sweep` **11 de 12 rojas**, `qa-planning-sweep` 3 de 6, `qa-analysis-sweep` 1 de 6, todas falsas. Dos causas distintas. (a) El 402 del muro lo registra el navegador en DOS listas —la respuesta Y una línea de consola *"Failed to load resource… status of 402"*— y yo filtré una sola, así que el gate funcionando contaba como error. (b) En `qa-sweep` afirmé pestaña Anual, donut, diálogo de categoría y recurrentes **sin mirar el plan**: con el usuario en el muro esos widgets no están porque el muro anda. La rama que agregué era justamente la del gating, y es la única que no ejercité | `cobertura-alrededor-del-cambio` | esta tarea, al quedarme sin sesiones paralelas y correr la mitad que faltaba. **No** mi verificación previa, que estaba verde 9 de 9 | No: nada obliga a correr un harness parametrizado con todos sus argumentos | filtro angosto en dos formas (el formato de consola y el de respuesta son distintos, y un `\b402\b` suelto excusaba un error de JS que mencionara el número — probado con 7 casos, incluidos crash real y 5xx), y los widgets de Pro condicionados al plan. Que el paywall se vea bien NO se duplica acá: es `qa-gate.mjs free`. Verificado en los seis cruces plan×barrido, y `pro` sin regresión (15/6/8) |
| 2026-08-10 | **El fix de B17 arreglaba el caso grande y dejaba vivo el mismo bug en chico.** El hallazgo era que el detector de gasto inusual promedia `monto` crudo mezclando monedas; el ledger prescribía `monto_pen ?? monto` y lo implementé tal cual, con tres tests que pasan. Pero `monto_pen` es NULLABLE **a propósito** (la rama USD fuera de rango deja un null honesto), así que ese `??` cae al monto crudo justo en las filas en dólares: vuelve a meter dólares en un promedio de soles y a imprimirlo con "S/". Y en la otra punta, un consumo de US$40 sin conversión se comparaba como 40 soles, o sea que el gasto más grande del mes es el único que nunca dispara la alerta. Mis tests fijaban la conducta con una fila `monto_pen: null` **implícitamente PEN**: la rama que faltaba no se ejercitaba | `cobertura-alrededor-del-cambio` | el **revisor adversarial del diff** (CLAUDE.md), no mi verificación, que estaba verde con 10 tests y las dos suites | No. Y el punto ciego es más fino que "no probé el error": probé el null, pero no el null *con moneda distinta*, que es el único que rompe | la query trae `moneda`, `enSoles` descarta la fila que no se puede expresar en soles, y el gasto nuevo en USD sin conversión **no dispara nada** en vez de mentir. Tres tests nuevos: descarte de la fila USD, silencio del gasto USD sin conversión, y muestra que cae bajo el umbral de 3 |
| 2026-08-10 | **El doble check de la Ola 2 encontró que B14 no tenía ni un test que EJECUTARA el código.** Los dos call-sites nuevos quedaron cubiertos por un barrido estático que verifica que la LÍNEA diga `llegoElAviso(variable)`. Eso no distingue `if (x)` de `if (!x)`, y sobre todo no ve un error de SCOPE: `cron/checks.js` es JS, `tsc` no lo mira, y `checkRecordatorioDiario` —el cron de las 8pm— **no lo ejecutaba ningún test** (`checkPremiumExpiry` sí, por `trial-premium-collision`). Una variable mal nombrada habría tumbado la corrida de recordatorios de todos, recién al disparar el reloj | `mis-guards-tambien-fallan` | el doble check, preguntándose qué NO puede ver un barrido de texto | El guard estático existía y estaba verde; por construcción no podía ver ninguna de las dos cosas | `tests/cron/comprobante-solo-si-llego.test.js`: ejecuta los DOS crons con `notificarUsuario` mockeado. Probado con dos mutaciones —renombrar la variable (scope) e invertir la guarda—, las dos lo matan |
| 2026-08-10 | **Y el test que escribí para cubrir eso traía una aserción vacua, en el mismo archivo.** Un tercer caso buscaba `ReferenceError` en `log.error` para probar el scope. Pasa SIEMPRE: el `catch` por usuario de `checkRecordatorioDiario` (`checks.js:227`) es `catch(e) { /* silencioso por usuario */ }` y **no loguea nada**, así que `log.error` nunca se llama. La aserción no solo era vacía: el comentario que tenía al lado describía mal el mecanismo ("se traga en el catch, se loguea"). Lo delató la mutación de scope, que mató el PRIMER caso y no el que decía cubrirlo | `asercion-vacua-por-implicacion` | la prueba por mutación del doble check, no la corrida verde | No | el caso se **borró** en vez de arreglarse (el efecto ya lo afirma el primero) y el docstring deja escrito por qué un chequeo de logs no sirve acá. El catch silencioso quedó anotado como hallazgo nuevo (B25) en el ledger |
| 2026-08-10 | **Los dos guards que escribí para cerrar hallazgos traían su propio hueco, y los dos medían texto.** (a) `columnasDeMigraciones` —el guard que existe para que la lista de columnas del merge no vuelva a quedarse atrás por tercera vez— pedía `alter table usuarios add column <x>` de una sola pieza, y se perdía la forma multi-columna `ADD COLUMN a, ADD COLUMN b`, que este árbol YA usa en las migraciones 006, 018 y 050: se habría quedado con la primera columna y las demás eran invisibles. (b) El grep que impide volver a sacar el plan de la respuesta de alertas buscaba `data?.isPro`, y `const { isPro } = data ?? {}` lo evade | `mis-guards-tambien-fallan` | el **revisor adversarial del diff**, en el mismo pase | No | (a) el parser se extrajo a `columnasQueAgrega(sql)` **exportada**, y el test la ejercita con SQL sintético multi-columna en vez de copiar el regex —una copia solo detecta que editaste una de las dos, la lección de railway-watchpatterns—; (b) el grep cubre las dos formas, probado con la mutación por destructuring |
| 2026-08-10 | **Paralelizar dos llamadas al LLM le agregó una llamada a las rutas que NO la usaban.** P′2 dispara `detectarCategoriaIA` junto al parser en `registrar_manual`. En el camino feliz eso ahorra un round-trip; en las dos salidas tempranas (el redirect a query, y el "no pude extraer el monto") el resultado se descarta — pero la request ya salió, y el cliente es `new OpenAI({ maxRetries: 3, timeout: 60000 })`: hasta **cuatro HTTP durante cuatro minutos** por un mensaje que ni siquiera era un gasto, quemando presupuesto de rate-limit justo donde el 429 ya mordió (las 163 filas de `salvarGastoSinIA`). Mi verificación estaba verde: los tests afirmaban el paralelismo y el resultado, ninguno preguntaba **qué cuesta la rama que no lo usa** | `camino-feliz-unico` | el **revisor adversarial del diff**, antes del push | No | `detectarCategoriaIA` acepta `{ signal }` y el handler aborta en las dos salidas. Cubierto por tres tests (llega al SDK, aborta en cada salida, no aborta en el camino feliz), cada uno probado con su mutación |
| 2026-08-10 | **Y el test que escribí para ese abort dejaba fuera el call-site más caro.** Cubría el abort de la rama "el parser no encontró monto" y no el del `catch` cuando el parser **lanza** — que es peor, porque si lanzó por un 429 el clasificador está reintentando contra la misma organización saturada. Lo delató que la mutación cayó por accidente en la línea equivocada (las dos tienen la misma indentación, y `String.replace` toma la primera): quitar ese `abort()` dejaba los cinco tests en **verde** | `cobertura-alrededor-del-cambio` | una mutación mal apuntada, no la que había planeado | No | test propio para la rama del throw, con su mutación; las dos ahora mueren por separado |
| 2026-08-10 | **Colapsar el 1+N en una query hizo que raíces y subcategorías compartan el techo de 1000 filas de PostgREST.** La versión vieja no podía truncar así: cada query estaba acotada por su padre. La nueva las trae juntas, y PostgREST corta **sin señalizarlo** (`data` vuelve corta, `error` sigue en `null`), así que un árbol truncado clasificaría gastos contra una lista incompleta y en silencio. Este repo ya se comió ese fallo exacto dos veces (migraciones 039 y 040). No es un bug vivo —21 raíces es el máximo en prod— pero la garantía se perdió sin que nada lo dijera | `error-no-leido` | el **revisor adversarial del diff**, antes del push | No | `log.warn` cuando la respuesta llega a 1000 filas, con test y mutación. No arregla el truncado: lo delata, que es lo que hoy no existe |
| 2026-08-11 | **Encadené las dos escrituras en UN call-site de tres, y el propio comentario que escribí decía que el patrón estaba prohibido.** B26 hace que `asegurarCategoriaUsuario` cree categorías canónicas, y `crearSubcategoriaLibreUsuario` crea el padre por su cuenta: lanzadas en paralelo insertan la misma raíz dos veces. Lo arreglé en `registrar_manual` con un comentario explicando el peligro, y dejé `corregir_categoria` y `corregir_multiple` con el patrón exacto que el comentario prohíbe. Peor: **ahí la carrera la introduje yo**, porque el guard viejo impedía que esos call-sites insertaran canónicas. Y es la lección que yo mismo había escrito **ese mismo día** en `feedback_disciplina_de_verificacion` ("si el fix tiene N call-sites, la mutación va N veces") | `cobertura-alrededor-del-cambio` | el **revisor adversarial del diff**, antes del push | No: el E2E ejercita `registrar_manual`, que era justo el que sí estaba bien | los tres encadenados, y sobre todo la **causa raíz en la DB** (migración 067): el índice único `(usuario_id,nombre,padre_id)` no cubría NINGUNA raíz, porque `padre_id` es NULL en todas y Postgres trata los NULL como distintos. Encadenar promesas solo cierra la carrera dentro de un mensaje |
| 2026-08-11 | **Escribí la fila con un nombre y la busqué con otro, y la subcategoría se perdía sin un solo log.** `asegurarCategoriaUsuario` normalizaba por `CATEGORIA_MAP` antes de insertar, y `crearSubcategoriaLibreUsuario` volvía a buscar al padre con el nombre CRUDO (`buscarCategoriaRaiz` compara exacto). Aparte, tratar todo el `CATEGORIA_MAP` como alias era una regresión de producto: ese mapa mezcla alias ortográficos (`Alimentacion`→`Alimentación`) con **colapsos con pérdida** (`Viajes`→`Otros`, `Hogar`→`Vivienda`), así que quien pedía "pásalo a Viajes" —que antes recibía su categoría Viajes— iba a recibir una raíz `Otros` que nunca pidió | `consumidor-no-actualizado` | el **revisor adversarial del diff**, antes del push | No | `resolverNombreCategoria` devuelve el nombre EFECTIVO y lo usan los dos lados; los alias se distinguen de los colapsos comparando sin tildes ni mayúsculas. Y el mock del test se hizo fiel (los inserts ahora son visibles para las lecturas siguientes): con el mock viejo este bug era indistinguible de una limitación del doble |
| 2026-08-11 | **Reusar un prompt entre dos clasificadores le metió al segundo una instrucción que contradice la suya.** El bloque de categorías custom termina con *"subcategoria debe ser una de las listadas; si no hay match exacto, usar sin_categoria"*, correcto para correos (ese prompt las declara cerradas) y contradictorio en el de gastos, cuyo system prompt dice tres líneas antes *"usa ese nombre exacto aunque no esté en la lista"*. Como el bloque se concatena AL FINAL, ganaba la restrictiva: los usuarios con árbol propio —la población de B26— dejaban de poder estrenar subcategorías nombrándolas | `efecto-lateral-al-deduplicar` | el **revisor adversarial del diff**, antes del push | No: mis tests afirmaban que el bloque estaba presente y que decía "gasto" en vez de "correo", ninguno miró si lo que dice tiene sentido ahí | la regla se volvió opcional (`subsCerradas`), con test que exige la ausencia de esa línea en el prompt de gastos y su presencia en el de correos |
| 2026-08-11 | **Arreglé B26 en el intent y me olvidé de los otros dos caminos de registro, que viven en OTRO archivo.** El fanout de multi-gasto y el de "ingreso + gastos" (`message-processor.js`) llaman `detectarCategoriaIA` pero nunca `asegurarCategoriaUsuario`, así que *"gasté 20 en taxi"* hacía crecer el árbol y *"gasté 20 en taxi y 30 en cine"* no: **el mismo gasto dejaba el árbol distinto según cómo se escribió**, y en la forma de lista seguía sin aparecer en `/categorias` — el síntoma exacto que B26 venía a cerrar. Es la TERCERA vez en la misma sesión que el fix cubre unos call-sites y no todos, y las dos anteriores ya estaban escritas en la memoria de verificación | `cobertura-alrededor-del-cambio` | el **doble check adversarial**, después del push | No: el E2E ejercitaba `registrar_manual`, y busqué los call-sites con grep sobre `handlers/intents/`, que no incluye `message-processor.js` | los dos caminos arreglados y el E2E extendido con el caso de lista. La regla que faltaba: buscar por la FUNCIÓN en todo el árbol, no por el archivo donde creo que vive |
| 2026-08-11 | **Mi "regresión de producto" era al revés, y creaba 14 raíces muertas.** Separé `CATEGORIA_MAP` en alias ortográficos y colapsos con pérdida, y dejé a los segundos como categoría libre, razonando que quien pide "Viajes" merece su categoría Viajes. Medido con el código real: `guardarTransaccion` → `normalizarCategoria` resuelve TODO el mapa, así que la transacción caía en `Otros` igual y la raíz `Viajes` quedaba **visible en `/categorias` y en el selector de presupuestos, y siempre en cero**. Peor, `_normCat` de `budget.js` no aplica el mapa, así que un presupuesto sobre ella tampoco alertaba. La mentira era el árbol, no el destino | `causa-inventada-junto-al-numero` | el **doble check adversarial**, después del push | No: el test que escribí *afirmaba* el comportamiento equivocado | la raíz se llama como la categoría donde CAE la transacción, y un guard de clase recorre `CATEGORIA_MAP` entero comparando las dos puntas: una entrada nueva mal puesta rompe el build |
| 2026-08-11 | **Escribí en DEFECTOS.md que existía un test que no existía, y el test que sí escribí era vacuo.** La entrada del mismo día decía *"con test que exige la ausencia de esa línea en el prompt de gastos **y su presencia en el de correos**"*; la segunda mitad nunca se escribió. Y el test de "el default reproduce el prompt de correos palabra por palabra" enumeraba tres frases y omitía justo la que yo acababa de volver opcional. **Medido por el doble check**: mutar esa línea dejaba la suite entera en verde, 1140/1140 | `mis-guards-tambien-fallan` | el **doble check adversarial**, que corrió la mutación en vez de leer el test | No | la aserción positiva agregada, y verificada con la misma mutación que el agente usó para demostrar el hueco |

| 2026-08-11 | **Para no repetir el mensaje del muro comparé la respuesta de la continuación con la de la primera parte (`d2.respuesta !== r1`), y eso traga la confirmación de un gasto que SÍ se guardó.** El dedup se aplica a la SALIDA, cuando el efecto lateral ya ocurrió: una continuación register+register cuyas dos mitades parsean idéntico produce la misma confirmación **a propósito**, así que el usuario terminaba con dos filas en `transacciones` y un solo ✅ — o sea reenviando el mensaje y triplicando. Es una regresión contra HEAD: el código viejo concatenaba sin comparar. La duplicación real que yo creía tapar era otra (la fila de abajo) | `efecto-lateral-al-deduplicar` | el **revisor adversarial del diff**, antes del push, midiéndolo (`guardarTransaccion` llamado 2 veces, un solo ✅) | No | la comparación se sacó y la duplicación se cortó en el ORIGEN (`ctx.redirigidoAQuery`); `tests/handlers/muro-dispatch.test.js` afirma las DOS confirmaciones, visto fallar reintroduciendo la comparación |
| 2026-08-11 | **El gate del muro se disparaba DOS veces por un solo mensaje entrante, y con él `guardarMensaje` y `analytics.capture`.** Cuando el handler de `registrar_manual` redirige a una query, resuelve el mensaje ENTERO; de vuelta en `message-processor` mi `d1.muro` valía `false` —el gate había corrido adentro del handler, invisible desde afuera— así que la continuación despachaba la misma query otra vez. Medido: 2 eventos `wa_muro_lectura`, 2 filas de `conversaciones` del gate + 1 de `webhook.js` = **3 filas idénticas**, y `obtenerHistorial` lee solo las últimas 6, o sea que un compuesto quemaba 4 de los 6 turnos del contexto del clasificador con el mismo texto. Fuera del muro, el handler de lectura corría dos veces (un `ver_reporte` se generaba dos veces) | `efecto-lateral-al-deduplicar` | el **revisor adversarial del diff**, antes del push | No: mis tests miraban el TEXTO de salida, ninguno contaba invocaciones | `ctx.redirigidoAQuery` corta la continuación en el origen, y dos tests nuevos cuentan **invocaciones** (`vecesLectura`, eventos del gate) en vez de mirar el texto — uno por cada redirect, porque quitarle el flag a uno solo pasaba en verde |
| 2026-08-11 | **El test que escribí para esa regresión era vacuo, y lo destapó la mutación, no la corrida en verde.** Usaba `'gasté 20 en taxi y gasté 20 en taxi'`, que `detectarMultiGasto` agarra **mucho antes del dispatch** (dos pares monto+preposición) y despacha por el fanout homogéneo: el test nunca tocaba la continuación, así que pasaba idéntico con y sin la comparación puesta. El mensaje tiene que tener UN solo par para llegar al camino que el test dice cubrir | `equivalencia-verde-por-casualidad` | **la matriz de mutación** (la mutación sobrevivió con 13/13 verdes); la revisión adversarial ya había señalado el defecto de producción, el test vacuo salió después | Sí: la matriz por call-site era el control y funcionó | mensaje con un solo par (`'gasté 20 en taxi y otra vez lo mismo'`), y la trampa escrita en el propio test |
| 2026-08-11 | **El guard "un solo camino de dispatch" prohibía `getHandler(` y nada más, y eso no es la única puerta.** Una revisión adversarial lo evadió con `require('./intents/reportes').handle({ intencion: 'ver_reporte', … })` — la forma más natural de escribir el quinto call-site — y el guard dio 7/7 verde entregando una lectura sin gate. Además `DIRS_PRODUCCION` no incluía la **raíz** (`index.js`, `gmail.js`) ni `scripts/`, el mismo hueco que ya había pagado el guard de `Math.random` el 05-ago, y el conteo exigía `await dispatchIntent(`, así que una llamada partida no contaba | `mis-guards-tambien-fallan` | **revisión adversarial**, con una sonda que dejó el archivo evasor y midió el verde | No | `.handle(` agregado como segunda regla (tiene cero apariciones legítimas hoy), raíz + `scripts/` + `tasks/` al barrido, conteo sin `await`, y **escrito en el archivo lo que el guard NO puede ver** (destructurar `handle`), para que nadie lo lea como hermético |
| 2026-08-11 | **El oráculo del E2E del muro podía pasar por vacuidad y el comentario afirmaba que no.** Decía reproducir la fórmula de `ver_total_gastado` y no lo hacía en dos puntos: le puse cota superior de fecha (el handler no tiene ninguna) y sumaba con `monto_pen ?? monto` donde el handler usa `monto_pen \|\| monto`. Cualquiera de los dos hace que la cadena buscada no pueda aparecer nunca, y entonces el negativo `!resp.includes(...)` pasa **siempre**: el harness reportaría el muro sano justo cuando está roto | `asercion-vacua-por-implicacion` | **revisión adversarial**, comparando el oráculo contra `services/transactions.js` | No | el oráculo copia las dos rarezas del original, con el motivo escrito al lado para que nadie las "arregle" |

| 2026-08-11 | **Mi fix de B28 introdujo una CARRERA que partía la misma categoría en dos buckets.** Para decidir si persistir el nombre custom crudo, `resolverCategoriaPersistida` consultaba el árbol del usuario. Pero quien crea esa raíz es `asegurarCategoriaUsuario`, y sus TRES call-sites la lanzan **fire-and-forget** justo antes de `guardarTransaccion` (para no devolverle round-trips al camino del gasto que la Ola 3 le sacó). O sea que el **primer** gasto de cada categoría custom perdía la carrera y persistía `'Otros'`, y el segundo persistía el nombre: el mismo concepto en dos buckets para siempre, y un presupuesto sobre esa categoría sub-contando. Antes del fix el orden no importaba porque el resultado era siempre `'Otros'` | `camino-feliz-unico` | el **revisor adversarial del diff**, antes del push; ningún test ejercitaba las dos funciones juntas | No | la función pasó a ser **pura y síncrona** (sin consultar el árbol): sin lectura no hay carrera. Un test afirma que no contiene `await` ni `supabase`, y la mutación que reintroduce la consulta mata los 7 |
| 2026-08-11 | **El guard que escribí contra alucinaciones del clasificador no existía en el sistema, solo en la función aislada.** El test afirmaba que un nombre inventado cae en `'Otros'` "para que el árbol del usuario siga siendo la fuente de qué existe". Pero el árbol NO es un oráculo independiente: se alimenta del mismo string, porque `asegurarCategoriaUsuario` crea como categoría libre cualquier nombre no canónico. Desde el segundo gasto la alucinación ya está en el árbol y se persiste cruda. El test pasaba porque el mock arrancaba vacío por construcción, no porque el sistema filtrara | `asercion-vacua-por-implicacion` | el **revisor adversarial** | No | el guard se quitó junto con la consulta: lo que de verdad acota qué nombres aparecen es el prompt del clasificador (canónicas ∪ árbol), no una re-lectura después |
| 2026-08-11 | **Los dos asserts que "adapté" en los E2E quedaron casi tautológicos, y ensuciaban al usuario QA sin limpiar.** Cambié `CATEGORIAS_VALIDAS.has(row.categoria)` por "canónica O en el árbol del usuario", consultando el árbol DESPUÉS de que el pipeline real corrió `asegurarCategoriaUsuario` — o sea que para que la categoría no fuera canónica, la raíz tenía que existir, y entonces el assert la encontraba siempre. Además el harness solo borra la transacción, así que cada raíz basura quedaba acumulada y el criterio de la corrida siguiente era más permisivo todavía | `equivalencia-verde-por-casualidad` | el **revisor adversarial** | No | los dos asserts se revirtieron: los fixtures de esos harness ("taxi", "pollería") son casos canónicos y ahí `CATEGORIAS_VALIDAS` es la aserción correcta |
| 2026-08-11 | **El `catch` que documenté como fail-safe está muerto para el fallo que dice cubrir**, y el test que lo "probaba" ejercitaba una rama que producción no puede alcanzar. `postgrest-js` sin `.throwOnError()` **no lanza**: atrapa hasta el rechazo del fetch y resuelve `{data:null, error}`. Y `buscarCategoriaRaiz` desestructura solo `{ data }`, así que un fallo real de Supabase daba `data=null` → "el usuario no la tiene", sin un solo log. Mi test hacía que `supabase.from()` lanzara **sincrónicamente**, algo que el cliente real nunca hace | `error-no-leido` | el **revisor adversarial**, verificándolo contra el fuente de `postgrest-js` | No | desapareció con la reescritura (la función ya no lee la DB). Queda anotado porque el patrón —creer que un cliente que no lanza se atrapa con try/catch— vive en más sitios |

| 2026-08-11 | **Al reescribir B28 perdí el camino DIFUSO de la normalización, y eso reintrodujo el síntoma que B28 venía a cerrar.** La primera versión (la que consultaba el árbol) arrancaba con `if (normalizarCategoria(cruda) !== 'Otros') return normalizada`. Al hacerla pura saqué esa línea porque una mutación decía que era redundante — y lo era **en la versión vieja**, donde el flujo pasaba igual por ahí. En la nueva no: `resolverNombreCategoria` compara EXACTO (`CATEGORIAS_VALIDAS.has` y `CATEGORIA_MAP[nombre]`) mientras `normalizarCategoria` además capitaliza, baja a minúsculas y saca tildes. Resultado: `"ALIMENTACION"` y `"TRANSPORTE"` —que el clasificador devuelve a veces— se persistían **en mayúsculas** en vez de resolverse a la canónica: la misma categoría partida en dos grafías, o sea el bug de B28 reintroducido por el fix de B28. Estuvo desplegado ~1 hora | `cobertura-alrededor-del-cambio` | **un test de B34**, escrito para otra cosa, que asertó `'ALIMENTACION' → 'Alimentación'` y salió rojo | Sí y falló: la mutación de B28 midió esa línea sobre la implementación VIEJA y la declaró redundante; al reescribir la función no se re-corrió | la línea vuelve, con el motivo escrito al lado, y un test propio (`una canónica mal escrita se normaliza igual`) que muere con la mutación que la quita. **La lección: una mutación mide la implementación que estaba puesta cuando corrió. Reescribir la función invalida su matriz entera** |
| 2026-08-12 | **Pushé un archivo nuevo a `qa-e2e/` sin correr la suite, y dejé `main` en rojo.** El archivo era un probe read-only para medir B31/B32 —no toca producción, así que "no hay nada que romper" fue el razonamiento— y `tests/qa-e2e-inventario.test.js` existe justamente para eso: **todo `.mjs` de la raíz de `qa-e2e/` tiene que estar en el canary o nombrado en el README**. La suite quedó roja entre el push y el arreglo, y con "Wait for CI" eso **frena el deploy del backend** para cualquiera que empuje detrás. El guard no falló: hizo exactamente lo que dice su encabezado, incluida la parte de barrer el **árbol de trabajo** para avisar antes del push — solo que hay que correrlo | `push-sin-suite` | `npm test`, corriendo DESPUÉS del push en vez de antes | Sí, y estaba puesto: el guard barre el working tree justo para atraparlo antes. Lo que faltó fue ejecutarlo | fila en `qa-e2e/README.md` para el probe. Y la regla que ya estaba escrita y no apliqué: **un archivo bajo `qa-e2e/` o `tests/` es tan "código" como uno de `services/` a los efectos de la suite**, aunque no lo ejecute producción |
| 2026-08-12 | **B28 estableció "canónica por el mapa, o el nombre crudo del usuario" y dejó CUATRO puertas más escribiendo `transacciones.categoria` sin pasar por ahí** (hallazgo B30). Tres de reglas por comercio (`corregir_categoria`, `corregir_multiple`, `editar_categoria_comercio`) y una que el hallazgo ni nombraba: el comando `/cambiar` de `webhook.js`, donde `catKnown` compara EXACTO salvo mayúsculas, así que `/cambiar berny alimentacion` no matchea por la tilde y escribe `'Alimentacion'`. No era preventivo: un usuario real tenía sus gastos de comida partidos en dos, 5 filas en `'Alimentacion'` y 5 en `'Alimentación'`, y la línea del corte era exactamente qué comercios tenían regla. Al arreglarlo aparecieron dos defectos vecinos en `editar_categoria_comercio`: anunciaba **"✅ Regla creada"** sobre reglas que `guardarReglaComercio` descartaba en silencio (no devolvía nada, así que el llamador no podía saberlo), y era el único de los tres caminos que **no aseguraba la categoría en el árbol** del usuario — o sea que dejaba filas en una categoría que `/categorias` no lista y que el selector de presupuestos no ofrece | `invariante-con-una-puerta-sin-barrer` | la revisión adversarial de B28 (hallazgo B30 del ledger), y la medición del 12-ago que le puso un usuario con nombre y apellido | No. B28 tiene su test y su mutación, y las dos miran `guardarTransaccion`: ninguna pregunta *"¿quién MÁS escribe esta columna?"* | la resolución vive en `guardarReglaComercio` (chokepoint de la tabla) **y** en cada call-site, porque resolver sólo en uno parte el pasado y el futuro del mismo comercio en dos categorías. 8 mutaciones, una por puerta, todas muertas. E2E `qa-regla-categoria-resuelta.mjs` con corrida de control contra el código viejo (6/10 vs 10/10). Migración 069 unifica las 6 reglas y 6 filas ya escritas |
| 2026-08-12 | **El E2E de la foto llevaba semanas rojo por una aserción que sólo podía pasar un día** (hallazgo B29). `qa-e2e-registro-gasto-foto` afirmaba `row.fecha === hoyPeru()` sobre un fixture PNG cuya pantalla de Yape tiene impresa `24 jul. 2026`, y el camino de la foto respeta la fecha del recibo. O sea que nació condenada a ponerse roja el 25 de julio. Y como el harness cuesta una llamada Vision real por corrida no está en el canary, así que nadie lo vio | `asercion-atada-al-calendario` | el ledger de la auditoría, que lo listó como "sin diagnosticar" | No, y estructuralmente no podía: el único que corre esa aserción es un harness manual | la aserción pasa a comparar contra la fecha IMPRESA (que además es más fuerte: prueba que Vision la LEE en vez de caer al default), y el invariante baja a la suite rápida en `webhook-imagen.test.js`, que sí corre en cada push. La mutación "forzar hoy" lo mata |
| 2026-08-12 | **El fix de B30 pisó una lección escrita 20 líneas más arriba en el mismo archivo.** `guardarReglaComercio` empezó a devolver el destino de la regla para que el llamador dejara de anunciar "Regla creada" a ciegas — y no leía el `{error}` del upsert. postgrest **no lanza** cuando rechaza (RLS, constraint): devuelve el fallo en `error` y el `await` resuelve normal, así que el `try/catch` sólo veía errores de red. Con eso el fix quedó **peor que el bug que venía a arreglar**: antes nadie miraba el retorno, y ahora el handler creaba la raíz en el árbol, retroaplicaba sobre el histórico y anunciaba "✅ Regla creada" sobre una tabla donde no entró una fila. `guardarSnapshotEliminacion`, en `handlers/intents/transacciones.js:77`, tiene esa misma lección escrita en un comentario de cinco líneas. Y dos hallazgos vecinos: el `null` mezclaba "la descarté por política" con "no la pude escribir", que piden consejos OPUESTOS (cambiar la categoría vs reintentar la misma); y el fix dejaba dos grafías divergentes vivas (nada recortaba la categoría que va a `transacciones` mientras la de la regla sí, y `editar_categoria_comercio` era el único camino que no capitalizaba) | `error-no-leido` | la **revisión adversarial del diff**, antes del push, corrida porque el cambio toca `transacciones.categoria` | Sí y falló, por partida doble: mi propio test mockeaba el upsert devolviendo siempre `{error: null}`, así que la rama no existía para la suite; y las 8 mutaciones que había corrido pasaban todas por caminos donde la escritura salía bien | el mock del test tiene ahora **tres modos** (ok / rechazo con `{error}` / excepción), el retorno es discriminado (`{ok, destino}` / `{ok:false, motivo}`), y hay un test que exige que los dos motivos NO coincidan. Matriz de mutación re-corrida entera: **12/12**. La regla que se pagó: **el mock que sólo conoce el camino feliz vuelve inexistente la rama de error, y ninguna mutación sobre el camino feliz la puede encontrar** |
| 2026-08-12 | **Medí el desfase de fechas con la conversión de zona AL REVÉS y la primera lectura salió más del doble.** `transacciones.created_at` es `timestamp without time zone` con UTC adentro, y escribí `created_at AT TIME ZONE 'America/Lima'`, que no CONVIERTE desde UTC: interpreta el naive COMO hora de Lima y lo empuja +5h. Con eso, todo lo registrado después de las 19:00 de Lima aparecía creado al día siguiente y contaba como fila "con fecha pasada": **952 de 1471** en vez de las 75 de 290 reales. Iba camino a decidir sobre B29 con un número inflado 3x | `causa-inventada-junto-al-numero` | mirar 25 filas crudas antes de creerle al agregado: los `created_at` de las 04:06 con `fecha` del día anterior no tenían sentido para un registro por WhatsApp | No hay control acá: es SQL exploratorio. Lo único que lo atrapa es no decidir sobre un agregado sin haber mirado filas | la forma correcta es `AT TIME ZONE 'UTC' AT TIME ZONE 'America/Lima'`, y queda escrita en el reporte de la medición. Regla: **un agregado que sorprende se mira fila por fila antes de usarse**, y más cuando el sentido del error es el que hace más interesante la conclusión |

| 2026-08-12 | **El cascade de un rename de categoría conocía TRES tablas y el esquema tenía más.** `/api/categories` reetiquetaba `transacciones`, `presupuestos` y `reglas_comercio` con una lista literal (`NAME_REF_TABLES`); `gastos_compartidos.categoria` y `spending_alerts.category` quedaban apuntando al nombre viejo. Sin error y sin log: las filas simplemente dejaban de agrupar con el resto. En `spending_alerts` no era cosmético — `category` es la clave de dedup de `/api/alerts` (el mismo spike se listaba dos veces, una por grafía) y el argumento del botón "ponle un límite", que fabricaba un presupuesto sobre una categoría inexistente. El comentario de la lista **declaraba** la exclusión de `gastos_compartidos` ("data que ven terceros"), o sea que el argumento correcto estaba escrito y aplicado a la tabla equivocada: la que de verdad es vocabulario compartido es `space_expenses`, cuyo selector se llena de la constante `CATEGORIAS` y no del árbol de nadie | `consumidor-no-actualizado` | auditoría CTO, de rebote al preparar la migración 069 (B30) | **No.** Nada ataba la lista al esquema, y un barrido de `information_schema` por nombre de columna tampoco habría bastado: se pierde `shared_spaces.split_rules[].category`, que es jsonb y es **la que mueve plata** (`resolveSplitPlan` matchea por `===` exacto) | TRES, porque son tres preguntas distintas y ninguna sola cierra la clase: `webapp/src/lib/category-cascade.test.ts` (hermético — ¿el código hace lo que la declaración dice?, probado por 5 mutaciones), `qa-e2e/qa-categorias-cascade-schema.mjs` (esquema vivo — ¿la declaración describe la DB?, barre también TODA columna jsonb, no solo las que se llaman "categoria"), y `qa-e2e/qa-categorias-cascade-e2e.mjs` (endpoint real — ¿el UPDATE pega en Postgres?; contra el código viejo da 4 rojos). La declaración, con el porqué de cada entrada y de cada exención, vive en `webapp/src/lib/category-refs.ts` |
| 2026-08-12 | **`likeEscape` creía volver `ilike` un match exacto, y no puede: PostgREST traduce `*` a `%` del lado del SERVIDOR.** Escapaba `%`, `_` y `\`, así que el comentario decía "match exacto CI" y el guard lo confirmaba… sobre el string ya escapado. Medido contra prod: sembrando tres alertas, `ilike('category', '*')` devuelve las TRES. `cleanName` solo valida largo 2–30, o sea que una categoría llamada `*` es creable desde el panel, y con las políticas `delete` de CASCADE_REFS su borrado es un DELETE sin `where` sobre los presupuestos, las reglas y las alertas de esa persona, más todas sus transacciones a "Por revisar" — sin rastro, porque el trigger de la 055 no cubre esas tablas. Y el mismo operador estaba un paso antes: el POST busca la fila existente con ese filtro, así que crear `QA *` chocaba 409 contra `QA Alpha`, o si estaba inactiva la reactivaba y la RENOMBRABA. Exposición real al 12-ago: **cero** (ninguna categoría ni valor de `transacciones` en prod contiene `*`, `%` o `\`) — latente, no activo | `guard-que-afirma-sobre-el-intermedio` | **revisión adversarial del diff**, antes del push | **No, y peor: el guard que escribí ese mismo día lo tapaba.** Afirmaba `patron === '100\%\_ahorro'`, o sea el valor que produce `likeEscape`, no lo que Postgres hace con él. Un test que compara contra su propia salida no puede ver que el operador la reinterpreta. El E2E tampoco: sembraba UNA fila por tabla y ninguna de control, así que un filtro que matchea de más era estructuralmente invisible | `exactCI()` (regex POSIX anclada + metacaracteres escapados) sobre `imatch`, en las **tres** copias — el cascade, el endpoint y `usage/route.ts`, que es el que cuenta lo que el usuario ve antes de confirmar. Un test que prohíbe `.ilike(` en los tres archivos, el fake del guard que **lanza** si le pasan otro operador, y el paso 4 del E2E, que siembra un TESTIGO y exige que sobreviva. Verificado por 8 mutaciones |
| 2026-08-13 | **Escribí el filtro de intención del salvavidas del 429 rechazando palabras funcionales del español, y lo justifiqué con una premisa falsa que yo mismo puse en el comentario.** `extraerGastoSinIA` descartaba el mensaje si contenía `que`, `como`, `cuando`, `donde` o `cambio` en CUALQUIER posición, y el comentario decía *"el costo de no salvar es que el usuario recibe 'reenvía tu mensaje' y lo reenvía"*. La función es **pura y determinista**: reenviar el mismo texto cae en la misma rama, así que "gasté 20 en el taxi **que** me llevó al trabajo" no se registraba NUNCA mientras durara el 429 — que es exactamente el escenario para el que existe el salvavidas, y el mensaje al usuario le prometía lo contrario. Cinco frases naturales medidas, cinco `null`. En el mismo bloque viajaron dos más: `abono` como verbo de ingreso hacía que *"compré abono 50 para las plantas"* entrara con el **signo invertido** (gasto → ingreso, inflando ingresos, ahorro y score), y `prest[eé]` sin el `me` convertía *"presté 100 a Juan"* —una deuda— en un gasto propio | `justificacion-que-no-se-midio` | **revisión adversarial del diff**, antes del push | **No, y mis tests lo bendecían.** Había escrito un test llamado *"rechaza de más antes que de menos (falso rechazo documentado)"* que fijaba el comportamiento como decisión deliberada. Un test que documenta una decisión no la valida: la premisa que la sostenía nunca se ejercitó, y era falsa | los interrogativos y los verbos-comando van **anclados al inicio** (`arranca()`, con relleno de saludo), porque en español la pregunta y el comando se FORMAN al principio y en el medio esas mismas palabras son relativos y sustantivos. `RE_OTRO_COMANDO` se partió en `RE_OTRO_DOMINIO` (cualquier posición: sustantivos que dicen "esto es otra cosa") y `RE_COMANDO_INICIO`. El verbo de GASTO gana sobre la palabra de ingreso. 36 tests en `tests/lib/salvage-429.test.js`, con los cinco falsos rechazos fijados como contraprueba y la mutación al código viejo matando 16 |
| 2026-08-13 | **Al validar el monto que se copia en `/api/split/join` rompí un flujo vivo: un participante con parte S/0 quedaba con 409 permanente.** Puse `parseMontoDinero(participante.monto_debe)` sin `allowZero`, pero `POST /api/split` crea participantes con `allowZero: true` — o sea que una parte de S/0 es un dato que la propia API escribe a propósito. La ruta se contradecía consigo misma: dos líneas más abajo, `estado: montoPendiente <= 0 ? 'pagada'` ya contemplaba ese caso | `camino-feliz-unico` | **revisión adversarial del diff**, antes del push | **No.** Los tests cubrían el validador (`parseMontoDinero`) y el rechazo de montos corruptos; ninguno preguntaba qué valores LEGÍTIMOS produce el otro extremo del mismo flujo. Es `cobertura-alrededor-del-cambio`: verifiqué que lo inválido se rechaza, no que lo válido sigue pasando | `allowZero: true` en las dos puntas, y la regla: **antes de validar un valor que se COPIA, hay que leer quién lo escribe** — el rango que acepta el productor es el contrato, no el que uno supone |
| 2026-08-13 | **El guard de clase que escribí para cerrar esta clase tenía tres evasiones y un bug de estado, y una de las evasiones estaba VIVA en el repo.** `tests/plata-validada.test.js` (1) miraba 400 chars después de `.insert(`, así que armar el objeto arriba y pasarlo por variable —estilo normal— lo hacía invisible; (2) daba por validado cualquier archivo donde apareciera la PALABRA `validarMonto`, y `webapp/src/app/api/transactions/route.ts` e `import/route.ts` tenían cada una su **copia local** con ese nombre, o sea que el guard bendecía la deriva copiada-a-mano que `money.ts` existe para terminar; (3) `soloCodigo` rellenaba los comentarios de bloque con espacios de la misma longitud, así que un JSDoc normal de este repo empujaba el `monto:` fuera de la ventana. Y `RE_PERSISTENCIA` llevaba flag `g`, con lo cual `.test()` avanzaba `lastIndex` y devolvía veredictos **alternados** para el mismo fuente | `guard-que-bendice-lo-que-vino-a-vigilar` | **revisión adversarial**, que escribió los tres fragmentos que pasaban verde | **No.** Mis contrapruebas eran las formas que yo ya tenía en la cabeza (el `.update({ budgets })` de S′6 y variantes), o sea que probaban el detector contra su propio diseño | los tres fixtures de evasión quedaron como tests; el argumento por variable se **resuelve** (se busca la declaración y se mira esa ventana); `DUENOS_DEL_VALIDADOR` marca como copia toda definición fuera de su módulo; los comentarios de bloque se colapsan; y `RE_PERSISTENCIA` perdió el `g`. Las dos copias locales se reemplazaron por `parseMontoDinero`. **`EXENTOS` quedó vacío**: cero escrituras de plata fuera del validador en los dos árboles |

| 2026-08-13 | **Arreglé la inversión de signo del salvavidas y la reintroduje al revés, en el mismo día.** Primera versión: el tipo miraba solo las palabras de ingreso, así que *"compré abono 50 para las plantas"* entraba como INGRESO. Segunda: "cualquier verbo de gasto gana", que rompió el mixto — en *"me pagaron 500 y compré 100 de comida"* el MONTO sale del verbo de ingreso (500) y el TIPO del de gasto, o sea un ingreso registrado como gasto. Las dos son la misma falla de fondo: **el monto y el tipo se decidían con reglas distintas**. En la misma vuelta, anclar los interrogativos al inicio (que arreglaba el falso rechazo masivo) abrió el caso opuesto: *"el sueldo de 3000 cuando entra"* es una consulta pura y fabricaba un **ingreso de S/3000**, que va al ahorro y al score. Y el `return null` por monto fuera de rango mataba el rescate entero cuando el mensaje traía un número de recibo ANTES del monto (*"pagué mi recibo 1234567890 de 80 soles"*) | `correccion-que-abre-la-direccion-opuesta` | **segunda vuelta de la revisión adversarial**, con sondas ejecutadas sobre el código v2 | **No.** Mis 36 tests cubrían los casos que la PRIMERA vuelta había reportado, uno por uno. Ninguno era un mensaje MIXTO ni una consulta sin signo de pregunta: probé el parche, no la regla | el tipo lo decide **el verbo que produjo el monto** (`verbo[0]`), no el texto entero — una sola regla cierra las dos inversiones. Los interrogativos **acentuados** rechazan en cualquier posición (con tilde no son ambiguos) y los sin tilde siguen anclados. `sueldo`/`salario`/`depósito` salieron de la lista de verbos: son SUSTANTIVOS y como evidencia son mucho más débiles que un verbo conjugado. Una corrida de 8+ dígitos se saltea como identificador en vez de abortar |
| 2026-08-13 | **Segunda tanda de agujeros en mi propio guard de clase, y esta vez la encontró la MUTACIÓN, no la lectura.** Corrí una batería de 11 mutaciones (una por archivo arreglado) y **5 sobrevivieron**. Tres eran huecos reales: (1) los chokepoints se **auto-bendecían** — `services/debts.js` contiene la palabra `registrarDeuda` porque es el nombre de su propia función exportada, así que quitarle `validarMonto` dejaba el guard verde; (2) `String.match` sin `g` devuelve la PRIMERA declaración del archivo, no la más cercana, así que dos funciones con el mismo nombre local (`const fila` inocuo arriba, `const fila` con plata abajo) hacían leer la ventana equivocada; (3) `$` es identificador válido en JS **y** ancla en regex, así que `const $fila = {...}` producía un patrón que no matchea nunca. Más el literal de array (`const filas = [{...}]`), que es la forma más común de las dos que el docstring declaraba no ver | `guard-que-bendice-lo-que-vino-a-vigilar` | la **batería de mutación** (huecos 1 y el array) y la **segunda revisión adversarial** (huecos 2 y 3, medidos con sondas) | **Sí y falló**: mis contrapruebas eran las formas que yo ya tenía en la cabeza. La mutación por archivo es lo único que preguntó *"¿este guard ve ESTE arreglo?"* en vez de *"¿ve la forma que imaginé?"* | los chokepoints valen solo si el archivo los **llama** y no los **define**; se recorren TODAS las declaraciones del nombre; `$` se escapa; se acepta el literal de array y el `.push()` sobre un array insertado por tajadas o mapeos. La regla que se pagó: **un guard nuevo se prueba mutando cada sitio que dice cubrir, uno por uno** — su propia contraprueba solo prueba su diseño |
| 2026-08-13 | **Un harness que le pega a producción le mandaba a Favio una alerta indistinguible del evento real que ese detector existe para avisar — y el aviso traía el comando de la medición ya armado sobre el celular de un desconocido.** `avisarPrimeraVezSilencioso` (que escribí el 09-ago) avisa la primera vez que un usuario CONOCIDO llega sin número: es el único mecanismo que abre sola la ventana para medir si a un username-only todavía se le puede escribir. `qa-bsuid-username.mjs` siembra un usuario con BSUID y `whatsapp = '519' + 8 dígitos AL AZAR` —el espacio exacto de los celulares peruanos reales—, le pega al webhook de prod y lo borra. O sea que **cada corrida producía el evento**. La leí como un usuario real y estuve a punto de correr el probe, que **manda un WhatsApp de verdad**: solo salió exit 2 porque el harness ya había borrado la fila. Y lo peor está un nivel abajo: `del()` no miraba `r.ok` y el chequeo de limpieza era un `console.log`, así que el harness salía **0 con la fila viva**; si queda viva, `obtenerOCrearUsuario` la **adopta** cuando esa persona escribe, hereda `is_test_user`, y `enviarWhatsapp` deja de llamar a Meta: **el bot le queda mudo para siempre**, con `merge_and_link` propagándolo por OR. En la misma vuelta: el caso B contamina `errores` con filas idénticas a las reales (el 13-ago, **7 de 7** eran de harness y el conteo se leía como eventos reales — es la base sobre la que está construido todo el diseño BSUID), y `webhook-harness` stubeaba `enviarWhatsapp` pero **no** `enviarTelegram`, así que hasta un harness "local" le escribía al celular de Favio en cada corrida | `el-harness-produce-el-evento-que-el-detector-vigila` | La alerta misma, leída como real. La cadena entera —adopción de la fila, contaminación de `errores`, el Telegram del harness local— la encontró la **revisión adversarial** del fix, no yo | **No, y mi propio fix no traía ninguno**: borrar los dos literales `is_test_user: true` dejaba toda la suite verde. `qa-guard` no podía verlo porque ese harness usa su propio cliente REST **a propósito** (es el oráculo; compartir cliente con el código bajo prueba deja de serlo) | cuatro piezas, porque son cuatro preguntas: el corte por `is_test_user` en el aviso (misma marca que ya usa `enviarWhatsapp`, con test de mutación); `qa-e2e/qa-usuarios-marcados.mjs` en el canary, **lista BLANCA** de fixtures (acá el peligro es una fila marcada que nadie declaró, así que el default es gritar); `tests/qa-e2e-siembra-marcada.test.js`, guard estático con `EXENTOS` **vacío**; y el cleanup que asevera de verdad (`del()` lanza, el borrado es un `check()` atado al exit code, y se traga sus errores porque corre desde un `finally` — lanzar ahí REEMPLAZA la excepción que de verdad importa). **El hueco que queda, declarado y no tapado:** el aviso ya no tiene cobertura viva del POSITIVO. Intenté dársela sembrando un usuario sin la marca y `qa-guard` lo bloqueó, con razón: esas filas entran al embudo como un alta real. Queda unitario (5 tests) y lo vivo asevera la supresión contra `h.telegrams` |
| 2026-08-14 | **Emití un código de 32 chars contra una columna `character varying(12)`, y ninguna de las dos rutas leía el `error` del UPDATE.** Postgres no trunca: tira `22001`. La ruta devolvía **200 con un código que la fila nunca guardaba** —link roto para siempre, en silencio— y la rama idempotente (`if (!inviteCode)`) no lo reparaba: cada intento acuñaba otro que tampoco se guardaba | `error-no-leido` | el **revisor adversarial** (primer pase) | **No, y por una razón que vale escribir**: el harness corría contra `app.neto.pe`, que servía el código VIEJO de 8 chars — o sea que la única corrida que existía era la de CONTROL. El unit test afirma sobre la función pura y no puede ver el ancho de la columna | 12 chars de 55 (entra en la columna); las dos rutas leen el `error` y devuelven 500; y `qa-invite-codes` **re-lee la fila** después del POST (`deuda_codigo_persiste`), que es el check que lo habría visto sin saber nada del esquema |
| 2026-08-14 | **El estimador de entropía de mi harness aprobaba códigos débiles, y mi arreglo lo empeoró.** La v1 contaba el alfabeto observado (rojo falso: 57.7 bits de 69.4 reales). La v2, por CLASE, leía un alfabeto de 8 símbolos como 71.5 bits. La v3, invirtiendo el coleccionista de cupones, aprobaba **78 de 200 veces** un largo de 11 (63.6 bits, justo bajo el piso) y hacía **flakear el caso real 10 de 200** | `estimador-que-cambia-un-error-por-otro` | el **revisor adversarial** (segundo pase), con la tabla medida | No: las tres versiones las escribí y verifiqué yo, y las dos primeras las "validé" con una sola corrida verde | cuatro reglas DETERMINISTAS sobre la forma (largo, charset, distintos, clases): 0/50.000 en las siete mutaciones y 50.000/50.000 en el real. Más una autoprueba con fixtures que corre ANTES de juzgar a producción |
| 2026-08-14 | **El chokepoint de `invite_code` que escribí para cerrar S′10 no barría `lib/`, `scripts/` ni la raíz del backend** — justo donde vive `lib/codigos-seguros.js`, el sitio más natural para un helper de invitaciones nuevo. El guard HERMANO del mismo archivo ya barría esos tres. Tampoco veía `fila.invite_code = gen()` | `barrido-de-un-solo-arbol` | el **revisor adversarial** (segundo pase), midiendo con probes | No, y la contraprueba que sí corrí explica por qué: muté los 6 archivos que el guard DICE cubrir, y los 6 están en directorios que sí barría. **Mutar lo cubierto no puede encontrar lo no cubierto** | una sola lista `RUNTIME_BACKEND` compartida por los dos guards (dos listas que deberían ser la misma divergen; una sola no puede) + el patrón de asignación a propiedad. 7/7 probes rojos |
| 2026-08-14 | **El guard de S′8 barría POR LÍNEA, así que no veía un template literal multilínea, una concatenación en varias líneas ni el HTML armado en una variable** — las tres formas más naturales de reescribir la rama que acababa de borrar | `la-unidad-del-barrido-no-es-la-linea` | el **revisor adversarial** (segundo pase) | **No, y mi mutación mintió**: reemplacé el redirect por HTML y el guard salió rojo, pero por la aserción del `redirect`, no por el detector. Un mutante que muere por otra razón no mide lo que uno cree. La mutación honesta es agregar HTML nuevo SIN tocar el redirect, y ahí los tres pasaban verde | extracción del argumento de cada `.send/.write/.end` con paréntesis balanceados. De paso apareció que anclar en `res.` veía **1 de 5** respuestas (la mayoría son `res.status(400).send(`), y la antivacuidad lo delató |
| 2026-08-14 | **Cerré el segundo confirmante en `debts/join` y dejé la ruta hermana `split/join` con el mismo bug**: su UPDATE de `usuario_id` —el ÚNICO token de idempotencia de esa ruta— iba después del insert y sin leer el error. Toqué `split/invite` en el mismo diff y no miré `split/join` | `invariante-con-una-puerta-sin-barrer` | el **revisor adversarial** (segundo pase) | No. El harness tenía `split_segundo_confirmante` y pasaba **igual con el bug**, porque en el camino feliz el UPDATE anda | reclamo PRIMERO y condicional (`.is('usuario_id', null)`), con release si el insert falla; y `split_reclamo_persiste`, que pregunta por la FILA y no por el 200 — muere con el mutante |
| 2026-08-14 | **Escribí un test cuya aserción no puede fallar nunca.** Al escalonar los crons (P′10) el desfase es `offsetMs = (i * PASO) % cadaMs`, y el test afirmaba `offset < cadaMs`: eso es una propiedad del operador `%`, cierta para cualquier `PASO`, incluido `0`. O sea que el test se leía como "el desfase está bien" y no miraba nada | `asercion-que-repite-la-formula` | **mi propia matriz de mutación**, que es la única razón por la que se vio: puse `PASO_OFFSET_MS = 240 * 1000` esperando rojo y salió **verde** | No, y no podía haberlo: el defecto está DENTRO del control. Lo único que lo delata es mutar el código que el test dice cubrir y exigir que muera | la aserción pasa a ser la propiedad que el fix produce y que sí puede romperse: **dos tareas del mismo período no comparten instante de arranque**. Con `PASO = 0` o `PASO = ` el período, muere |
| 2026-08-14 | **El wrapper de no-solape difería la llamada un microtask.** Reemplacé 20 `setInterval(fn, ms)` directos por un `correrSinSolape(nombre, fn)` que hacía `Promise.resolve().then(fn)` para normalizar el throw síncrono. Funcionalmente parece lo mismo y no lo es: `setInterval` invoca `fn` sincrónicamente, y el wrapper la corría una vuelta de microtasks después. Nada se rompía hoy, pero es un cambio de orden de ejecución que nadie pidió, escondido dentro de una refactorización "sin cambio de comportamiento" | `efecto-lateral-al-deduplicar` | **el test que escribí a continuación**, que falló en las 4 aserciones porque contaba llamadas justo después de invocar | Sí en el sentido que importa: el test se escribió antes del push y falló. Lo que NO había era una razón para elegir el `.then()` — se eligió por comodidad de manejar el throw síncrono, sin mirar qué más traía | `fn()` se llama sincrónicamente con `try/catch` alrededor: la excepción síncrona sube tal cual (termina en `uncaughtException`, como hoy) y la rechazada llega a `unhandledRejection`, que es quien avisa al admin. Cubierto por el caso "una excepción SÍNCRONA libera el registro y sube tal cual" |
| 2026-08-14 | **Mis dos guards de los crons no ejercitaban el cableado: cuatro mutaciones de producción pasaban en verde con 66/66.** `scheduling.test.js` leía `cron/index.js` como TEXTO con regex y `sin-solape.test.js` probaba la primitiva AISLADA; nadie hacía `require` del módulo que programa. Sobrevivían: borrar `if (t.alBoot) ...` (los 4 barridos dejan de correr al boot, incluido el de reconciliación de cupos Gmail), poner el desfase en `0` (**vuelve el hallazgo P′10 entero**), borrar el `setTimeout` envolvente, y cablear `setInterval(fn)` salteándose el anti-solape —con su propio test siguiendo verde, porque la primitiva funciona sola—. Y una quinta: comentar un nombre del `module.exports` de `checks.js` deja la entrada en `undefined`, y el test que dice *"cada tarea resuelve a una función"* afirmaba sobre un regex del texto del mapa, así que **no resolvía ninguna función** | `mis-guards-tambien-fallan` | el **revisor adversarial**, midiendo las cinco con `sed` | No. Mi matriz de mutación tenía 7 mutantes y los 7 murieron, pero todos eran de `schedule.js` y de la primitiva: **muté lo que el guard decía cubrir, no lo que no cubría** | `cron/programar.js`, extraído justamente para poder correrlo: `tests/cron/programar.test.js` lo ejercita con relojes falsos (una tarea que nunca resuelve tiene que llamarse UNA vez en 10 ticks; dos del mismo período no pueden arrancar juntas). Las cinco mutaciones mueren |
| 2026-08-14 | **El guard de cadencia cubría una forma de gate de las dos que existen.** `tieneGateDeMinutos` solo reconocía `getMinutes() > 14`, así que `checkPremiumExpiry` y `checkTrialExpiry` (`getHours() >= 8`) y `checkRecordatorioOnboarding` / `checkActivacionDia2` (9-21h) quedaban fuera de toda aserción — teniendo **el mismo acoplamiento período↔ventana** que el guard existía para vigilar. Medido: subir `checkPremiumExpiry` de 1h a 24h pasaba en verde, y con un boot a las 3am ese check no vuelve a correr nunca. Y le faltaba la cota superior: bajar un check de 15 min a 1 min también pasaba, con 15x de carga contra Supabase | `guard-que-afirma-mas-de-lo-que-ejercita` | el **revisor adversarial** | No, y el encabezado del guard describía el invariante general ("período ≤ ancho de ventana") mientras el código miraba un caso | `ventanaDe()` deriva el ancho de las dos formas y **tira** ante una tercera que no reconozca (sub-reportar acá es un PASS sobre un cron que no corre), más la cota superior de "una sola entrada por ventana" |
| 2026-08-14 | **Apliqué un desfase global calculado sobre el índice del array, y la tarea de período más corto lo pagó.** `keepWarmWebapp` (4 min) es la última entrada de la tabla: le tocaba `22 × 20s = 440s`, y aunque el `% cadaMs` protege el período, el primer tick del interval cae en `offset + cadaMs` — o sea que pasaba de +4 a **+7min20s** después de cada deploy. Es la única tarea que existe para que no haya ventana fría de Vercel, justo cuando el proceso reinicia | `ampliar-el-alcance-sin-reapretar-el-predicado` | el **revisor adversarial** | No. Mi test afirmaba que los offsets del mismo período son distintos, que es cierto y no dice nada sobre si el offset es SANO para ese período | `calcularOffsets` trabaja **por grupo de período** (una tarea sola en su grupo lleva offset 0) y acota el paso a `período/(n+1)`. De paso desaparece la dependencia del índice global: reordenar la tabla ya no mueve los offsets de otros grupos |
| 2026-08-14 | **El comentario prometía fail-fast y el proceso entregaba fail-parcial y vivo.** `programar()` tiraba en medio del `forEach` ante un nombre que no resuelve, con el comentario diciendo *"es la clase de fallo que este archivo no puede permitirse"*. Pero `startCronJobs()` corre dentro de un `setTimeout` y el handler de `uncaughtException` **no hace `process.exit`** (a propósito): las tareas anteriores al typo quedaban programadas, las posteriores no, `limpiarContadores` —que se programa después— tampoco, y Railway veía un proceso escuchando y sano | `justificacion-que-no-se-midio` | el **revisor adversarial**, siguiendo el throw hasta el handler de proceso | No: escribí la promesa del comentario sin seguir qué le pasa al throw dos capas más arriba | validación de la tabla ENTERA antes de programar nada; la tarea rota se saltea y se reporta por `alFaltarFuncion` → `registrarError` + `notificarErrorAdmin`, y las 23 sanas quedan programadas |
| 2026-08-14 | **Mi arreglo del guard de cadencia movió el borde en vez de cerrarlo.** Generalicé el detector para cubrir los gates horarios (`getHours() >= 8`) y verifiqué con la mutación "24 horas", que murió. Pero el barrido de arranques seguía yendo de 0 a 3600s, o sea que **solo probaba boots dentro de la hora 0**, y una ventana horaria se repite cada DÍA, no cada hora. Medido: `checkPremiumExpiry` con período de **20 h** pasaba en verde, y arrancando a las 04:00 no cae un solo tick entre las 8 y las 24. Mi mutación de 24 h moría **por casualidad**: la hora 0 queda fuera de esa ventana; con una ventana que incluyera la hora 0, 24 h también pasaba | `guard-que-afirma-mas-de-lo-que-ejercita` | la **segunda vuelta** del revisor adversarial | No, y lo que falló fue el DISEÑO de la mutación, no su ejecución: elegí el valor que confirmaba el arreglo en vez del que lo tensaba. La mutación pasó, así que di la clase por cubierta | barrido de 24 h para la clase `horas`, y la exigencia por **día** (sobre varios días un período que no divide 24 entra por deriva y taparía justo el salto). Con eso mueren 20 h, 24 h y 25 h; 11 h sobre una ventana de 24 sigue pasando, que es correcto |
| 2026-08-14 | **El ancho de ventana que el guard leía del gate no era el que manda.** `checkRecordatorioOnboarding` gatea de 9 a 21h, así que el guard le concedía 12 horas de período. Su elegibilidad real vive en el `WHERE`: `created_at` entre `hace6h` y `hace3h`, una ventana móvil de **3 horas** que se cierra sola. Con período de 6 h el guard pasaba en verde y un usuario registrado a las 07:00 (elegible de 10:00 a 13:00), con ticks a las 09:30 y 15:30, **no recibe el empujón nunca** | `guard-que-afirma-sobre-el-intermedio` | la **segunda vuelta** del revisor adversarial | No. El detector "falla cerrado" ante una FORMA sintáctica que no reconoce, pero la ventana móvil no está en el gate: no hay forma de derivarla del texto, y yo no fui a buscar de dónde salía la elegibilidad real | el guard detecta la forma que **no puede medir** (`.gte` + `.lte` sobre `created_at` en el mismo cuerpo) y **exige** que `schedule.js` declare `ventanaMaxMs`, tomando el mínimo entre gate y ventana móvil. Borrar la declaración pone el guard rojo |
| 2026-08-14 | **Puse el umbral de atasco en TICKS y escribí el comentario en minutos.** `sin-solape.js` avisaba a los "3 ticks ≈ 45 minutos", pero el mismo `3` significa 45 min para una tarea de 15, 12 min para `keepWarmWebapp` y **72 horas** para `checkGmailHuerfanos` — que es justo la otra tarea que llama a Google sin timeout. Tres días de silencio en el segundo camino de atasco más probable. Y avisaba UNA sola vez (`=== umbral`), así que una fila suelta en `errores` nunca dispara el detector de patrón (5 iguales en 1 h) y si el WhatsApp cae por el cooldown global de 5 min, no queda rastro accionable | `justificacion-que-no-se-midio` | la **segunda vuelta** del revisor adversarial | No: el comentario tradujo ticks a minutos usando el período de UNA tarea y lo presentó como el umbral de todas | umbral en **milisegundos colgada** y reaviso con backoff ×4 (45 min, 3 h, 12 h…). El test pasa el reloj en vez de contar ticks |
| 2026-08-14 | **Un efecto lateral al cargar el módulo reventó un test al importar.** `google.options({ timeout })` en el tope de `gmail.js` tumbó `tests/gmail-estado-auth.test.js` con `google.options is not a function`: su doble de `googleapis` cubría `auth`/`gmail`/`oauth2` y nada más | `efecto-lateral-al-deduplicar` | `npm test`, en la misma sesión | **Sí, y funcionó**: el archivo entero se cayó en el import. Vale anotarlo igual porque un doble incompleto **no falla con una aserción** — se lleva el archivo puesto, y el mensaje apunta al mock, no al cambio que lo causó | `options` agregado al doble, guardando lo que se le fija, con el comentario que explica de dónde viene |
| 2026-08-17 | **La guarda que escribí contra el borrado por frase ambigua cerró UNA de dos puertas idénticas.** Puse el filtro en `deshacer_ultimo` con un comentario que afirmaba que era "el ÚNICO borrado sin sujeto". Es falso: `eliminar_transaccion` sin comercio/monto/fecha cae a `obtenerUltimaTransaccion` y borra igual, y **las dos salen del mismo tool** (`manage_transaction`, `neto-tools.js:818/824`) — quién de las dos sale lo elige gpt-4o-mini. O sea que el mismo "Quiero reiniciar" clasificado como `delete` sin argumentos seguía borrando, y además escribiendo en `gmail_excluidos` | `ampliar-el-alcance-sin-reapretar-el-predicado` | **revisión adversarial** del diff. Mi verificación estaba verde: 1465 tests y la mutación de mi guarda mataba 3 casos | No, y no podía: mis tests preguntaban "¿el intent guardado borra?". Ninguno preguntaba "¿queda otro intent que haga lo mismo?" — una propiedad sobre la EXISTENCIA de un camino gemelo, que no se ve mirando el call-site | `pideBorrarUnGasto()` compartido por los dos, con test que recorre las dos intenciones y mutación que mata 1 caso más |
| 2026-08-17 | **Mi guarda dejaba pasar justo las frases más caras: las de borrar la CUENTA.** `PIDE_BORRAR` acepta `elimin`, `borr`, `cancel`, así que **"quiero eliminar mi cuenta"** la pasaba y terminaba borrando el último gasto con un "Listo. Eliminé Sueldo S/480". El destino correcto de esa frase es `desconectar_cuenta`, y es la MISMA clase de misroute que produjo "Quiero reiniciar". La guarda verificaba que el mensaje nombrara *un* borrado, no que nombrara una **transacción** | `ampliar-el-alcance-sin-reapretar-el-predicado` | **segunda revisión adversarial** (la del ARREGLO) | No | `HABLA_DE_LA_CUENTA` como reverso, con `it.each` de cuatro frases reales sobre las dos intenciones |
| 2026-08-17 | **La regex que arregla lo anterior nació NO-OP: los `` quedaron como el carácter BACKSPACE (0x08).** La escribí con un script y el escape se corrompió al pasar por heredoc. La regex **compilaba sin error y no matcheaba nada**, o sea que la guarda existía y era decorativa. Lo peor es cómo se esconde: `sed` la muestra igual que si estuviera bien, y `JSON.stringify` también — porque `` **es** el escape de backspace en JSON. Sólo `re.source` lo delata | `el-guard-nacio-vacuo` | el **test**, que siguió rojo después del "arreglo". Yo había verificado la lógica con node y daba `true`: probé la regex que creía haber escrito, no la que estaba en el archivo | Sí, y es lo único que lo vio | La línea se reescribe con `String.raw` desde un archivo (no inline), y el gotcha queda pegado a la función. **Regla que deja:** si un patrón "no matchea sin razón", imprimí `re.source`, no el archivo |
| 2026-08-17 | **Marcaba la baja declarada ANTES de mirar si el borrado había fallado.** El `if (fallos.length)` que agregué para no mentirle al usuario estaba al FINAL, después del UPDATE: con un statement timeout en el delete, la fila quedaba marcada como baja, salía el aviso al admin diciendo "movimientos borrados: 131" y lo único que cambiaba era el texto que leía la persona. Tres afirmaciones falsas sobre el mismo evento | `camino-feliz-unico` | el **test que escribí para esa misma rama**, en la corrida siguiente | Sí | El corte va antes de escribir nada. Mutación: sacar el `if (error)` del bucle de deletes mata el test |
| 2026-08-17 | **El guard de call-sites que escribí para el MRR se medía contra su propia documentación.** Verificaba que las rutas admin pidieran `cuenta_borrada_at` buscando la cadena en el TEXTO del archivo — y el comentario que yo mismo había escrito arriba del `.select()` contiene la palabra. Con la columna borrada del select, el guard seguía verde | `el-guard-nacio-vacuo` | la prueba por mutación, en el acto | Es el guard | Extrae el `.select()` que cuelga de `from('usuarios')` **después de borrar los comentarios**, más un caso antivacuidad que exige haberlo extraído. (Este archivo NO se pusheó: quedó con el trabajo de MRR diferido) |
| 2026-08-17 | **El wipe acumulaba los fallos con lo IRREVERSIBLE primero, así que un fallo a mitad dejaba el peor estado posible.** `ejecutarBorradoTotal` borraba `transacciones` → `categorias_usuario` → `presupuestos` juntando los errores en un array en vez de cortar. Si fallaba la segunda: movimientos ya borrados (irrecuperable), categorías y presupuestos vivos, `cuenta_borrada_at` sin poner, sin aviso al admin — y a la persona se le decía *"No pude eliminar todos tus datos"*, lo contrario de lo que pasó. Y el remate: `onboarding_paso` quedaba en **-1**, así que el mensaje siguiente que no fuera el número del menú caía en *"Cancelado. Tu cuenta sigue igual 👍"*. Segunda afirmación falsa sobre el mismo evento, y el *"escríbeme de nuevo y lo reintento"* era literalmente imposible de obedecer | `camino-feliz-unico` | la **segunda revisión adversarial** del 17-ago. Se pusheó igual, con el motivo de que el arreglo completo pedía repensar el flujo | No: el único test de la rama de fallo sembraba el error en `transacciones`, la PRIMERA — o sea justo el caso donde acumular y cortar dan lo mismo. Un fallo en cualquiera de las otras dos no lo miraba nadie | El arreglo es de **orden**, no de mensajes: lo irreversible va último y se corta al primer fallo, así que el invariante *"si algo falla, las transacciones siguen ahí"* sale gratis y el texto no puede mentir en ninguna rama. Cuatro tests nuevos, cada uno visto rojo con su mutación (orden viejo sin `break`, quitar el reset del paso -1, volver al mensaje viejo) |
| 2026-08-17 | **Mi test nuevo aseveraba sobre un intermedio en vez de sobre el hecho.** Para probar que un wipe fallado no toca `transacciones` afirmé `expect(otherChains['transacciones']).toBeUndefined()` — el chain del mock se crea lazy en el primer `from(tabla)`, así que su ausencia parecía prueba de que nadie la tocó. No lo es: el **conteo previo** al borrado (`select ... head:true`, lo que hace accionable el aviso al admin) ya lo crea. La aserción correcta es contar los `.delete()` | `guard-que-afirma-sobre-el-intermedio` | `npx vitest`, en el acto | Sí, y ruidosamente: falló al primer intento. Vale anotarlo porque falló **por suerte** — sin el conteo previo hubiera pasado en verde midiendo una propiedad más débil que la que decía medir | `deletesDe(tabla)` cuenta llamadas a `delete`, que cubre los dos estados (sin chain = 0, con chain y sin delete = 0) |
| 2026-08-17 | **Mi arreglo del wipe cambió una mentira por la contraria.** Reordené los deletes para que el mensaje de fallo pudiera afirmar *"tus movimientos siguen ahí"*, y lo DEDUJE del orden: si se llegó a la rama de fallo, o `transacciones` no se tocó, o su delete fue el que falló. La segunda mitad es falsa. `{ error }` significa **"el cliente no pudo confirmar"**, no "el servidor no lo hizo": un 504 del edge, un socket cortado o un restart del contenedor devuelven error mientras Postgres termina el DELETE y commitea. Y con mi reordenamiento `transacciones` pasó a ser la última **y** la más cara (el trigger `trg_audit_borrado` le suma un INSERT por fila), o sea la que más probablemente se corta por arriba. En esa rama le decía a la persona que sus gastos estaban a salvo justo cuando ya no estaban | `causa-inventada-junto-al-numero` (variante: garantía deducida de un invariante que no cubre el modo de fallo real) | **revisión adversarial del ARREGLO**. Mis 4 tests nuevos estaban verdes y la prueba por mutación mataba las 5 mutaciones que diseñé | No, y no podía: el mock resuelve `{error}` sin filas borradas, así que el caso ni siquiera es expresable ahí. El error fue de razonamiento, no de cobertura | El mensaje **mide**: recuenta `transacciones` tras el fallo y ramifica en TRES estados (quedan N / quedan 0 / no se pudo contar). "No sé" dejó de ser sinónimo de "está bien". Mutación que lo mata: borrar la relectura del conteo (mueren 3 tests) |
| 2026-08-17 | **El comentario decía que el fallo quedaba en `errores` y no quedaba en ningún lado.** Justifiqué no avisar al admin de un wipe fallido con *"ya quedó en `errores`/log con la tabla que falló"*. `log.error` es pino a **stdout**; el único escritor de esa tabla es `registrarError`, y este camino nunca lo llamaba. O sea que el único rastro eran los logs de Railway — y con el orden nuevo esa rama puede haber destruido `presupuestos` y `categorias_usuario` en silencio | `causa-inventada-junto-al-numero` | **revisión adversarial del ARREGLO** | No | `registrarError('WIPE', ...)` con la tabla y el conteo. Y **dos tests nuevos**, porque sacar la llamada no mataba ni uno: la había agregado sin cobertura sobre ella misma |
| 2026-08-17 | **Puse el `delete` de `gmail_cuentas` dentro del `try` de la revocación**, así que una excepción de `revocarAccesoGmail` se llevaba puesto también el delete — que no depende de que la revocación salga bien. El aviso al admin decía "falló la revocación" sin mencionar que la fila seguía viva, así que el admin revocaba a mano en Google y dejaba la fila, que es justo lo que alimenta `emailGmailVinculado` y el `login_hint` | `camino-feliz-unico` | **revisión adversarial del ARREGLO** | No: mi test solo asertaba que el aviso salía, no sobre `gmail_cuentas` | Dos `try` separados, cada mitad se nombra en el aviso. Mutación: volver a fusionarlos mata el test |
| 2026-08-17 | **El arreglo del arreglo confundió "no había nada" con "se perdió".** Tras el hallazgo de que el mensaje deducía en vez de medir, hice que recontara `transacciones` y leí `quedan === 0` como *"se borraron igual pese al error"*. Ese cero tiene DOS causas indistinguibles, y elegía siempre la peor: a alguien con **cero gastos** que pedía la baja y se caía en `presupuestos`, le decía *"tus movimientos sí se eliminaron"* con `transacciones` sin tocar una sola vez — y encima lo mandaba a soporte diciéndole que ya no había reintento, cuando en ese estado el reintento converge solo. El desambiguador estaba calculado dos líneas arriba (`filas`, el conteo previo) y lo imprimía en el mismo `detalle` sin usarlo | `evidencia-que-no-discrimina` | **segunda revisión adversarial**, que lo reprodujo corriendo el flujo | No: mi test para esa rama fijaba `__count = 0`, y como el mock servía UN conteo para las dos llamadas, `filas` también daba 0. O sea que el escenario que el test decía cubrir era **inexpresable en el mock** y lo que ejercitaba era justo el caso donde el mensaje miente | `sePerdieron` exige las TRES (falló `transacciones` · antes había · ahora no queda). El mock ancla el conteo a la FASE (`__countDespues` tras el primer delete) en vez de a la llamada — una cola con `shift()` se desincronizaba porque el webhook consulta `transacciones` antes de llegar al wipe |
| 2026-08-17 | **"Esto SÍ va al admin" era falso por segunda vez en el mismo día.** Cambié el comentario que mentía sobre `errores` y escribí otro igual de falso al lado: `registrarError` solo notifica cuando la misma clave llega a **5 en una hora**, contra un contador en memoria que un redeploy borra. O sea que un wipe fallido aislado —el que pudo destruir `presupuestos` y `categorias_usuario` en silencio— dejaba una fila en una tabla que nadie mira y **cero** avisos, mientras el wipe EXITOSO sí empuja notificación: el evento inocuo sonaba y el destructivo era mudo | `causa-inventada-junto-al-numero` | **segunda revisión adversarial** | No | `notificarAdmin` DIRECTO en esa rama, nombrando qué se perdió. `registrarError` se queda como el rastro consultable. Mutación: apagar el aviso mata su test |
| 2026-08-17 | **El handler de SIGINT que agregué al harness era peor que el problema que arreglaba.** Lo puse para que un Ctrl-C no dejara una fila huérfana, con `process.once(sig, async () => { await limpiar(); ... })`. Eso corre **concurrente** con `main()`: el primer await cede el control. Un Ctrl-C entre el re-chequeo y el despacho de un caso destructivo borraba la fila sembrada, el handler consultaba `obtenerUltimaTransaccion` por su cuenta, le salía la transacción **REAL** más reciente del usuario QA, y la borraba por id — y `qa-guard` lo deja pasar, porque salió de un SELECT fijado a un usuario permitido. La fila huérfana que venía a evitar se auto-curaba sola en la corrida siguiente | `efecto-lateral-al-deduplicar` (variante: cleanup concurrente con el código que mide) | **segunda revisión adversarial** | No, y ningún test podía: es una carrera dentro de un harness | La señal solo iza `abortado`; `main()` corta en el borde del caso y llega a su propio `limpiar()`. Segundo Ctrl-C = salida dura sin limpiar |
| 2026-08-18 | **Mi cambio dejó ciego un guard probado por mutación, y lo hizo en VERDE.** El test del call-site 4 del muro (`muro-dispatch.test.js`, el redirect post-parser) usaba `'gasté 20 en algo, cuánto llevo este mes'`. Con el splitter nuevo ese mensaje pasa a ser COMPUESTO: `registrar_manual` se queda con la mitad de escritura, el redirect post-parser **ya no se evalúa nunca**, y la lectura sale por la continuación. El test siguió pasando —el muro igual aparece, por el otro call-site— así que nada lo delató. Medido: con el mensaje viejo, neutralizar el redirect post-parser dejaba los **16 tests en verde**, cuando antes de mi cambio lo mataba. El mismo mensaje en `transacciones.test.js` sí se puso rojo y por eso lo miré; el de `muro-dispatch` no, y es el que casi se va | `cobertura-alrededor-del-cambio` | preguntarle al diff "¿qué test muere si revierto ESTA línea?" por cada uno de los 4 call-sites, no solo por el que se puso rojo | **No.** 130 tests en verde, incluidos los 16 de ese archivo. Un guard que pierde su objeto no falla: pasa por otra razón | Los dos tests usan el mensaje **sin coma** (que el splitter no reconoce como compuesto) con el porqué escrito arriba, y se re-verificó por mutación que los **4** call-sites vuelven a morir: 7 rojos al sacar el gate entero, 2 y 2 en los redirects, 6 en la continuación |
| 2026-08-18 | **Leí como "la mutación sobrevivió" dos mutaciones que nunca se aplicaron.** Corrí la matriz con `perl -0pi -e` sobre patrones multilínea con `
`, y los archivos tienen CRLF: 2 de 6 no matchearon nada. La salida es indistinguible de una mutación real que sobrevive —la suite en verde— así que estuve a punto de escribir tests para tapar huecos que no existían (M3 sí estaba cubierto) y, en la dirección peligrosa, de dar por cubierto lo que no lo estaba. Se sumó que restauré con `git checkout --` archivos con trabajo **sin commitear**, y eso no revirtió la mutación: revirtió a HEAD y borró cuatro ediciones mías | `mutacion-que-no-se-aplico` | comparar el archivo mutado contra su respaldo antes de correr la suite | No: no había control. La matriz imprimía el conteo de tests y nada sobre si el archivo había cambiado | La matriz corre `chk()` —un `diff` contra la copia de respaldo— y **aborta el resultado** si el archivo quedó idéntico. El respaldo es una copia en scratchpad, nunca `git checkout` sobre trabajo sin commitear |
| 2026-08-18 | **Mi arreglo registraba S/1.00 donde la persona escribió S/1.50, con un ✅ encima.** El splitter nuevo cortaba el mensaje mixto en la primera coma sin mirar si era la coma DECIMAL: `"Gasté 1,50 en pan, cuánto llevo hoy"` → mitad de escritura `"Gasté 1"`. Y el corte va ANTES del parser, así que ni siquiera hacía falta que gpt-4o-mini fallara: `parsearRegistroManual` recibía `"Gasté 1"` y devolvía 1, correctamente. Medido igual con `12,90` (→S/12) y `45,50` (→S/45); `detectarMultiGasto` no intercepta ninguno. **Es estrictamente PEOR que el bug que el diff venía a arreglar**: perder el gasto es visible y la persona lo reescribe, cobrarle un tercio no lo es | `corte-por-un-separador-que-vive-dentro-del-dato` | revisión adversarial del diff | **No, y en dos niveles.** 130 tests verdes + 41/41 del E2E contra producción + 9 de 9 mutaciones muertas. El E2E no podía verlo: su caso mixto usa `" y "`, no coma | `(?<!\d),(?!\d)` en `SEP_MIXTO`, y los tres casos decimales pasaron al lado POSITIVO del test asertando el MONTO — un negativo no sirve acá, lo que hay que fijar es que llegue el monto completo. Mutación MC1 (sacar los lookarounds) mata 3 tests |
| 2026-08-18 | **El control que decía cuidar ese caso pasaba por la razón equivocada.** Mi negativo era `['Gasté 1,50 en pan', 'la coma es el decimal']` — sin pregunta al final, así que la función lo rechazaba por *no tener mitad de lectura* y la coma decimal nunca se ejercitaba. El motivo escrito al lado era falso. Lo mismo con `'Compré 3 panes, 2 leches y una gaseosa'`. Y la medición que lo respaldaba (`0 de 510 casos del pool se parten`) tampoco podía verlo: **el pool tiene CERO mensajes con coma decimal**, o sea que el cero medía una clase que no contenía el caso | `negativo-que-rechaza-por-otra-condicion` | revisión adversarial del diff | No: los 18 controles estaban verdes y uno de ellos era exactamente este caso, mal construido | `condicionesQueRechazan()` descompone la regla y los AISLANTES exigen `toHaveLength(1)`: un negativo solo cuenta si cae por UNA condición. Los sobre-determinados quedaron, pero ya no se los cita como cobertura de nada |
| 2026-08-18 | **La guarda de "un solo monto" era ciega por construcción: el corte muda los montos sobrantes al otro lado.** La apliqué sobre `parte1`, y como el separador cae ANTES del segundo monto, `"pagué 15 taxi, 40 cena, cuánto llevo hoy"` dejaba `parte1 = "pagué 15 taxi"` con un solo monto: pasaba la guarda y **registraba S/15 mientras los 40 desaparecían sin rastro**. Igual con `"Compré 3 panes, 2 leches..."` → S/3 en "panes". El docblock afirmaba que esta guarda evitaba justo eso, y solo lo evitaba cuando el corte caía DESPUÉS de los dos montos — que es como yo había construido el caso de prueba | `guarda-del-lado-equivocado-del-corte` | revisión adversarial del diff | No. Mi AISLANTE (`gasté 15 en taxi 40 en cena, cuánto llevo hoy`) no tiene separador entre los montos, así que probaba la única forma en que la guarda SÍ funcionaba | El conteo va sobre el mensaje **entero**, antes de partir. Dos AISLANTES nuevos con el separador entre los montos, y la mutación "volver a `parte1`" mata 2 tests |
| 2026-08-20 | **El guard que escribí para verificar que un `motivo` fuera CIERTO se medía contra ese mismo `motivo`, y contra los comentarios, y contra el `.select()`. Tres evasiones, las tres verdes.** El archivo exige que toda función que mande por `CANALES.SOLO_WHATSAPP` mire `supabase_auth_id` — la única forma de que la frase *"el destinatario no tiene cuenta web"* no envejezca en silencio, que es justo lo que le pasó a `checkRecordatorioOnboarding`. Buscaba la cadena en el TEXTO de la función, así que: (1) el comentario *"Si tiene supabase_auth_id, ya se logueo en webapp"* de `maybeWebappInvite` la daba por cumplida; (2) borrado el comentario, la daba por cumplida el propio `motivo`, que dice *"el trigger exige supabase_auth_id NULL"* — o sea la afirmación verificándose a sí misma; (3) borrados los dos, `checkActivacionDia2` seguía verde por su `.select('id, whatsapp, nombre, supabase_auth_id, …')`, que es una PROYECCIÓN y no dice nada sobre a quién se eligió. Con las tres puestas, borrar la guarda real dejaba el guard en verde y el aviso saliendo por un canal que entrega al 10% | `guard-que-se-mide-contra-su-documentacion` | la **prueba por mutación**, en el acto: las tres salieron de borrar la guarda de un call-site real y ver el archivo verde | Es el guard | **El parche por scrubbers no aguanto y se cambio el enfoque.** La revision adversarial encontro una CUARTA en el acto: mover la lista de columnas a una constante (`.select(COLS_NUDGE)`), que ningun borrador de literales puede ver y es un refactor mundano. Mas la prosa de un `log.info`, la clave suelta en `datos: {}` y una variable `supabase_auth_id_pendiente`. Y una estructural: `funciones()` solo veia `function` en columna 0, asi que un call-site escrito como arrow de modulo se pegaba al cuerpo de la funcion ANTERIOR y heredaba su filtro sin mover el conteo. La regla dejo de ser "¿aparece la cadena, salvo aca, aca y aca?" y paso a **lista blanca de formas que FILTRAN**: filtro de PostgREST (`.is/.eq/.neq/.not/.or/...`) o acceso a propiedad (`u.supabase_auth_id`). Un `.select()` no entra: es proyeccion, no filtro. Las 8 evasiones son fixtures, hay 2 controles positivos (sin ellos un predicado que devuelve siempre false pasaba los 8 negativos), y las 5 mutaciones sobre los call-sites reales salen rojas |
| 2026-08-20 | **Escribi "cada hora" tres veces sobre un cron que corre cada 15 minutos, y ese numero ES el argumento.** Los comentarios del dedup y del claim razonaban "si el ledger no queda, se re-avisa en la proxima corrida horaria" — o sea 3 avisos en la ventana de 3h. `cron/schedule.js:86` dice `cadaMs: 15 * MIN`: son **~12**. Y no es hipotetico: el **17-jul y el 20-jul, un usuario cada dia**, recibieron 12 `onboarding` identicos espaciados 15 minutos exactos. **OJO con esa evidencia**: sostiene la CADENCIA y la MAGNITUD, no la causa — las 12 filas estan en la tabla, o sea que el insert funciono. En julio este cron todavia no dedupeaba contra `notification_deliveries`. La conclusion ("no filtrar `skipped%`") sigue siendo correcta; lo que estaba mal era la magnitud del daño de equivocarse, que es justo lo que decide cuanto blindar el ledger | `numero-sin-medicion` | **revision adversarial** del diff, que fue a leer el scheduler en vez de creerle al comentario | No: ningun test lee prosa, y el intervalo no aparece en el archivo que lo razona | Los tres comentarios dicen 15 min y citan el incidente del 20-jul. Y lo que el numero pedia: `registrarEntrega` **lee el `{ error }` del insert** (`lib/whatsapp.js`), que era la unica escritura del ledger sin verificar — su `try/catch` estaba muerto porque supabase-js no lanza. Con `tests/lib/registrar-entrega-error.test.js`, mutacion probada |
| 2026-08-20 | **Medi "9 destinatarios historicos, ninguno con cuenta web" sobre el ledger equivocado, y con eso justifique el fix.** `maybeWakeUpOnboarding` registra en `survey_events`; yo consulte `notification_deliveries`, que es mas nueva, o sea un SUBCONJUNTO — y lo lei como si fuera la poblacion. Medido bien: **25 destinatarios, 3 con `supabase_auth_id`**. La premisa que yo daba por cumplida-en-los-hechos era falsa en los hechos tambien | `conclusion-mas-ancha-que-la-medicion` | **revision adversarial**, que busco el ledger del trigger en vez de reusar mi consulta | No | La medicion correcta esta en el comentario, con la tabla que la sostiene nombrada. Antes de escribir "medido sobre N", verificar que N sea la poblacion y no la tabla que tenia a mano |
| 2026-08-20 | **El mismo commit arreglaba un aviso que no llegaba bifurcando el canal, y arreglaba el otro CALLANDOLO.** A `maybeWakeUpOnboarding` le puse `if (usuario.supabase_auth_id) return false`. Pero el runner ya descarta a quien no tiene WhatsApp, asi que esa guarda no protegia a ningun web-first: **solo excluia a quien tiene las dos cosas**, o sea justo a quien SI tiene campana donde mostrarle el aviso. Es el mismo defecto de origen que el diff venia a cerrar, resuelto en la direccion contraria, y en el archivo de al lado | `arreglo-en-la-direccion-contraria` | **revision adversarial** | No: el guard de clase que escribi lo daba por CUMPLIDO — silenciar satisface "no manda por SOLO_WHATSAPP a alguien con cuenta web" igual de bien que bifurcar | Bifurca como `checkRecordatorioOnboarding`: AMBOS con cuenta web, SOLO_WHATSAPP sin ella. La leccion que el guard no puede dar: satisfacer el invariante no es lo mismo que arreglar el problema |
| 2026-08-20 | **Pegue un numero real a una causa inventada, que es la trampa que este mismo archivo nombra.** Al arreglar el `{ error }` descartado de `registrarEntrega` escribi, en tres lugares, que "eso YA PASO: el 20-jul dos usuarios recibieron 12 WhatsApps identicos". Consultado en prod: fueron **dos dias distintos con un usuario cada uno** (17-jul y 20-jul), y sobre todo **las 12 filas ESTAN en la tabla** — el insert no fallo. La causa real fue que en julio ese cron no dedupeaba contra `notification_deliveries` (ese dedup llego el 17-ago con `000fc52`); se marcaba pisando `onboarding_paso`, y la query seleccionaba esos mismos pasos. Dos causas distintas con el mismo desenlace, y yo use la evidencia de una para acreditar la otra. Lo que el dato SI sostiene —la cadencia de 15 minutos y el ~12— es correcto y era todo lo que hacia falta | `causa-inventada-junto-al-numero` | **segunda revision adversarial**, la del ARREGLO | No: ningun test lee prosa, y el numero era verdadero | Los tres lugares dicen que la evidencia sostiene cadencia y magnitud, y nombran la causa real de julio. La regla: antes de escribir "esto ya paso", verificar que el rastro sea del MISMO mecanismo, no del mismo sintoma |
| 2026-08-20 | **"Hoy nadie lo tiene en false", y habia 3.** Al agregarle `recordatorios_activos` al nudge escribi ese numero sin medirlo. Medido: 3 filas en `false` — 2 lapidas de cuentas borradas (la migracion 073 lo pone asi) y 1 `is_test_user`. Usuarios reales vivos: cero, asi que la CONCLUSION ("esto no cambia a quien le llega") se sostiene; el enunciado, no | `numero-sin-medicion` | **segunda revision adversarial** | No | El comentario da el desglose y dice por que las 3 ya estaban fuera por otra condicion (`.neq(is_test_user)` y `.is(cuenta_borrada_at, null)`) |
| 2026-08-20 | **Corregi el cuerpo in-app en un archivo y no en el otro, en el mismo commit.** `checkRecordatorioOnboarding` recibio un `cuerpo:` propio con su comentario explicando que el copy de WhatsApp no es accionable en la campana. La rama `AMBOS` que agregue a `maybeWakeUpOnboarding` **no lo lleva**, asi que el chokepoint deriva el cuerpo del mensaje de WhatsApp — que pide escribir el nombre por chat y ofrece `/silenciar`, ninguna de las dos posible en la webapp | `arreglo-aplicado-a-una-de-dos-copias` | **segunda revision adversarial** | No | `cuerpo:` propio, con el porque escrito al lado |
| 2026-08-20 | **El mismo `{ error }` descartado seguia vivo 240 lineas mas abajo del que arregle, en el mismo archivo y sobre la misma tabla.** `procesarStatuses` hace `update(...).select(...)` y solo lee `data`; con error, `data` viene null y cae en la rama que lo llama *"status sin fila de notificacion (mensaje conversacional)"*, a nivel **debug**. O sea que un UPDATE rechazado se leia como "esto no era un aviso". Es peor que el caso que arregle, porque tiene una explicacion benigna esperandolo. Lo que se pierde en silencio: `delivered_at`/`failed_at` —la fuente de verdad de entrega— y la llamada a `avisarVeredictoD10`, unico camino del veredicto del experimento BSUID | `error-no-leido` | **segunda revision adversarial**, que fue a mirar el resto del archivo en vez de solo el diff | No | Rama propia que NIEGA explicitamente la lectura benigna en el mensaje, con test y mutacion. Y la leccion de alcance: al arreglar un error descartado, barrer el archivo entero — la clase rara vez viene sola |
| 2026-08-20 | **Al ensanchar la ventana del nudge subi `ventanaMaxMs` de 3h a 15h y con eso DESARME el unico guard escrito para ese cron**, argumentando en el commit que dejarlo en 3 "habria sido una declaracion falsa". `ventanaEfectivaMin` hacia `min(gate, movil)`: con 3h daba 3h y rechazaba cualquier periodo mayor; con 15h da 12h. Medido por el revisor: subir `cadaMs` a 4h deja **1597 tests en verde** y 298 minutos de hora-de-alta por dia sin cobertura. Y hay algo peor que descubri arreglandolo — **con la config VIEJA ese `min` tampoco podia ver el bug original**: daba 180 min, el periodo de 15 min pasaba comodo, y el 50.9% del padron no recibia nada. El guard era estructuralmente incapaz de ver el fallo para el que existia | `formula-del-guard-mal-modelada` | **revision adversarial**, con un barrido de los 1440 minutos de alta | Es el guard | El ancho que manda para una ventana movil que cruza un gate diario NO es `min(abierto, movil)`: es `min(abierto, movil − cerrado)`, y si `movil < cerrado` hay usuarios que maduran y expiran de madrugada — eso ahora es un fallo APARTE, con su propio test. La formula reproduce el barrido (3h → 0 min sin cobertura, 4h → 298) y la config vieja sale marcada como imposible. `ventanaMaxMs` se queda en 15h: el campo declara el ancho CRUDO, y lo que estaba mal era como se combinaba |
| 2026-08-20 | **Escribi "la espera maxima es de 15h" derivando el gate como si fuera continuo, en el mismo parrafo que invoca el tick de 15 minutos.** El peor caso no es el alta de las 18:00 sino la de las **17:46**: madura 20:46, le quedan 14 minutos de gate y el tick es cada 15, asi que puede no caer ahi y esperar a las 09:00 — 15h14m. No rompe nada (sigue bajo el techo de 18h), pero es el mismo numero mal derivado dos veces en una sesion | `conclusion-mas-ancha-que-la-medicion` | **revision adversarial** | No: ningun test fijaba el peor caso | El docblock da 15h14m y explica por que; hay un test con el alta de las 17:46. Y otro que exige MARGEN sobre ese borde, porque el test del rescate caia justo en 15h con `.gte` inclusivo: un techo de 15, 16 o 17 pasaba el archivo entero |
| 2026-08-20 | **La validacion que agregue al mock para atrapar fixtures rotos era INERTE: tiraba desde dentro del `try/catch` del cron.** El predicado del filtro corre dentro del `await` que el cron envuelve, asi que el `throw` lo comia `log.error` y el test veia "no se notifico a nadie" — o sea PASA. Comprobado: con el fixture sin `created_at`, la version que tiraba dejaba los 23 tests en verde. Es exactamente `feedback_await_que_resuelve_no_prueba_exito`, aplicado a mi propio instrumento | `guard-tragado-por-el-codigo-que-vigila` | la contraprueba, cuando fui a verificar que el mock endurecido gritara | Es el guard | El mock ANOTA en un array de modulo y el veredicto lo da un `afterEach`, que corre fuera de ese catch. Probado con las dos formas reales: `created_at` ausente, y el formato con espacio que devuelve la columna (`timestamp without time zone`), que es justo el que sale de copiar una fila de produccion |

| 2026-08-22 | **Escribí en el docblock del guard que `spending-alerts.js` "tenía una lectura muda y no estaba en ninguna de las dos listas", y era falso — lo dijo un instrumento que yo YA SABÍA que estaba roto.** El dato salió de correr el parser viejo sobre los nueve servicios, en la misma sesión en que estaba arreglando ese parser porque reportaba líneas correctas como mudas. `spending-alerts.js:236` es exactamente ese falso positivo: `obtenerHistorialAlertas(usuarioId, limit = 20)` tiene un parámetro por defecto, el backtrack se quedaba con el `= 20` y devolvía la firma como LHS. La línea lee su error desde antes de esta sesión. Y el hallazgo servía de justificación al cambio más grande del commit (derivar la lista de archivos en vez de declararla), o sea que era el dato que sostenía el argumento | `numero-sin-medicion` | yo, al re-correr el barrido con el parser ya arreglado antes de fijar los asserts | No: ningún test lee prosa, y el número venía de un script real | El docblock dice lo que se midió — los cuatro servicios que faltaban estaban limpios — y la conclusión se sostiene en el argumento estructural, no en un hallazgo. La regla: un instrumento que estás arreglando no acredita nada hasta re-correrlo |
| 2026-08-22 | **Dos comentarios nuevos remitían a `registrarAbonoDeuda`, una función que no existe** (se llama `abonarDeuda`). Los escribí en `services/debts.js` y `services/metas.js` para explicar que el bug era el mismo en los dos lugares, o sea que el comentario existe justamente para que alguien vaya a mirar el otro sitio — y el nombre lo manda a un `grep` vacío | `nombre-que-no-resuelve` (nueva) | el test funcional, al llamar `debts.registrarAbonoDeuda` y recibir `is not a function` | No | Los dos dicen `abonarDeuda`. Lo barato: los comentarios que remiten a otro símbolo se verifican con un `grep` del nombre, igual que un import |
| 2026-08-22 | **Mi prueba por mutación no veía una guarda que yo había escrito en el mismo commit, y eso se lee IGUAL que cobertura.** El regex del mutador aceptaba una variable suelta (`if (err)`), así que `if (errSpace \|\| errMiembros)` —la guarda de población de `shared-spaces.js`, la que decide si alguien se entera de que le movieron el reparto— nunca se mutó. No apareció como superviviente: no apareció. Es la hermana de `mutacion-que-no-se-aplico`: allá el patrón no matchea el archivo y acá no matchea la guarda, y las dos salidas son un verde sin nadie mirando | `mis-guards-tambien-fallan` | leer la lista de supervivientes y notar que una guarda mía no estaba **en ninguna de las dos columnas** | Es el guard | El regex acepta disyunciones, y el docblock dice por qué. La regla de lectura: en un reporte de mutación, lo que falta es tan informativo como lo que sobrevive — hay que cuadrar el total contra el diff |
| 2026-08-22 | **Los 30 tests funcionales que escribí pasaban en verde con 10 de mis 39 arreglos revertidos.** Tres causas, y ninguna es distracción: (1) escribí **un caso por FORMA y no por sitio** — los cuatro dedups de `reminder_dN` son cuatro `if` distintos y el fixture era un usuario de día 3, así que los de día 7, 14 y 30 no se corrían nunca; (2) para las accesorias afirmé `resolves` y nada más, y eso vale igual con el `log.error` borrado — la mutación no cambia el valor de retorno, así que la mitad "y además lo dice" estaba sin probar; (3) **importé cuatro servicios de cinco**: `shared-spaces.js` no aparecía en el archivo, así que sus tres arreglos no tenían quién los mirara | `cobertura-alrededor-del-cambio` | `scripts/mutar-lecturas-error.mjs` | No: 30 tests verdes, y la suite entera también | Los 16 casos del bloque 6, uno por SITIO. Y el script queda en el repo: cuadra cada guarda del perímetro contra los tests, y **aborta si la mutación no cambió el archivo** |
| 2026-08-22 | **El control negativo de las reglas de reparto pasaba por vacuidad: mi fixture no movía la parte de nadie.** Sembré `split_rules: [{ category: 'Comida', weights: {} }]`, pero el motor lee `splits`, no `weights`. Con la clave equivocada la regla no nombra a ningún miembro, el reparto cae al de por defecto, y `effectiveSplitPercentsFor` da el mismo porcentaje antes y después — el código corta con `if (a === d) continue`, no arma ninguna línea y no notifica **ni con el arreglo ni sin él**. El test decía "no se anuncia un reparto inventado" y lo que probaba era que un espacio sin cambios no manda avisos | `negativo-que-rechaza-por-otra-condicion` | la mutación, que dejó ese arreglo vivo con el test en verde | No | Fixture con `splits` que sí mueve 90/10 → 10/90, y un CONTROL POSITIVO al lado: sin él, cualquier fixture que no notifique se lee como cobertura |

| 2026-08-22 | **El guard derivaba su perímetro siguiendo UN salto, así que el hermano exacto de lo que este commit arreglaba quedaba afuera.** Cambié la lista escrita a mano por una derivada de los `require` de `cron/checks.js` y lo celebré en el docblock; el revisor midió el salto siguiente. `generarResumenSemanal` vive en `summaries.js` —dentro del perímetro, y sus dos hermanas del mismo archivo se arreglaron en este commit— pero delega en `obtenerGastosSemana` (`services/transactions.js`) y `obtenerPresupuestosMes` (`services/budget.js`), **las dos descartando su `{ error }`**. Con la tabla caída, el resumen del domingo no sale y no queda una línea; y el usuario que pregunta por su semana recibe "no registraste gastos", que es falso. O sea: mover una query un archivo más adentro la sacaba de jurisdicción, que es exactamente el refactor que creó este trabajo | `perimetro-de-un-salto` (nueva) | **revisión adversarial**, siguiendo los requires en vez de leer el docblock | No: el guard estaba verde y su propio docblock argumentaba que la derivación cerraba la clase | La derivación es **transitiva** (17 archivos, no 9). Las dos lecturas del resumen semanal se arreglaron; lo que entró y no se barrió queda en `PENDIENTES`, un trinquete que no deja SUBIR el conteo y exige sacar la entrada cuando llega a cero |
| 2026-08-22 | **Convertí "esa fila no existe" en excepción, y el mock no podía mostrarlo.** `.single()` con cero filas devuelve `PGRST116` —está en los gotchas del propio proyecto— y mi mock devolvía `{ data: null, error: null }`. Con eso, el `if (error) throw error` que le puse a `ajustarDinamico` y a la lectura del espacio en `avisarAMiembros` deja MUERTO el `if (!fila)` de la línea siguiente: una meta borrada pasa a ser una excepción, y un espacio borrado pasa a emitir `log.error` sobre un caso legítimo. Los tests no podían verlo porque el mock nunca producía el caso | `el-mock-no-implementa-lo-que-el-bug-usa` | **revisión adversarial**, leyendo el mock contra la semántica real de PostgREST | No: 46 tests verdes, incluido uno que afirmaba el `rejects` de `ajustarDinamico` | El mock emite `PGRST116` con cero filas; los dos sitios exceptúan ese código; y hay cuatro casos nuevos — la fila inexistente devuelve null, el fallo real sí corta, y el espacio borrado no loguea |
| 2026-08-22 | **Al tapar un agujero del guard abrí el opuesto, y el caso original quedaba cubierto igual.** Para que `const [{data,error}, {data:b}] = await Promise.all([q1,q2])` no pasara entero gracias al error de la primera, exigí el error de TODOS los elementos del patrón. Con eso, un `Promise.all` que mezcla una query con cualquier otra promesa —`[{ data, error }, otros]`— salía reportado como MUDO: el guard en rojo sobre código correcto, que es la dirección que el propio archivo dice que enseña a ignorarlo | `correccion-que-abre-la-direccion-opuesta` | **revisión adversarial**, ejecutando la forma contra el parser | No: mi fixture del `Promise.all` tenía dos queries, o sea la única forma en que el `every()` era correcto | El índice: cada query calcula su POSICIÓN dentro del `Promise.all` y responde por su propio elemento. Los dos casos son fixtures y las dos direcciones están fijadas |
| 2026-08-22 | **Mi guard preguntaba si la cadena `error` aparecía en el TEXTO del patrón, no si `error` era una clave.** Cuatro evasiones, las cuatro ejecutadas contra el parser y las cuatro en verde: una cadena literal `etiqueta = 'error, '`, un rest `...error` (que deja el error en `error.error`), un `meta: { error } = {}` anidado bajo una clave que la respuesta no tiene, y —sin arreglo posible— un `error: _ignorado` que nadie usa. El fixture `errorPrevio` que ya estaba cubría "una VARIABLE cuyo nombre contiene error", que es otra cosa | `mis-guards-tambien-fallan` | **revisión adversarial** | Es el guard | Se blanquean las cadenas, se parte el patrón en elementos de primer nivel y se compara la CLAVE de cada uno. Las tres primeras son fixtures; la cuarta va escrita como el límite real, porque distinguirla pide análisis de uso |
| 2026-08-22 | **Apliqué "tira, que el catch de abajo ya loguea" sin ir a mirar si ese catch loguea.** Es la rama de la taxonomía para una decisión por usuario, y en `verificarRachaAportes` el único call-site de producción (`handlers/intents/metas.js`) la envuelve en un `catch(e) { /* silent */ }`. O sea que el `throw` compraba exactamente el mismo silencio que el `return 0` que reemplazaba, con más ceremonia. Lo mismo pasó en dos comentarios de `metas.js`, que describían un síntoma que esas funciones no producen: el "todavía no tienes metas de ahorro" sale de una query inline del handler que sigue descartando su error, y `obtenerLogros` directamente no tiene call-site | `regla-aplicada-sin-verificar-su-premisa` (nueva) | **revisión adversarial**, yendo a los call-sites | No | `verificarRachaAportes` loguea ANTES de tirar, con el porqué escrito al lado. Los dos comentarios dicen qué alcanza cada arreglo y cuál es el consumidor real |
| 2026-08-22 | **El mutador podía reportar "ninguna sobrevivió" con la suite rota, y su lista de archivos era una lista a mano al lado de una derivada.** Tres cosas: (1) `corre()` lee cualquier exit distinto de cero como "la mutación murió", así que con los tests rojos por un motivo ajeno las ~90 mutaciones salían todas muertas y el script terminaba en exit 0 — el mismo verde falso que su propio docblock combate un nivel más arriba; (2) declaraba 6 archivos mientras el guard derivaba 17, dejando sin mutar justo los que ese docblock celebra haber incorporado; (3) escribe el árbol vivo sin handler de `SIGINT`, así que un Ctrl-C deja un `if (false)` —la forma exacta del bug original— en el working tree. El revisor lo vio en dos snapshots durante su propia corrida | `mis-guards-tambien-fallan` | **revisión adversarial** | Es el guard | Baseline verde obligatorio antes del loop, perímetro derivado con el mismo recorrido que el guard (reimplementado, no importado: dos mecanismos), y `SIGINT`/`SIGTERM` restauran el archivo en vuelo |

| 2026-08-23 | **El `catch` que escribí para no perder un pago podía perderlo entero.** El claim nuevo de `handlers/webhook.js` lee `eClaim.message` a secas: si el rechazo no es un `Error`, ese `catch` tira `TypeError` ADENTRO y se lleva puesto todo lo que venía después — `guardarTransaccion` (el gasto), `notificarAdmin` (el aviso) y la respuesta al usuario—, dejando solo el "no pude procesar la imagen" del catch de afuera. Sobre una captura de PAGO es el peor desenlace del archivo. Y es la misma clase que yo acababa de arreglar y documentar en los ocho catches de `cron/checks.js` del mismo commit: escribí el docblock que la explica y la reintroduje treinta líneas más allá, en mi propio código nuevo. El hermano del canal silencioso sí lo tenía bien | `regla-escrita-y-no-aplicada-a-mi-propio-diff` (nueva) | **segunda revisión adversarial**, sobre el arreglo de la primera | No: 1835 tests verdes | `msgErr` sale de `cron/checks.js` a `lib/error-monitor.js` y se usa en los cuatro catches nuevos del webhook. Caso propio en `webhook-imagen.test.js` con un rechazo `null`, y el envoltorio se instala ANTES del `require` (webhook DESESTRUCTURA sus imports: reasignar la propiedad después no cambia nada, y el test habría pasado sin ejercitar la rama) |
| 2026-08-23 | **Un `UPDATE` sin `.eq('id', ...)` pasaba los 1835 tests, en las dos funciones nuevas del claim.** Quitarle el selector de fila a `reclamarSolicitudPro` marca `pago_pendiente=true` a TODO el padrón (y después el `maybeSingle` revienta por múltiples filas: a cada uno que mande su captura se le contesta "reenvíamela" mientras nadie puede pagar); quitárselo a `liberarSolicitudPro` apaga el pendiente de todos y los borra del panel. La causa estructural es la misma en los dos harness: **los almacenes falsos tienen UNA fila, y un almacén de una fila no distingue un WHERE que apunta a esa fila de uno que apunta a todas** | `fixture-de-una-fila-no-ve-el-selector` (nueva) | **segunda revisión adversarial**, midiéndolo con dos mutaciones | No | Los dos UPDATE afirman su `['eq','id',...]` en `pro-payment-fallos.test.js`, contra el registro de métodos que el mock ya guardaba y que ningún caso miraba |
| 2026-08-23 | **`liberarSolicitudPro` soltaba el claim sin comprobar que no hubiera solicitud.** Llegar sin `pagoId` no prueba que no haya fila: el INSERT pudo commitear y perderse la respuesta, y la webapp —que guarda contra `pagos` y no contra `pago_pendiente`— pudo abrir la suya durante la subida a Storage más el multipart a Telegram. En los dos casos se apaga el badge de una solicitud REAL: desaparece del panel (que filtra por `pago_pendiente`) y la próxima captura vuelve a ganar el claim, o sea la carrera que la función existe para cerrar. Era la única escritura del flujo de pago sin ninguna condición sobre el estado que deshace, al lado de un `reclamar` que sí la tiene | `compensacion-incondicional` (nueva) | **segunda revisión adversarial** | No | Lee `pagos` antes de soltar; con una pendiente NO suelta y avisa; y una lectura caída tampoco suelta (no saber no es "no hay"). Tres casos + el control de que sí suelta cuando de verdad no quedó nada |
| 2026-08-23 | **Dos casos de mi propio archivo de tests estaban verdes por el fixture, no por el código.** En el del catch interno de espacios, el "otro miembro sí recibe" lo cumplía el aviso de OTRO espacio (el mismo usuario estaba en los dos), así que un `break` que se lleva a todos los miembros que faltaban del espacio sobrevivía. En el de conversiones, `evaluados` es `eventos.length` pase lo que pase y el segundo evento no producía ninguna señal, así que cortar el barrido tras el primer fallo daba el mismo resultado observable. Los dos casos declaraban en el docblock que afirmaban la continuidad del loop | `asercion-que-otro-camino-satisface` (nueva) | **primera revisión adversarial**, con un `break` por caso | No: los 11 tests verdes, y la mutación de los arreglos también | Un miembro que existe SOLO en el espacio que falla, y una transacción sembrada que hace `marcados === 1`. Las dos mutaciones (`break`) están en la corrida de mutación |
| 2026-08-23 | **El docblock afirmaba una cobertura que no medí.** Escribí que "revertir `msgErr(e)` a `e.message` en UNO solo tiene que morir" después de aplicarlo a los 41 catches del archivo y cubrir siete. El revisor revirtió cinco de una vez —incluidos el downgrade de premium vencido y la bajada al muro, donde saltarse a los que faltan no es un aviso menos— y la suite quedó verde | `causa-inventada-junto-al-numero` | **segunda revisión adversarial** | No | Los dos loops de PLAN entraron a la tabla (9 sitios cubiertos), y el docblock dice el número real y qué queda afuera |
| 2026-08-23 | **El aviso al admin de la rama nueva no tenía dedup, al revés que su hermana de tres líneas más abajo.** Un claim falla cuando PostgREST está mal, o sea de forma CORRELACIONADA: cada captura que entrara durante la caída disparaba otro Telegram, y la alarma que avisa de un pago perdido se vuelve ruido justo cuando importa. Y no decía qué mirar: el UPDATE pudo commitear antes de perderse la respuesta, y si `pago_pendiente` quedó en true sin fila en `pagos` no hay NINGUNA ruta que lo limpie sola (`rechazarSolicitudPro` exige una fila pendiente, `/pago` busca por número y este usuario no tiene) | `misma-forma-no-es-mismo-arreglo` | primera y segunda revisión adversarial | No | `avisarUnaVez` con clave por monto+comercio, y las dos ramas dicen explícitamente que hay que revisar `usuarios.pago_pendiente` de ese id |

## Defectos ajenos que enseñan lo mismo (no cuentan como míos, pero la clase sí)

| Fecha | Defecto | Clase | Nota |
|---|---|---|---|
| 2026-08-04 | `merge_and_link` fusionaba `plan` pero descartaba `trial_estado/inicio/vence`: el camino feliz del funnel creaba Pro indefinidos invisibles a todos los crons | `consumidor-no-actualizado` | migración 059. **Sin control mecánico todavía**: nada obliga a que un `usuarios` con columna nueva de estado actualice el merge. Candidato a guard. |
| 2026-08-04 | `qa-guard` no reconocía las columnas de dueño de `referidos`, así que bloqueaba la tabla entera y los referidos quedaban intestables | `consumidor-no-actualizado` | La barrera se leía como "esto no se puede testear". Arreglado agregando `referrer_id`/`referido_id`. |
| 2026-08-12 | **Un trigger de Postgres sin espejo local capitaliza `transacciones.subcategoria`, y el código compara el centinela en minúscula.** `trg_normalize_subcategoria` existe desde el 2026-03-24 (dos migraciones remotas, `normalize_subcategoria_trigger` + `capitalize_subcategoria_trigger` 30 minutos después) y **no tenía archivo en `app/migrations/`**: quien leyera ese directorio no podía saber que la DB reescribe una columna que el código escribe. El código guarda `'sin_categoria'` y `'null'`; prod devuelve `'Sin_categoria'` y `'Null'` — **503 de 2234 filas (22.5%), y CERO en minúscula**. Con eso, los 16 `sub !== 'sin_categoria'` de la webapp y los 9 del backend no aciertan **nunca**: el centinela no se filtra y se pinta como una subcategoría de verdad. En WhatsApp era peor que cosmético, porque `guardarTransaccion` devuelve la fila con `.select()` (o sea post-trigger) y la confirmación de todo gasto sin clasificar decía `✅ S/20 en Otros > Sin_categoria` | `el-destino-reescribio-el-valor` | Salió de rebote al cerrar el cascade de rename (fila anterior). **Ningún control podía verlo**: no hay error, no hay log, y una comparación que nunca acierta se lee igual que un dato que nunca aparece. Migración **070** declara el trigger; el fix vive en `lib/subcategoria.js` / `webapp/src/lib/subcategoria.ts` (un solo dueño del centinela, comparación en minúscula) y lo sostienen **dos** guards —`webapp/src/lib/subcategoria-callsites.test.ts` y `tests/subcategoria-centinela.test.js`—, uno por árbol, porque el mismo defecto vivía en los dos canales (`barrido-de-un-solo-arbol`). 7 mutaciones, todas muertas, con corrida de control en verde. **Lo que NO se hizo, y es la parte que enseña: no se dropeó el trigger ni se corrieron los datos a minúscula.** Un `UPDATE` masivo lo volvería a capitalizar en la misma sentencia —saldría "exitoso" y la fila quedaría igual—, y sacar el trigger reabre el problema que B28/B30 cerraron un nivel arriba: la misma sub viva en dos grafías. La representación canónica de la DB es la del trigger; la del código es la minúscula, y el puente es el predicado |
| 2026-08-13 | **`validarMonto` devolvía 0 — el valor que su propio contrato dice que rechaza.** El chequeo de signo corría ANTES del redondeo, así que todo lo que caía en `(0, 0.005)` pasaba el `> 0` y salía como `0`: `validarMonto(0.001) === 0`. Vivía igual en las DOS copias (`lib/validators.js` y `webapp/src/lib/money.ts`) desde que existen. Dos consecuencias medidas: `POST /api/transactions {"monto": 0.001}` insertaba una transacción de **S/0**; y en deudas el MISMO valor daba DOS respuestas distintas —el handler lo aceptaba como 0 y el chokepoint lo rechazaba lanzando—, así que al usuario le salía *"Ups, algo falló al registrar la deuda"*, una caída de backend por un monto inválido | `orden-de-operaciones-que-invalida-el-contrato` | **segunda revisión adversarial** del diff, mientras miraba el `permitirCero` que este trabajo agregaba | **No, y `qa-money-edge` estructuralmente no podía**: afirma `0 → 400`, pero prueba el **literal** `0`. Con `0.001` el harness pasa por al lado. Un guard que prueba el valor frontera y no el que redondea hasta él no ve esta clase | redondear primero y decidir el signo después, en las dos copias, más la normalización de `-0` (que `Math.round` produce para `-0.001` y no es `0` para `Object.is`). `tests/lib/validators-redondeo.test.js` + el bloque espejo en `webapp/src/lib/spaces-budgets.test.ts`, con el caso que lo delata: **el mismo valor tiene que dar la misma respuesta en el handler y en el chokepoint** |
| 2026-08-08 | El slash command `/deploy` verificaba los tres deploys **pidiéndoles que respondieran**: `api.neto.pe/health` da `ok:true` en el backend VIEJO igual, y `app.neto.pe` carga igual, así que el paso 3 no distinguía "desplegado" de `SKIPPED` por `watchPatterns`, de frenado en `WAITING` por el gate, ni de un build fallido. Toda la familia `backend-deploy-{fresh,tested,gated}` existe porque esa comprobación no separa nada, y el comando no la nombraba. Aparte, decía "NO hacer push automaticamente" contra la convención del proyecto | `asercion-vacua-por-implicacion` | Encontrado de rebote: buscaba un `.md` **anidado** para usar de sonda en el deploy de control y el archivo elegido resultó estar mal. Reescrito en `00dd65d` — el mismo commit que sirvió de control, así que el experimento no costó un commit desechable. **La clase es la del repo entero**: un chequeo que pasa en los dos mundos es verde por vacuidad, igual que las observaciones de `.md` de raíz que no separaban el ancla de un patrón recursivo |
| 2026-08-13 | **El harness que aseveraba el silencio del camino sin `from` estaba ROJO desde el 09-ago y nadie lo miró.** `qa-e2e/qa-bsuid-media.mjs` contaba el TOTAL de mensajes salientes, y ese mismo día se agregó `avisarPrimeraVezSilencioso` (el detector que avisa cuando un usuario conocido cae al camino silencioso): llama a `notificarAdmin`, que sin token de Telegram cae en `enviarWhatsapp(ADMIN_NUMBER)` y aparece en el mismo array. O sea que el único guard E2E de "a esta persona NO se le responde" llevaba 4 días fallando por una razón benigna, y un `enviarWhatsapp` de verdad metido en ese camino habría sido indistinguible del ruido | `consumidor-no-actualizado` | Correrlo. Es manual post-deploy, no canary — por eso pasaron 4 días. **El mismo hoyo ya se había tapado en `tests/handlers/webhook-sin-from.test.js`** (tiene el comentario explicando el fallback a WhatsApp) y no se propagó al E2E: un arreglo por archivo en vez de por clase | La aseveración pasa a ser sobre el DESTINATARIO (`to === WHATSAPP_QA`), más un segundo check de que nada salió hacia un tercero — sin ese, filtrar por destinatario compraría el verde tapando cualquier envío. `ADMIN_NUMBER` se importa de `lib/config.js`, no se copia. Probado por mutación: un `enviarWhatsapp(conocido.whatsapp, ...)` en el bloque `if (!from)` lo pone rojo (12/12 → 11/12, exit 1) |
| 2026-08-14 | **Saqué el `<Toaster>` de sonner del root layout para aligerar `/login` (P′8) y con eso maté los toasts que se disparan justo antes de navegar.** Sonner no re-emite: el `<Toaster>` se suscribe al observer cuando se monta y arranca con la lista vacía, así que si el árbol que lo contiene se desmonta con la navegación el toast no se ve en NINGUNA pantalla. Los dos casos reales son el `toast.success('Sesión cerrada')` de `user-menu` (que empuja a `/`, otro árbol) y el `'Cuenta verificada'` del onboarding (que puede aterrizar en `/join/*`, un árbol que no tenía Toaster de ningún tipo). Yo había barrido los 21 call-sites de `toast()` y los 21 quedaban debajo de un Toaster — el barrido era correcto y la pregunta estaba mal | `movido-a-donde-no-sobrevive` | **revisión adversarial** del diff, leyendo el fuente de sonner (`dist/index.mjs:876-878`) en vez de creerle a mi barrido | **No, y ningún guard de los que tengo podía**: `error-state-callsites` mira páginas, no ciclos de vida, y no hay test de componentes en la webapp (vitest corre en `node`, sin jsdom) | Medí el precio en vez de discutirlo: sonner cuesta **9.2 KB gzip** en el root layout contra los 51.5 KB de `@base-ui`, así que vuelve arriba y `TooltipProvider` se queda abajo. El motivo (y la prohibición de volver a intentarlo) vive pegado al componente en `components/shared/app-toaster.tsx`, no en un commit |
| 2026-08-14 | **El fix de F8 dejó cuatro tarjetas del panel admin imprimiendo `0` cuando la lectura de usuarios falla.** Gateé el bloque de KPIs con `statsFallo` —la señal de `/api/admin/stats`— pero cuatro tarjetas de ADENTRO se alimentan de `users`. Si `/api/admin/users` se cae y `/stats` responde, el bloque se pinta con "Total Usuarios: 0", "0 txs total" y "Canales Webapp: 0". El `ErrorState` de usuarios que agregué sólo se renderiza `if (tab === 'users')`, así que desde las pestañas NLP o Tickets el fallo es **completamente invisible** mientras la pantalla afirma que no hay nadie. Es exactamente la clase que el fix decía cerrar. Igual, dos líneas fuera del ternario: `"0 de 0 errores NLP reales"` impreso justo encima del ErrorState que dice que la lectura falló | `gate-de-la-query-equivocada` | **revisión adversarial** del diff. Mi propia verificación estaba verde: 291 tests, `tsc`, y el guard nuevo que yo mismo había probado por mutación **cuatro veces** | Sí y no lo atrapó: el guard que extendí a `/admin` exige que las cuatro queries nombren `isError`, y las cuatro lo nombran. Mide TEXTO, no a dónde llega la señal — la misma lección de `mis-guards-tambien-fallan` | Los agregados derivados de `users` pasan por `numeroKpi()`, que imprime `—` cuando `usersFallo`; `totalPro` es `null` (no `0`) en ese caso, porque `null` es lo que dice "no lo sé". Los contadores de las pestañas y el de NLP entraron al mismo tratamiento |
| 2026-08-14 | **Al mover el rebote de `/` de `app/page.tsx` al middleware, en demo mode el magic link dejó de volver.** El corto de `NEXT_PUBLIC_DEMO_MODE` estaba arriba de todo, así que en demo `/` seguía cayendo a `page.tsx` — que yo había reducido a un `redirect('/login')` incondicional y ya no conservaba el forward de `code`/`token_hash` a `/auth/callback`. El comentario que escribí en `page.tsx` declaraba a ese archivo la red de seguridad del caso, y la red conservaba justo la rama que no importaba | `movido-a-donde-no-sobrevive` | **revisión adversarial** del diff. Mis 5 tests del middleware pasaban: ninguno tocaba demo mode, y el único `/` que probé a mano en el navegador corrió **con demo mode activo**, o sea contra el fallback y no contra el código que había escrito | No | El corto de demo baja DEBAJO del bloque de `/`, con el argumento escrito: ese corto existe para saltear chequeos de AUTH y este rebote no es uno. Test nuevo que fuerza `NEXT_PUBLIC_DEMO_MODE=true` y exige el forward; probado moviendo el corto de vuelta arriba (1 rojo de 8) |
| 2026-08-14 | Monté `TooltipProvider` (@base-ui) en el layout de `/admin` con el comentario *"el panel usa tooltips en los KPIs"*. **Ninguna página de `/admin` importa `@/components/ui/tooltip`**: los cuatro `Tooltip` que aparecen ahí vienen de `recharts`, que trae el suyo. O sea que saqué el bundle de `/login` (correcto) y lo volví a meter en `/admin` para cero tooltips, con una justificación que nunca medí | `justificacion-que-no-se-midio` | **revisión adversarial**, mirando de qué módulo sale cada `Tooltip` en vez de grepear la palabra | No | Fuera del layout de admin, con el porqué escrito ahí mismo para que el próximo que vea los `Tooltip` de recharts no lo vuelva a agregar |
| 2026-08-14 | **El fix de B23 le cerró al usuario WhatsApp-only la única puerta para RENOVAR, y el docblock que lo justificaba afirmaba un control que no existe.** Cambié `llegoElAviso` para que solo cuente como entrega la campana in-app de quien tiene cuenta web, y escribí que para los 44 WhatsApp-only "la puerta pasa a ser `/premium`, y esa **siempre** funciona". Las dos mitades son falsas: `/premium` el **comando** solo devuelve texto (se le sacó el efecto lateral a propósito el 04-ago), y el intent `ver_premium` abre la ventana solo si `plan !== 'premium'` — pero los avisos de vencimiento van justo a quien todavía tiene `plan === 'premium'`. Resultado: yapea los S/10, manda la captura, se le anota "Gasto — Neto, S/10", cero fila en `pagos`, cero aviso a Favio, y al vencer cae al muro creyendo que pagó. 1 usuario en esa situación al 14-ago | `justificacion-que-no-se-midio` | **revisión adversarial** del diff, rastreando los tres orígenes de `esperando_comprobante` en vez de creerle a mi comentario. Mi verificación previa estaba verde: 1358 tests, `tsc`, y 8 mutaciones muertas sobre los 4 call-sites | No, y ninguno podía: todos mis guards preguntan "¿la ventana se abre cuando debe?". Ninguno pregunta "¿queda alguna forma de abrirla?" — una propiedad sobre la AUSENCIA de caminos, que no se ve mirando el call-site | El flag salió de la decisión: qué es una captura lo decide `esPagoNeto`, no `esperando_comprobante`. Con eso las dos pérdidas simétricas se cierran (dentro de la ventana el gasto se anota igual; fuera, el pago se reconoce igual) y `llegoElAviso` deja de ser una decisión cara. 4 tests nuevos en cada camino + 11 mutaciones muertas |
| 2026-08-14 | **La guarda `pago_pendiente` que agregué para no abrir dos solicitudes descartaba el gasto, o sea abrió una TERCERA posición donde se pierde algo** — justo la propiedad que el cambio decía cerrar. Y es la peor de las tres: si la solicitud pendiente vino de un falso positivo de `esPagoNeto`, esa captura es el pago REAL, y el `return` seco le contestaba "ya lo estamos verificando" mientras la plata desaparecía. En el camino silencioso ni siquiera avisaba al admin, teniendo `avisarAdminPagoPerdido` ahí al lado | `camino-feliz-unico` | **segunda revisión adversarial** (la del ARREGLO, que es la que el 04-ago se saltó y costó S/89) | No. Mis dos tests nuevos de esa rama afirmaban `guardarTransaccion` **no** llamado: fijaban el defecto | El gasto se anota igual (`guardarTransaccion` dedupea por hash, así que reenviar la misma foto no duplica) y el admin recibe un aviso que dice explícitamente "si la pendiente era un falso positivo, ESTA puede ser el pago de verdad" |
| 2026-08-14 | **`esPagoNeto` pasó a correr para TODA imagen y su regex de comercio no tenía borde de palabra.** `/favio/` matchea "Faviola", "Faviana" y cualquier nombre que lo contenga; mientras el predicado solo corría dentro de la ventana de 48h el falso positivo era raro, y al sacarlo del flag quedó permanentemente alcanzable. Un yape de S/10 a una Faviola se leía como pago Pro: solicitud abierta, `estado_pago` pisado, Telegram al admin con la foto de un pago privado a un tercero, y —por la guarda de la fila de arriba— el comprobante REAL siguiente bloqueado | `ampliar-el-alcance-sin-reapretar-el-predicado` | **segunda revisión adversarial**, preguntando explícitamente con qué entrada real de Vision da falso positivo | No existía ni un test de `esPagoNeto` fuera de los dos casos felices de `webhook-imagen` | `\bfavio\b`, más un test con "Faviola Quispe" y S/10 que exige que siga siendo un gasto. La tolerancia del anual (97.5–100.5, o sea que S/100 redondo entra) se dejó a propósito: es un patrón de pago real, y con el borde de palabra ya exige además un destinatario llamado Favio |
| 2026-08-14 | **Al sacar el flag de la decisión, el monto ilegible de quien SÍ esperaba comprobante pasó a contarse como falla de infraestructura.** `esPagoNeto` da false por el `isNaN`, `tipo` sigue siendo `'gasto'` (no cae en la rama de `no_pago`), y llegaba al `throw new Error('No se detectó monto')` → catch genérico: "No pude procesar la imagen" al usuario y una fila con stack en `errores`, que es la tabla de diagnóstico del backend. Vision no leyendo un monto de un Yape es esperable, no un error | `efecto-lateral-al-deduplicar` | **segunda revisión adversarial** del diff | No | Rama propia con el mensaje de Pro cuando `esperaba`, y un test que exige que la respuesta diga "no pude leer el monto" y **no** "no pude procesar la imagen" |
| 2026-08-14 | **El guard nuevo de "los cuatro `select` traen `supabase_auth_id`" corría con el fixture VACÍO**, así que los cuatro bucles por usuario cortaban por `length === 0` antes de empezar. Un `.select()` sobre `usuarios` agregado DENTRO de un bucle —un refetch de la fila del destinatario, que es justo el caso que el guard existe para atrapar— era invisible: el conteo seguía en 4 y el test verde. El comentario afirmaba lo contrario ("una query nueva obliga a decidir si alimenta la decisión") | `guard-que-afirma-mas-de-lo-que-ejercita` | **revisión adversarial** del diff. La mutación que yo había corrido (quitar la columna) sí lo mataba, así que el guard parecía sano | Es el guard | Fixture no vacío que recorre los cuatro bucles, más una aserción antivacuidad (`notificar` fue llamado) que muere si los bucles no corren. Probado con la mutación que antes sobrevivía |
| 2026-08-15 | **El experimento de D10 delegaba TODO el veredicto al callback de status, y el desenlace más probable no produce callback.** Cuando Meta rechaza en el POST, `registrarEntrega` escribe la fila **sin `wamid`**, así que no hay status que matchear. O sea que si la premisa se sostiene —el caso esperado— el experimento se quedaba mudo y terminaba en un `log.info` de Railway que nadie mira, que es exactamente el estado del que venía a sacarlo | `camino-feliz-unico` | **revisión adversarial** del diff. Mi verificación previa estaba verde: 1418 tests y 16 mutaciones muertas, todas sobre el camino que sí tiene callback | No, y ninguno podía: mis tests preguntaban "¿el veredicto sale cuando hay callback?". Ninguno preguntaba "¿hay algún desenlace sin callback?" — una propiedad sobre la AUSENCIA de un camino | El anuncio se extrajo a `anunciarVeredictoD10`, con `origen: 'envio' \| 'callback'`, y se llama desde los dos. Test + mutación por cada origen |
| 2026-08-15 | **Un fallo TRANSITORIO se anunciaba como veredicto definitivo y cancelaba la medición de por vida.** `enviarWhatsapp` devuelve el mismo `{ok:false}` cuando el POST ni llegó a Meta (el timeout de 15s de B22, DNS, respuesta no-JSON), y ahí `code` es `null`. El aviso decía "la premisa del camino silencioso se sostiene" sin haber medido nada, y como el throttle es por usuario, el `delivered` real posterior ya no se anunciaba nunca. Es la **misma clase de B19**, cien líneas más arriba en el mismo archivo | `error-no-leido` | **revisión adversarial** del diff | No. Y la lección estaba escrita en ese archivo, en `ESTADOS_DEFINITIVOS` sobre `bsuidVistos`: leerla no alcanzó | Solo se anuncia con `r.code != null` (Meta contestó). Test del transitorio + mutación |
| 2026-08-15 | **El throttle del veredicto era first-writer-wins entre veredictos que no son equivalentes.** La clave era el `usuario_id` a secas y el ENVÍO no está throttleado, así que en una ráfaga (6 mensajes en 13 min es el patrón real) un rechazo quemaba la clave y el `delivered` posterior —el ÚNICO veredicto accionable, el que manda a desarmar el camino silencioso— se descartaba en silencio | `ampliar-el-alcance-sin-reapretar-el-predicado` | **segunda revisión adversarial** (la del ARREGLO) | No: mi test del throttle repetía el MISMO veredicto tres veces, así que no podía ver el caso de dos veredictos distintos | La clave lleva la clase (`llego` \| `no-llego`) y hay test de "positivo después de negativo" |
| 2026-08-15 | **Marcaba el aviso como dado sin saber si salió.** `notificarAdmin` se traga sus propios errores y devolvía `undefined` siempre, así que con Telegram caído el veredicto se perdía **para siempre** y el único síntoma era un mensaje que no aparece. Encima el test que decía cubrir esto usaba `mockRejectedValue` sobre una función que **nunca rechaza**: fabricaba una rama que producción no toma, que es una regla escrita en ese mismo archivo | `mis-guards-tambien-fallan` | **segunda revisión adversarial** | Es el guard | `notificarAdmin` devuelve booleano (+ su propio archivo de test, porque los tests del veredicto lo mockean y la mutación sobrevivía), y la clave se LIBERA si no salió. El test de la rama muerta se borró con el motivo escrito |
| 2026-08-15 | **La confirmación se armaba con una fila que `guardarTransaccion` no siempre devuelve.** En un hit de dedup devuelve el duplicado que encontró, que sale de un `select('id, tarjeta_last4')`: sin monto ni comercio. Es un objeto válido, así que la guarda `!tx` no lo atrapaba y el mensaje salía **"Anoté tu gasto: S/ undefined"** — el único mensaje que este camino le puede llegar a entregar a una persona | `camino-feliz-unico` | **revisión adversarial** del diff | No: mis fixtures devolvían siempre la fila completa, o sea que el mock era más benévolo que la función real | Guarda `tx.monto == null` + test del dedup + mutación |
| 2026-08-17 | **El test que escribí para el nudge de primer gasto pasaba en verde con el bug reintroducido.** Los 9 casos afirmaban el efecto correcto, pero el mock de supabase tenía `eq`/`neq`/`or`/`in` como no-op devolviendo el fixture entero, y el bug ES un filtro (`onboarding_completado.eq.false`): con la cadena ignorada, el código viejo y el nuevo producen lo mismo. El archivo se llamaba a sí mismo "la contraprueba del bug" | `el-mock-no-implementa-lo-que-el-bug-usa` | la prueba por mutación, antes de pushear | No: era el guard | El mock filtra de verdad sobre `eq`/`neq`/`or`/`in`, y el fixture lleva `onboarding_completado: true` con el motivo escrito al lado. La mutación ahora mata 5 de 10 casos. De rebote destapó un fixture propio al que le faltaba `tipo`, que hacía pasar un caso por el motivo equivocado |
| 2026-08-18 | **El test del mensaje de Gmail pasaba o fallaba por el helper, no por el código.** Un solo `it` hacía DOS borrados —uno con Gmail y otro sin— y comparaba cada resultado contra lo que devolvía `enviarTexto`, que entrega el PRIMER mensaje enviado, no el último. O sea que la segunda aserción miraba el texto de la primera llamada: el caso "no menciona Gmail" estaba comparando contra el mensaje que sí lo menciona | `guard-que-afirma-sobre-el-intermedio` | correrlo (falló al primer intento) | No: era el guard | Partido en dos `it` separados, con el motivo escrito arriba. Cada caso hace UN solo borrado, así que el helper devuelve lo que el nombre del test dice que mira |
| 2026-08-18 | **Mi limpieza del harness de borrado dejó huérfanos en producción, en las dos superficies que el harness existe para verificar.** El cleanup era un try/catch único con Storage y `auth.users` AL FINAL; una línea sin sentido que yo mismo había dejado (`delete from deuda_abonos where deuda_id = '000...'`) la bloqueó `qa-guard` —correctamente, no fijaba sujeto— y eso abortó todo lo que venía después. Quedaron un objeto en el bucket `comprobantes` y una identidad en `auth.users`, que son justamente las dos cosas que ningún cascade alcanza y que ninguna limpieza posterior de la DB iba a barrer. El cleanup tenía el MISMO bug que el wipe que venía a probar: cortar al primer fallo | `limpieza-que-corta-al-primer-fallo` | la verificación de estado de prod después de correr el harness (contar filas, no leer el "limpieza ok") | No: el propio harness imprimió `LIMPIEZA INCOMPLETA` y yo leí el resto en verde | Cada paso corre en su propio try (`paso()`) y NINGUNO tumba a los siguientes; Storage y `auth.users` se limpian PRIMERO, no al final. Los huérfanos se borraron a mano y se recontó prod contra su línea base |
| 2026-08-18 | **Le apliqué el razonamiento del teléfono a UNA de las tres tablas que lo tienen.** La migración del borrado documenta con cuidado por qué `nlp_errors` se borra también por `whatsapp` (su FK es SET NULL y la columna del número es propia), y no fui a preguntar qué otras tablas tienen esa misma forma. `errores` y `tickets_soporte` la tienen. Medido contra prod: **166 filas de `errores` con `usuario_id` en NULL y un `whatsapp` que pertenece a un usuario VIVO**, o sea que tras la baja el número de la persona seguía ahí, al lado del texto del error. El `residual` dinámico no podía verlo: cuenta por la columna de la FK, no por las columnas paralelas de identidad. Idéntico patrón en la misma sesión: les di a los ESPACIOS una guarda de tres `NOT EXISTS` para no destruir dato ajeno, y no se la di a metas ni a gastos compartidos, que tienen las mismas FK en CASCADE | `invariante-con-una-puerta-sin-barrer` | revisión adversarial del diff | No: el harness sembraba solo la tabla que yo ya había mirado | Las tres tablas comparten el WHERE, y el guard estático `tests/services/account-deletion.test.js` lo fija por tabla. Metas y gastos recibieron la misma guarda que los espacios. El límite del `residual` quedó ESCRITO en la migración, con sus cuatro formas de dar verde falso |
| 2026-08-18 | **Creé un estado nuevo (la lápida) y no barrí a quienes lo leen.** El borrado deja viva la fila de `usuarios` con `plan='premium'` intacto —decisión lockeada— así que las 6 queries de `cron/checks.js` que seleccionan por plan o por `trial_estado` la seguían trayendo: cada aviso REPOBLABA `notificaciones` y `notification_deliveries`, o sea que el borrado se deshacía solo hasta la fecha de vencimiento. Y en el mismo request de WhatsApp, `webhook.js` guarda el turno de la conversación DESPUÉS de que `manejarOnboarding` devuelve: reinsertaba en `conversaciones` el último mensaje de la persona **y el texto que le dice "borré todo lo que nos escribimos"**, en el 100% de las bajas por ese canal | `consumidor-no-actualizado` | revisión adversarial del diff | No. El `residual` no podía verlo: se calcula DENTRO de la transacción, así que no ve nada recreado después | Filtro explícito en las 6 queries + `tests/cron/lapida-no-recibe.test.js`, que barre `from('usuarios')` y exige una razón declarada para cada una. El webhook mira `usuario.cuenta_borrada_at`, que el servicio deja puesto en la fila en memoria |
| 2026-08-18 | **El primer intento de arreglar lo anterior fue peor que el problema, y lo descarté en el camino.** Puse el corte en `notificarUsuario`, que es el chokepoint declarado de todo aviso proactivo — parecía el lugar correcto. Metía una lectura de DB en la única función que no hacía I/O, puso 37 tests en rojo, y sobre todo obligaba a decidir qué hacer cuando ESA lectura falla: fallando cerrado, un hipo de Supabase suprime **todos** los avisos proactivos del producto (incluidos los de fin de trial, que son los que mueven plata) para tapar un caso que le pasa a una cuenta ya borrada. El remedio tenía más superficie de daño que la enfermedad | `camino-feliz-unico` | contarlo: 37 tests rojos me hicieron mirar el costo real, y ahí apareció la rama de error | — (no llegó a pushearse) | El corte vive donde se ELIGE al destinatario, no donde se envía. Queda un comentario en `lib/notify-user.js` explicando por qué NO va ahí, para que no se reintente |
| 2026-08-18 | **Mi arreglo a un hallazgo fue PEOR que el hallazgo, y estuvo aplicado en produccion.** La primera revision senalo que metas y gastos compartidos no tenian la guarda de "no destruir dato de terceros" que si tenian los espacios. Copie el predicado tal cual —`NOT EXISTS (... a.usuario_id IS DISTINCT FROM p_usuario_id)`— sin mirar que **`space_members.user_id` es NOT NULL y `meta_aportes.usuario_id` es NULLABLE**, y que ningun codigo de produccion escribe esa columna. Con NULL, `NULL IS DISTINCT FROM <uuid>` es TRUE: la meta se leia como "compartida con otro" y quedaba FUERA del borrado. Medido con el predicado real contra prod: **12 de 13 metas se borraban y una no** — la unica que tiene un aporte. Es estrictamente peor que la version anterior, que las borraba todas, y ademas dejo `meta_aportes` sin forma de morir (antes caian por cascade de la meta) | `predicado-copiado-sin-mirar-la-nulabilidad` | segunda revision adversarial, la del ARREGLO | No, y esta es la parte que mas ensena: **el harness pasaba en verde porque su fixture sembraba `meta_aportes` con `usuario_id: A`, un valor que ningun codigo de produccion escribe**. El fixture era mas benevolo que la realidad | `coalesce(a.usuario_id, m.usuario_id)` en la 073d (un aporte sin dueño es del dueño de la meta), el fixture del harness siembra NULL —la forma real— y hay una asercion nueva de que el aporte muere por CASCADE. Verificado midiendo los dos predicados sobre los datos vivos: 12/13 contra 13/13 |
| 2026-08-18 | **Escribí una segunda lista de monedas al lado de la que ya existía, y se abrió por donde no la miré.** El rescate de `registrar_manual` no debe disparar sobre un mensaje que es SOLO un número (un saldo dictado no es un gasto). Armé `esSoloUnNumero` con su propia lista, aceptando la moneda únicamente como PREFIJO. `lib/nlp-guards.js` ya tenía la lista buena, con los modismos peruanos. Resultado medido: `592.91` rebotaba pero `592.91 PEN`, `1045.21 S/`, `20 lucas` y `592.91 cocos` entraban como gasto — y `592.91 usd` entraba como gasto en DÓLARES, o sea que `guardarTransaccion` lo multiplicaba por el tipo de cambio y el saldo aterrizaba como ~S/2200 en `monto_pen`, que es lo que alimenta reportes y score. `20 lucas` registraba mientras `250 soles` rebotaba, siendo lo mismo | `segunda-copia-del-vocabulario` | revisión adversarial del diff | No. Mis tests probaban `592.91` y `S/ 1045.21`, o sea los dos casos reales que tenía a mano: ninguno con la moneda al final | `quitarTokensDeMoneda()` se exporta desde `nlp-guards` y `esSoloUnNumero` la usa, así que la lista es UNA. Guard con las seis formas de sufijo, y la mutación "volver a moneda-solo-prefijo" en `nlp-guards` sale roja |
| 2026-08-18 | **El rescate se quedaba con el primer monto y enterraba el resto adentro del nombre del comercio.** `detectarMultiGasto` exige verbo + preposición, así que `"15 taxi 40 cena"` no le dispara y llegaba al rescate; el extractor entra por su rama de número suelto y devuelve `{monto:15, comercio:"taxi 40 cena"}`. Se guardaba S/15 y la persona veía un ✅ creyendo que entraron los dos gastos. Antes de mi cambio eso rebotaba, y el copy nuevo que yo mismo escribí dice *"Si son varios gastos, va uno por mensaje"* — o sea que el mensaje que resuelve el caso es el que dejé de mostrar justo para ese caso. Es la misma falla que el docblock de `primerMonto` declara inaceptable, entrando por otra puerta | `camino-feliz-unico` | revisión adversarial del diff | No: mis 20 casos de medición eran todos de un solo monto o no rescatables, así que la muestra no contenía el escenario | `contarMontosCandidatos()` en `nlp-guards` (comparte los filtros de `primerMonto`, sin segunda copia) y la condición `> 1` en el call-site. Medido sobre el pool de 510: cuesta CERO rescates legítimos (68 antes, 68 después) y elimina 4 falsos positivos más |
| 2026-08-18 | **Un fallo de red convertía una consulta en un gasto registrado.** Cuando `detectarQuerySinMonto` reconoce una consulta, el dispatch va en un `try` cuyo `catch` solo loguea. Puse el rescate DESPUÉS de ese bloque, así que un fallo transitorio del handler de lectura caía al rescate y le registraba un gasto a quien había preguntado algo. No es hipotético: mi propio test se fue a 28 segundos y salió rojo porque la llamada de red murió, y el gasto se guardó | `rama-de-error-reinterpretada` | el reloj de un test flaky, no yo. Estaba por atribuirlo a "la red del CI" | No, y peor: mi test DEPENDÍA de que la red funcionara, o sea que ejercitaba esa rama solo por mala suerte | La condición pasa a `redirect \|\| ...`: si el mensaje fue reconocido como consulta, no se rescata pase lo que pase con el dispatch. El guard nuevo INYECTA el fallo (rompe `supabase.from`) en vez de esperar que la red se caiga sola |
| 2026-08-18 | **Escribí un comentario que explicaba el cambio con un mecanismo falso, y un test que "lo probaba" con un mock imposible.** Bajé el `abortoDetCat.abort()` para que el rescate no lo cancelara, y justifiqué que si no `await pDetCat` rechazaba y el gasto se perdía por el catch genérico. Falso: `detectarCategoriaIA` atrapa el `AbortError` en su propio try y devuelve `{categoria:null}`. El mock de mi test SÍ rechazaba, o sea que fijaba una decisión sobre una premisa que producción no puede producir. El cambio sigue siendo correcto pero por otra razón (se pierde la CATEGORÍA, no el gasto), y la razón escrita es la que lee la próxima sesión | `justificacion-inventada` | revisión adversarial del diff | No: la mutación mataba el test, así que el guard se veía sano. Un mock más benévolo que la realidad da verde en las dos direcciones | Comentario corregido, con la versión vieja citada para que no se reintente. El mock ahora RESUELVE `{categoria:null}` como la función real, y la aserción pasó a ser la categoría heredada; la mutación sigue matando |
| 2026-08-18 | **Argumenté fallar cerrado por una lectura de 10 filas y dejé abierta la de 115 que decide el mismo número.** Escribí `cargarPagosConPlata` con un `throw` deliberado y un comentario explicando que un MRR silenciosamente más bajo se lee como una caída del negocio. Tres líneas más arriba, las siete destructuraciones del `Promise.all` de `/economics` (y las cinco de `/stats`) seguían siendo `{ data }` peladas: un hipo del pooler pintaba **"MRR S/0.00 · 0 Pro activos" en verde con HTTP 200**, y como la respuesta era 200 el `ErrorState` de `/admin/operacion` no se disparaba. La peor era `admin_costs`: sin costos, `breakeven_gap` sale negativo y la tarjeta dice **"✓ Breakeven alcanzado"**. Y el remate: con `usuarios` vacío la lista de ids queda vacía, mi cargador retorna temprano sin ir a la red, y **el throw que escribí no llega a ejecutarse nunca** | `politica-aplicada-solo-donde-la-escribi` | revisión adversarial del diff (lente de runtime) | No: ninguna de esas lecturas tenía test, y el panel en ceros no rompe nada | Las 12 lecturas leen su `error` y devuelven 500 nombrando cuál falló, más una guarda de truncado a 1000 filas sobre `usuarios` |
| 2026-08-18 | **Arreglé un bug de zona horaria y rompí las columnas que sí andaban.** `monthWindowLima` movió el borde del mes de `00:00Z` a `05:00Z` para que `cuenta_borrada_at` (timestamptz) cayera en el mes Lima correcto. `premium_desde` y `premium_vence` son DATE y `new Date('2026-08-01')` da `00:00Z`, cinco horas ANTES del nuevo inicio de agosto: **toda fecha del día 1 se cayó al mes anterior**. Medido ejecutando las funciones reales: un alta del 01-ago daba `newProInMonth(agosto)=0` y `newProInMonth(julio)=1`, y `wasProAtMonthEnd` la contaba como Pro al cierre de julio, un mes antes de serlo. Contra producción: 1 usuario con `premium_desde` en día 1 y 2 con `premium_vence` en día 1, y crece ~1 de cada 30 altas. Lo peor: el docblock que escribí **sabía** que esas dos eran DATE y sacaba la conclusión contraria | `arreglo-que-desalinea-al-vecino` | segunda revisión adversarial, la del ARREGLO | No, y no podía: mis tests de `monthWindowLima` pasaban instantes UTC explícitos, así que ninguno tenía una fecha del día 1 | `msDeFechaLima()` normaliza las DATE a medianoche de Lima y la usan las cuatro comparaciones. Seis mutaciones (una por call-site) salen rojas, y hay casos del día 1 y del borde de los 30 días |
| 2026-08-18 | **El chart decía "+1 Pro nuevo" sobre un MRR que no se movía.** Hice que `newProInMonth` viera las reactivaciones (sin eso, el mes en que alguien vuelve sube el MRR con las dos columnas que lo explican en cero) y en `churnedInMonth` dejé una supresión con el argumento inverso: si se fue y volvió el mismo mes, no reportar la baja "para no contradecir al MRR". Las dos ramas no se coordinaron, así que agosto quedaba con `newPro: 1, churned: 0` y el MRR plano: la suma `Σ(new_pro − churned)` dejaba de reconciliar justo en el caso que la rama nueva vino a explicar | `dos-ramas-de-la-misma-fila-decididas-por-separado` | segunda revisión adversarial, la del ARREGLO | No: el test que escribí FIJABA la supresión, o sea que blindaba el defecto en vez de atraparlo | La columna cuenta EVENTOS: `+1` y `−1` en la misma fila se cancelan y reconcilian. El test viejo se reemplazó por el par (alta y baja del mismo mes) y por el caso que originó el hallazgo |
| 2026-08-18 | **Escribí el guard con dos instantes distintos y reabrí la clase que venía a cerrar, con el signo invertido.** `esBajaDeclarada` preguntaba "¿volvió alguna vez?" (`Infinity`) y `computeChurn` preguntaba por `now`. Con un pago de `aprobado_at` futuro, la misma persona entraba al MRR (retorno según el primero) Y al numerador del churn (todavía de baja según el segundo): sumaba en los dos lados del ratio. Con una `cuenta_borrada_at` futura pasaba lo espejo. Y el comentario que escribí al lado **afirmaba que los dos instantes ya eran el mismo** | `dos-relojes-para-la-misma-pregunta` | revisión adversarial del diff (lente de runtime) | No. No es alcanzable con datos sanos —las dos fechas las escribe `now()`— así que ningún fixture realista lo produce | Las tres funciones reciben el instante y no existe la pregunta "en el infinito". Dos tests con fecha futura, y la mutación que restaura el `Infinity` sale roja |
| 2026-08-18 | **La tercera versión del guard de call-sites, y las dos anteriores murieron por lo mismo: preguntaban por el ARCHIVO en vez de por el `.select()`.** La v2 borraba comentarios y extraía los selects, pero preguntaba "¿ALGÚN select de este archivo pide la columna?" y descubría a los consumidores por el texto literal `computeRevenue(`. Un ataque adversarial la evadió de cuatro formas, **las cuatro con la suite entera en verde y el MRR mal**: renombrar el import (`computeRevenue as calcularIngreso`), llamar a la función desde un helper fuera de `api/admin/`, agregar una SEGUNDA query que sí traía las columnas mientras la principal las perdía, y partir el nombre de la columna con un comentario en el medio — que el propio borrado de comentarios volvía a pegar. La v3 introdujo cuatro evasiones NUEVAS de la misma clase (`.from(CONSTANTE)`, sufijo `.replace()` después del literal, columna pedida sobre una tabla embebida). Y el guard llegó a detectarse **a sí mismo**: mi comentario de exención nombra `computeRevenue` para explicar por qué esa lectura no lo usa, y con eso el archivo contaba como consumidor | `guard-que-se-mide-contra-su-documentacion` | ataque adversarial dedicado a romper guards (20 mutaciones, 10 verdes) + la revisión del arreglo (4 más) | No, por construcción: un guard verde por evasión es indistinguible de un guard verde por corrección | La regla dejó de ser sobre los consumidores: TODO `.select()` de `usuarios` en rutas admin pide las columnas, con exenciones marcadas y fijadas en una lista. El `.from()` ilegible es fallo, el literal tiene que ser el argumento COMPLETO, el embed no cuenta, y `computeRevenue` solo puede llamarse desde donde el guard mira. 34 mutaciones probadas, 34 rojas |
| 2026-08-18 | **Vigilé tres columnas y dejé afuera la que más daño hace.** El guard exigía `id`, `is_test_user` y `cuenta_borrada_at` en el select. `esProPagado` es `plan === 'premium' && trialEstado !== 'activo'`: sin `trial_estado`, `undefined !== 'activo'` da true y **todo trial cuenta como pagador**. Medido contra producción: 9 Pro pagados contra 31 en trial, o sea que el MRR pasaba de S/90 a ~S/400 con la suite entera en verde. Es la misma clase del 02-ago (S/20 sobre S/56 por `is_test_user`) que ese archivo existe para atrapar, con un cero más | `inventario-incompleto-de-lo-que-decide` | ataque adversarial a los guards | No. La columna estaba en los dos selects, así que nada fallaba: lo que faltaba era la exigencia | `plan`, `trial_estado` y `tipo_plan` entraron a la lista, cada una con su consecuencia escrita. Las mutaciones que las sacan salen rojas |
| 2026-08-18 | **Mi cliente falso devolvía la fila entera pase lo que pase.** El test nuevo de `bindActivacion` verifica que una lápida no adopte una sesión, y el doble de Supabase ignoraba el `select`: la mutación "sacar `cuenta_borrada_at` del select" dejaba el test **en verde** con la columna fuera de la query real. Es exactamente el modo de falla del fixture de `meta_aportes` de esta misma semana —un doble más benévolo que la realidad convierte al guard en decoración— y lo repetí catorce días después | `fixture-mas-benevolo-que-la-realidad` | mutación propia sobre el guard recién escrito | Sí: la memoria y la fila de DEFECTOS existían. No alcanzó | El cliente falso PROYECTA la fila según las columnas del `select`, como hace PostgREST. La mutación ahora sale roja |
| 2026-08-20 | **Escribí que el arreglo dejaría una fila en `errores` y no la deja.** Los comentarios nuevos de `cron/checks.js` justificaban leer el `{ error }` diciendo que sin eso el cron se apaga *"sin una línea en `errores`"*, dando a entender que ahora sí la habría. **Falso**: el único escritor de esa tabla es `registrarError` (`lib/error-monitor.js`), `cron/checks.js` no lo importa, y `lib/logger.js` no tiene transport ni hook que reenvíe — sólo pretty-print en dev. Los 20+ `log.error` agregados van a stdout de Railway y a ningún otro lado. El runbook de `reference_neto_error_diagnostics` manda a consultar `errores` por tag, así que alguien buscando `INACTIVITY` o `ACTIVACION_DIA2` no iba a encontrar nada y el silencio se leía como "no falló" — el mismo silencio que el trabajo venía a romper | `documente-una-garantia-que-el-codigo-no-da` | Lo encontró el revisor adversarial del diff, no la suite (ningún test puede ver que un comentario miente). Corregido nombrando la limitación en el comentario. Enrutar los errores de los crons a la tabla queda en el backlog de confiabilidad |
| 2026-08-20 | **El guard nuevo arrancó con dos puntos ciegos DENTRO de su propio archivo.** `lecturas-leen-el-error.test.js` afirma que ninguna lectura de `cron/checks.js` descarta el `{ error }`, y anclaba su parser en `await supabase`. `checkRecordatorioOnboarding` lee sus dos tablas con `await Promise.all([supabase.from(...), supabase.from(...)])`: ahí el `await` está sobre el `Promise.all`, no sobre las queries, **así que esas dos lecturas nunca se verificaron**. Leían su error por suerte, no por el invariante. Y la segunda evasión venía del backtrack, que retrocedía 400 caracteres hasta el último `const` **cruzando statements**: una reasignación `yaAviso = await supabase...` heredaba el LHS del destructuring de la línea de arriba y pasaba como si leyera el error — en un archivo donde casi toda línea previa es un `const { data, error } =`, evadía desde casi cualquier ubicación | `el-guard-no-cubre-lo-que-declara-cubrir` | Las dos las encontró el revisor adversarial ejecutando el parser, no leyéndolo. Ancla movida a `supabase.from(` y statement cortado en el `;` anterior; las dos formas quedaron fijadas como fixtures y las dos mutaciones mueren. El parser pasó de ver 44 lecturas a 46 |
| 2026-08-20 | **Mi mock trataba NULL como JS donde Postgres lo trata como SQL, y no lo vi aunque acababa de corregir lo mismo un método más arriba.** En `lecturas-con-error.test.js`, `neq` y `not(...,'eq',...)` estaban como `f[col] !== val`: en PostgREST `col <> v` con la columna en NULL da NULL y **la fila NO entra**. Afecta a dos queries reales del archivo (`.neq('is_test_user', true)` y `.not('status','eq','abandoned')`), las dos sobre columnas nullables. Lo caro es el contraste: los operadores de RANGO del mismo mock **sí** llevaban la corrección explícita, con su comentario sobre `null < x`, así que el archivo se leía como si el tema estuviera resuelto. Es la trampa que este repo ya pagó dos veces (`SIN_TRIAL_ACTIVO` en el propio `checks.js`, el `coalesce(is_test_user, false)` de la migración 057) | `fixture-mas-benevolo-que-la-realidad` | Latente, no vivo: medido en prod, 0 de 116 usuarios con `is_test_user` en NULL y 0 de 13 metas con `status` en NULL. Lo encontró el revisor adversarial. Corregido en los tres operadores |
| 2026-08-20 | **Dejé `order` y `limit` en no-op y con eso ningún fixture podía separar "la última" de "una cualquiera".** El mock de `lecturas-con-error.test.js` devolvía la PRIMERA fila del fixture, y dos lecturas del cron deciden por FECHA (`ultimaTx` en el recordatorio de inactividad, `ultimoTurno` en la ventana de 24h de la activación). Con una sola transacción por usuario en los fixtures no se nota nunca: **invertir `ascending` en el `.order` salía verde**. Es la misma clase del mock de `nudge-primer-gasto`, cuya primera versión tenía la cadena entera en no-op y pasaba con el bug reintroducido — un método más abajo | `mock-que-no-ejercita-la-condicion` | Encontrado por el revisor adversarial. `order`+`limit` implementados, y agregado el caso que los hace importar: un usuario que anotó AYER y tiene una transacción de hace meses no debe recibir el recordatorio. Con eso, invertir `ascending` muere |
| 2026-08-20 | **Escribí la justificación de un corte al revés de como es.** El dedup de `checkRecordatorioSuscripciones` corta al fallar, y el comentario decía que eso sale gratis *"porque con Supabase caída el envío tampoco iba a funcionar"*. No se sostiene: el error está acotado a una request de PostgREST sobre `notificaciones`, mientras `notificarUsuario` sale a Meta por otro camino. Y la aritmética iba en la dirección contraria a la que el comentario sugería: este cron tiene UN tick por día y el aviso sólo sale si faltan EXACTAMENTE 3 días, así que **cortar cuesta el aviso del ciclo entero** (probable) y no cortar cuesta un duplicado sólo si `detectarSuscripciones` mueve `ultimo_pago` dentro de la ventana de 25 días (raro). La decisión de cortar sigue en pie, pero por otro motivo: un aviso repetido sobre un COBRO se lee como un cobro repetido | `justifique-la-decision-correcta-con-el-argumento-falso` | Encontrado por el revisor adversarial. Ningún test puede ver esto: el comportamiento es el mismo, lo que estaba mal era el porqué — y el porqué es lo que va a leer quien toque esto en seis meses |
| 2026-08-20 | **Pegué el comentario del costo irreversible al sitio equivocado, y el sitio correcto quedó sin arreglar.** En `checkActivacionDia2` escribí sobre la LECTURA de población que *"a quien se pierda por una caída silenciosa acá no lo recupera ninguna corrida posterior"*. Es falso para esa lectura: el cron corre cada 15 min sobre una ventana de 24h, así que un error transitorio se reintenta hasta 48 veces. La frase es cierta palabra por palabra para el **UPDATE de `activacion_nudge_at`** unas líneas más abajo, que es el ledger de un-solo-envío y **sigue sin verificar**: si falla mudo, todas las condiciones de selección valen en el tick siguiente y el mismo link puede salir decenas de veces el mismo día | `el-comentario-correcto-en-la-linea-equivocada` | Encontrado por el revisor adversarial. Corregido el texto y nombrado el sitio real. La escritura queda en el backlog: es una escritura, no una lectura, y pide su propio test. Latente hoy — medido: 0 filas en `notification_deliveries` con `tipo='activacion'` y 0 usuarios con `activacion_nudge_at` no nulo, o sea que ese cron nunca envió nada en producción |
| 2026-08-20 | **Mi prueba por mutación reportó "sobrevive" sobre una mutación que nunca se aplicó.** El script que revierte cada arreglo hacía `s.replace(old, new)` sin comprobar que `old` estuviera en el archivo. Con los escapes de regex mal pasados a través del heredoc, dos reemplazos no encontraron su patrón, el archivo quedó intacto, los tests pasaron y el reporte dijo `*** SOBREVIVE ***` — o sea, **exactamente lo mismo que diría un guard que no atrapa nada**. Estuve a punto de "reforzar" un test que ya funcionaba | `mutacion-que-no-se-aplico` | Agregado `assert count(old) == 1` antes de cada mutación. Con eso, una de las dos pasó a MUERE y la otra falló ruidosamente en vez de mentir. Es la lección de `feedback_mutacion_que_no_se_aplico`, repetida |
| 2026-08-20 | **Escribí un fixture negativo que pasaba por otra condición, y con eso una mutación real quedó viva.** El guard blanquea comentarios antes de parsear; el fixture que iba a probarlo era `const { data } = /* error */ await supabase...`, y ese caso lo rechaza **la regex estricta** (después de `error` viene ` *`, no `:` ni `,` ni `}`), no el blanqueo. O sea que la mutación "dejar de blanquear comentarios" sobrevivía en verde con el fixture presente. El que separa las hipótesis lleva `error }` DENTRO del comentario, que sí matchearía la regex sin el blanqueo | `negativo-que-rechaza-por-otra-condicion` | Lo encontró mi propia prueba por mutación sobre el guard, no la revisión. Reemplazado el fixture y agregado el segundo efecto del blanqueo (un `await supabase.` comentado no es código) |
| 2026-08-20 | **La primera versión de los tests dejaba 12 de 32 mutaciones vivas, con los 56 casos en verde.** Tres causas, y ninguna se ve leyendo el archivo. (1) Los dedups de espacios y suscripciones **nunca se alcanzaban**: sus servicios estaban mockeados devolviendo vacío, así que el cron cortaba antes de llegar al dedup — un mock que devuelve nada es un test que no llega. (2) El assert de tag comparaba subcadenas sobre el JSON entero, y `'EXPIRY'` está contenido en `'EXPIRY_WARN'`, así que cuatro logs distintos del mismo cron se tapaban entre sí y quitarle el log a cualquiera de ellos no rompía nada. (3) Las decisiones por-usuario no tenían caso **porque su comportamiento no cambia**: ya fallaban cerrado por accidente, el `continue` es el mismo, y lo único observable —el log— era justo lo que no estaba mirando | `el-test-verde-que-no-ejercitaba-la-linea` | Encontrado por la prueba por mutación, no por la revisión ni por la suite. Los 12 casos corregidos; hoy las 36 mutaciones mueren y **todas por el test funcional**, no sólo por el guard de forma — que es la distinción que separa "cubrí el comportamiento" de "cubrí la sintaxis" |
| 2026-08-22 | **Dos de mis tests nuevos pasaban por una condición distinta de la que decían probar, y la prueba por mutación lo mostró: las dos guardas que cubrían sobrevivían en verde.** (1) El caso de la primera lectura de `recategorizarTransaccion` tumbaba TODAS las lecturas de `transacciones`, así que con la guarda neutralizada el flujo caía al reintento por palabra, que fallaba igual y devolvía **el mismo mensaje**: la segunda guarda tapaba a la primera. (2) El caso del dedup de subcategorías filtraba por la columna `padre_id`, y `buscarCategoriaRaiz` también la anota (`is('padre_id', null)`), así que el fixture mataba esa OTRA lectura y la función salía por su throw sin llegar nunca al dedup — el caso probaba la guarda de al lado | `negativo-que-rechaza-por-otra-condicion` | La prueba por mutación, no la revisión ni los 87 casos en verde. Es la tercera vez que la misma clase aparece midiendo, y las tres veces el síntoma fue idéntico: **el fixture no discrimina entre dos lecturas que comparten una columna**. Arreglados haciendo caer sólo la lectura del caso (el patrón con espacio; el `padre_id` con VALOR) |
| 2026-08-22 | **Maté mi propia prueba por mutación con un `\| head -5` y salió con exit 0.** Node recibió SIGPIPE cuando `head` cerró el pipe, el script murió después de 3 de 136 mutaciones, y la salida —tres líneas `muere` seguidas de nada— tiene exactamente la misma forma que una corrida sana truncada por el propio `head`. Lo único que la delató fue que **faltaba la línea de resumen**; sin ese detalle habría concluido "todas mueren" sobre el 2% del barrido. Encima, el script escribe el árbol vivo: su `finally` restauró, pero el manejador de señales sólo cubre `SIGINT`/`SIGTERM` | `corrida-truncada-que-sale-verde` | Yo, al notar que faltaba el resumen. Re-corrido entero a un archivo, sin pipe. Es la misma familia que `mutacion-que-no-se-aplico`: **una verificación que no llegó a correr se parece a una que pasó** |
| 2026-08-22 | **Reutilicé el nombre `error` en un scope donde ya estaba declarado**, en `corregirTransaccionEspecifica` (mi `const { data: txs, error }` chocaba con el `const { error }` del update, doce líneas más abajo). `SyntaxError: Identifier 'error' has already been declared`, o sea el archivo entero sin cargar | `binding-que-choca-en-el-scope` | `node -e "require(...)"` antes de correr un solo test. Trivial de arreglar y por eso vale la fila: el control que lo atrapó (cargar el archivo) costó un segundo y no estaba en mi rutina |
| 2026-08-22 | **Tres defectos del harness que escribí AYER (ítem 7, `9594739`) y que sólo aparecieron al usarlo hoy.** (1) El mock aliaseaba `chain.maybeSingle = chain.single`, así que devolvía `PGRST116` con cero filas — algo que `maybeSingle` **nunca** hace; cualquier caso escrito sobre esa forma ejercitaba una rama que la producción no produce, y `shared-spaces.js:176` tiene un comentario explicando justamente por qué eligió `maybeSingle`. (2) El mock no devolvía la fila escrita en un `upsert` (sólo en `insert`), así que el control de `guardarPresupuesto` salía rojo por el mock. (3) El mutador cerraba la condición con un regex que exigía `)` pegado a la variable, o sea que **no veía ninguna guarda `if (error && …)`** — la forma obligada de todo `.single()`, porque `PGRST116` no es un fallo. Cuatro guardas del árbol caían en ese punto ciego y ninguna aparecía como superviviente: no aparecía | `guards-que-no-ven` | Usar el harness para lo siguiente, que es el único control que lo prueba. Los tres arreglados acá; al ver las guardas nuevas, los supervivientes pasaron de 37 a 43 |
| 2026-08-22 | **Mi probe dijo tres veces que la landing no reportaba Core Web Vitals, y las tres veces el que no reportaba era el probe.** `probe-rum-vitals.mjs` salía `NO_REPORTA` contra una landing que reporta bien, con tres causas encadenadas y todas del lado del instrumento. (1) **PostHog descarta el tráfico automatizado**: su detector termina en `return !!navigator.webdriver` pero antes recorre `navigator.userAgentData.brands` contra una lista que incluye `headlesschrome`, y tapar sólo la primera señal no alcanzaba — Chromium headless publica `brand: "HeadlessChrome"` ahí aunque se le pase un userAgent de iPhone. (2) **Aborté la petición interceptada** para no contaminar el dataset, y con eso rompí la cola de envío: el `$web_vitals` sale SEGUNDO, así que el cliente lo retuvo tras el primer POST fallido. El probe rompía el transporte que estaba midiendo. (3) **Leí el cuerpo como `data=<base64>`**, la forma vieja de la documentación, cuando PostHog lo manda **gzippeado** y hay que sacarlo de `postDataBuffer()` — con `postData()` los bytes ya vienen destruidos. Con eso veía dos peticiones de ingesta "con cero eventos adentro" | `negativo-producido-por-el-instrumento` | Yo, al exigirle un CONTROL al instrumento: reporté los envíos SIN filtrar y aparecieron 6 (Cloudflare RUM, GA, doubleclick) y ninguno de PostHog. Ahí el sospechoso dejó de ser la landing | No. Es un archivo nuevo, y el primer `NO_REPORTA` era perfectamente convincente: JSON prolijo, exit 1, mensaje accionable | El propio probe: su `NO_REPORTA` ahora distingue "ninguna ingesta salió" de "hubo ingesta sin vitals", y en el primer caso el mensaje manda a revisar el enmascarado ANTES que la landing. Y una **prueba por mutación**: con `web_vitals: false` en el build da exit 1, restaurado da exit 0 |
| 2026-08-22 | **Reintroduje en `check-cwv-rum.mjs` el bug que su archivo hermano documenta en seis líneas.** `check-cwv.mjs` explica desde el 17-ago que `process.exit()` con un fetch en vuelo hace abortar a Node en Windows con `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` y el proceso sale con **127**, que no es ninguno de los tres códigos del canary — por eso ese archivo setea `exitCode` y retorna. Escribí el mío tomándolo de modelo, copié la convención de exit codes, copié hasta la disciplina de "sin key no hay PASS", y puse `process.exit(2)` en los catch de los dos fetch. Un canary que sale 127 no es "falla": es un código que el runner no sabe clasificar | `gotcha-documentado-al-lado-y-reintroducido` | Yo, probando a propósito los caminos de fallo con una key inválida. El camino feliz nunca lo toca: sólo aparece cuando ya hubo red | Sí, y en el lugar más visible posible: el comentario está en el archivo que estaba usando de plantilla. Leerlo no fue suficiente | El flujo entero se envolvió en `main()` para poder retornar; los tres caminos de fallo (sin key / key inválida / sin config) se probaron y dan exit 2 limpio. El comentario del `salir()` ahora dice que ESTA versión lo reintrodujo, para que no se lea como advertencia teórica |
| 2026-08-22 | **Mi primera medición de baseline fueron "5 corridas" de las que 3 eran la misma.** PageSpeed Insights cachea por (url, strategy): las corridas 2 y 3 volvieron en 2 segundos con valores byte por byte idénticos a la 1. La mediana habría salido igual de convincente y el rango habría salido sospechosamente angosto — y todo el ítem se apoyaba en tener un baseline comparable, con la regla de "mínimo 3 corridas y medianas" escrita justo para no cometer el error de al lado | `repeticion-que-no-es-muestra` | Yo, mirando los SEGUNDOS de cada corrida (13s, 2s, 2s, 19s, 19s). El único indicio estaba ahí; los números no lo delataban | No. La regla que tenía escrita ("3 corridas y medianas") cuenta corridas, no observaciones independientes, así que se cumplía al pie de la letra | `measure-cwv-lab.mjs` mete un `?cwv=<n>` distinto por corrida, y además marca `sospecha_cache` en cualquiera que vuelva en <=4s en vez de descartarla en silencio. Verificado que el buster no distorsiona: Cloudflare Pages sirve el HTML como `cf-cache-status: DYNAMIC` con o sin query string, con el mismo TTFB |
| 2026-08-22 | **El verde de mi probe no cubría dos de los tres umbrales que su propio `on_fail` dice proteger.** `probe-rum-vitals.mjs` aceptaba con `(LCP != null \|\| FCP != null \|\| CLS != null \|\| INP != null)`, o sea "al menos una de cuatro", y una de las cuatro es FCP, que **ni siquiera es uno de los umbrales declarados**. En una corrida real de producción llegan LCP y FCP, y CLS e INP vienen null — o sea que el verde de todos los días convivía con dos de los tres umbrales sin ningún dato, y si mañana PostHog dejara de mandar LCP y sólo mandara FCP, el probe seguiría diciendo REPORTA mientras el canary se queda sin la métrica principal. Al apretarlo apareció un segundo problema que la versión laxa escondía: **el LCP no está garantizado en UNA visita** (A/B sobre la misma build: una corrida con `[LCP,FCP]` y la siguiente con `[FCP]` sola, por el flush diferido de PostHog), así que exigirlo a secas convertía el check en intermitente | `verde-mas-ancho-que-lo-que-protege` | La revisión adversarial por subagente, corriendo el probe contra producción y mirando qué venía en `metricas` | No. El probe salía verde, la salida traía `"CLS": null, "INP": null` a la vista, y aun así yo leí "REPORTA" y seguí | Se exige LCP y se **reintenta** la visita hasta 3 veces (verlo una vez contesta "el pipeline puede reportar LCP"). La salida lista `metricasVistas` y `noObservadasEnEstaCorrida`, y el `on_fail` ahora dice explícitamente que un verde acá NO prueba que INP y CLS estén llegando — eso lo contesta `check-cwv-rum.mjs` sobre 28 días |
| 2026-08-22 | **Fijé en el código la propiedad que enciende los web vitals y dejé al control remoto la hermana que decide CUÁLES se capturan.** `capture_performance: { web_vitals: true }` cierra el on/off: verificado en el bundle, con un booleano local el server-side ya no se consulta. Pero `allowedMetrics` es un getter aparte, y cuando `web_vitals_allowed_metrics` no está declarada cae a `persistence.props['$web_vitals_allowed_metrics']`, o sea otra vez a la consola de PostHog. Un cambio ahí a `["FCP","CLS"]` apagaba LCP e INP **sin dejar diff ni commit** — que es literalmente el fallo que el commit dice haber cerrado, un nivel más abajo. Y combinado con el defecto de arriba, ese escenario dejaba el probe en verde | `configuracion-fijada-a-medias` | La revisión adversarial, leyendo el getter `allowedMetrics` en el bundle en vez de creerle a mi comentario | No. Yo había leído `get isEnabled()` y verificado esa rama; me detuve en la propiedad que estaba tocando y no busqué las hermanas que comparten el mismo mecanismo de fallback | Las dos propiedades declaradas juntas: `capture_performance: { web_vitals: true, web_vitals_allowed_metrics: ['LCP','INP','CLS','FCP'] }`, con el comentario diciendo por qué son dos y no una |
| 2026-08-22 | **Escribí como regla durable, en cuatro archivos, una conclusión que salió de dos tandas y que la tercera dio vuelta.** "El score de Lighthouse es ruido y el LCP no" lo saqué de dos tandas donde el score se movió 14-16 puntos y el LCP 150ms. Sonaba a hallazgo de método y lo puse en el docblock del harness, en dos CLAUDE.md y en la memoria. El revisor corrió el mismo instrumento un rato después: score 89/92/94 (5 pts) y LCP 3002/3151/2851 (300ms) — al revés. Una cuarta tanda mía dio score 83-88 contra LCP **2551-3226 (675ms, 23%)**. O sea que cuál métrica es más estable **cambia con la hora del día**, y los valores absolutos también se corren. La regla no era falsa por poco: estaba invertida la mitad de las veces | `conclusion-mas-ancha-que-la-medicion` | La revisión adversarial, corriendo el instrumento otra vez en vez de leer el número que yo había dejado escrito | Sí, y era MI PROPIA regla: `feedback_una_corrida_de_lighthouse_no_mide` dice que una corrida no mide. La respeté para el número (mediana de 5) y la violé para la CONCLUSIÓN SOBRE EL INSTRUMENTO, que derivé de una sola tanda | La tabla de las cuatro tandas queda en el docblock, y la regla que la reemplaza no elige métrica: comparar sólo dentro de la misma tanda, reportar la dispersión al lado de la mediana, y descartar como ruido toda diferencia que no supere la dispersión de su propia tanda |
| 2026-08-22 | **Mi probe afirmaba "nada llega a producción" y mandaba una grabación de sesión por corrida.** Para no contaminar el dataset intercepté la ingesta de PostHog con un regex que ENUMERABA rutas: `(i/v0/e\|e\|batch\|capture)`. El session recording postea a `/s/`, que no estaba en la lista, así que salía a producción — con el canary diario, ~30 sesiones falsas por mes de un Chromium de datacenter, y encima huérfanas, porque los eventos sí estaban bloqueados. El dataset de `$web_vitals` quedaba limpio (que era lo que yo estaba mirando) y el de replay no | `enumeracion-que-no-cubre-el-conjunto` | Yo, verificando a mano la afirmación más riesgosa del cambio antes de que volviera el revisor — que llegó al mismo hallazgo por su cuenta | No, y la trampa estaba en que el caso que me importaba (las métricas) SÍ estaba cubierto, así que toda la evidencia que miré confirmaba la afirmación | Se interceptan **todos** los POST a `posthog.com` y se dejan pasar los GET (los assets). Control empírico: censo de POSTs no interceptados = 0, sobre tres endpoints distintos (`/e/`, `/s/`, `/i/v0/e/`) |
| 2026-08-22 | **Descarté la hipótesis correcta mirando el nodo equivocado, y elevé una corrida en frío a propiedad de la página.** Investigando el `NO_FCP` de app.neto.pe concluí dos cosas y las dos estaban mal. (1) *"No es un guard de hidratación que deja todo en `opacity:0`"*: lo verifiqué sobre `body`, que efectivamente estaba en opacity 1 — pero el elemento transparente era un DESCENDIENTE, la tarjeta que en móvil contiene el 100% de lo que hay sobre la línea de flote. El chequeo negativo pasó por una condición distinta de la que decía chequear, así que la hipótesis buena quedó tachada por escrito. (2) *"El primer pintado tarda 3676ms sin throttling"*: era la PRIMERA navegación de un proceso de Chromium recién lanzado, con DNS y TLS en frío. Re-medido con 4 contextos limpios seguidos: 3152ms la primera, **~250ms de TTFB y ~440ms de first-paint** las tres siguientes. Ese 3676 viajó al CLAUDE.md del workspace como hallazgo (*"3.7s para el primer pixel de un login es muchísimo"*) y mandaba a auditar un camino crítico que no tiene ningún problema | `conclusion-mas-ancha-que-la-medicion` | Volver a medir con N corridas en vez de una, y con un CONTROL: el mismo harness contra neto.pe y example.com da FP==FCP 4 de 4. Ahí el sospechoso dejó de ser la red y pasó a ser la página | Sí, y era la regla de al lado: `feedback_una_corrida_de_lighthouse_no_mide`. La apliqué a Lighthouse y no a Playwright, como si el "una corrida no mide" fuera del instrumento y no del método. Para lo de `body` no había control: el negativo se leyó como cobertura sin preguntar por qué rechazaba | El A/B sobre los mismos bytes de producción (interceptar el HTML y quitar la clase) separó causa de correlación: con `fade-in` el FCP aparece en 1 de 8 corridas, sin él en 8 de 8. Y `webapp/src/app/login/entrada-no-transparente.test.ts` impide que la clase vuelva, con la distinción que decide — al montar sí, `data-open:` no |
| 2026-08-22 | **La regla de prominencia de Gmail valía para los dos canales y el guard miraba uno solo, así que la webapp llevaba cinco meses fuera de cobertura.** `verify-claims.mjs` barre `landing/src` y nada más. Corriendo sus DOS patrones actuales contra `webapp/src` apareció `app/layout.tsx:36` con *"Conecta tu banco por WhatsApp"* en la meta description de **todas** las rutas de app.neto.pe, servida en producción, y es la regla `integracion-bancaria` que ese guard existe para impedir. Alrededor, cinco líneas más que las reglas actuales NO ven porque no hay regla de prominencia: el `h1` del login vendiendo Gmail de titular, *"Sin anotar nada"* (la frase que el CLAUDE.md nombra explícitamente como falsa, con el 9.5% al lado), *"Tus gastos se registran solos"* y *"Conecta BCP, BBVA, Interbank y más"* sobre la línea de flote en móvil. Todo de `b7816ef` (26-mar), cinco meses ANTES de que la regla se decidiera (04-ago) | `barrido-de-un-solo-arbol` | Favio, leyendo el `innerText` renderizado del login mientras se medía otra cosa | Sí, tres controles y ninguno podía: el guard por perímetro; el barrido de copy del 31-jul (`406194d`), que tocó `login/page.tsx` pero con alcance "WhatsApp-only copy"; y un comentario en `activar/page.tsx` que YA describía el problema (*"una landing de captación: 'Neto lee tus correos bancarios, sin anotar nada', la promesa de otro producto"*) y se resolvió construyendo una página aparte en vez de arreglar el login | `webapp/src/app/copy-claims.test.ts`, en el CI que gatea el deploy. Dos perímetros: las reglas de falsedad sobre todo `src/`, y las de prominencia sobre el cierre TRANSITIVO de imports de las páginas que el middleware no gatea (una página pública nueva entra sola; mover el copy a un componente no la saca). Verificado en rojo contra el copy de producción: 7 hallazgos en las 7 líneas. **Módulo compartido no es posible: la landing es otro repositorio y el CI de cada uno solo hace checkout del suyo**, así que las reglas quedan duplicadas a sabiendas, con los `id` alineados |
| 2026-08-22 | **Cerré el barrido de un solo árbol por el lado de la webapp y el otro árbol tenía cuatro.** Portar las reglas a `landing/scripts/verify-claims.mjs` destapó que el `<title>`, el `og:title`, el `twitter:title` y el `alt` del `og:image` de neto.pe decían *"Ordena tu plata sin mover un dedo"* —el mismo claim que "sin anotar nada", con otras palabras, en las superficies que la regla nombra por su nombre— y que contradecía al hero de la propia página, que vende *"Anotar gastos es el piso"*. Más los nombres de bancos en las `keywords` y el post `en-que-gasto-mi-plata`, que repetía la frase falsa en su meta description Y en su cuerpo, presentando Gmail como EL mecanismo sin decir que es de Pro ni opt-in. **Mi propio patrón no veía "sin mover un dedo"**: al reportarlo el día anterior lo llamé falso negativo y era exacto, la regla atrapa las tres fórmulas que ya se dijeron, no la familia | `enumeracion-que-no-cubre-el-conjunto` | Correr contra el árbol de la landing las reglas escritas para la webapp, en vez de suponer que la landing estaba limpia porque su guard estaba verde | Sí en parte: `verify-claims.mjs` existía desde antes y barría ese mismo árbol. No tenía la regla, que es distinto de no tener el alcance — el hallazgo del día anterior era de perímetro y éste es de reglas, y hacían falta los dos arreglos | Las reglas portadas, con el perímetro de conversión definido **por complemento** (se declaran las rutas donde Gmail sí puede aparecer y todo lo demás es conversión, así una página nueva cae del lado estricto). Contraprueba: 8 hallazgos contra el copy de producción |
| 2026-08-22 | **Modelé la excepción del Pricing como una condición de LÍNEA y con eso el guard reportó como infracción justo al fine print que la hace legítima.** La decisión del 04-ago tiene dos partes: el beneficio marcado "beta" y el fine print del framing. Yo exigí el `badge: "beta"` en la misma línea del hallazgo, así que la segunda mención del archivo —*"La lectura de correos se habilita al activar Pro... Es opcional y la conectas tú"*, o sea el cumplimiento— salió como violación. Un guard que marca el cumplimiento de la regla como incumplimiento se desactiva al día siguiente | `verde-mas-ancho-que-lo-que-protege` (al revés: rojo más ancho) | El propio guard, en su primera corrida | No, es código nuevo. Lo que sí había era la decisión escrita en el CLAUDE.md, que dice "marcada [beta] **y con el fine print**": las dos partes estaban ahí y yo implementé una | La condición pasa a ser del ARCHIVO y son tres: el beneficio con su badge, el fine print de Pro y el de opt-in. Probado quitando cada una: sin badge 1 fallo, sin fine print 2. Una excepción vale mientras se cumplan los términos con que se concedió |
