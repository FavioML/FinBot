{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cmd=$(jq -r .tool_input.command); case \"$cmd\" in *\"git commit\"*) bash .claude/hooks/pre-commit.sh ;; *) exit 0 ;; esac'"
          }
        ]
      }
    ]
  }
}
